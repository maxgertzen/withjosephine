# Josephine Soul Readings — Changelog

Historical ledger of shipped work. Active state, parked work, and durable rules live in `CLAUDE.md` at the project root. Add to this file when a PR merges; do not edit shipped entries.

---

## PR ledger

| PR | What | Status |
|----|------|--------|
| **PR #269** | **T4b `sjbq2dhs` — error-boundary stories (NotFound, ErrorBoundary, GlobalError).** Per the error-boundary carve-out in `docs/CONTAINER_PRESENTATIONAL.md`, the 3 error surfaces are exempt from View extraction. Stories duplicate the production JSX inline: `Pages/NotFound` uses `NOT_FOUND_PAGE_DEFAULTS` + a custom-copy variant (async server component can't be imported under T1b, so inline duplication is the documented cost); uses StyleProvider normally. `Pages/ErrorBoundary` imports `ErrorPage` from `./error` directly (`"use client"` + synchronous); two Error fixtures + stub `reset` noop; opts out of `<StyleProvider>` via `parameters.styleProvider: false`. `Pages/GlobalError` inline `GlobalErrorRender` mirrors the body content but drops `<html>/<body>` wrapping (Storybook iframe constraint); inline CSS scoped via `.global-error-root` so it can't bleed; opts out of `<StyleProvider>`; file carries the same `eslint-disable @next/next/no-html-link-for-pages` as the production file so `<a href="/">` passes lint. T1b stories-import-graph: 29/29 stories clean. Pre-push gates green: lint 0 errors (5 pre-existing warnings), typecheck, vitest, storybook:build. `code-review --effort high` SHIP; `simplify --scope diff` no findings. | ✅ shipped 2026-06-05 to `release/v1.9.0` (#269, squash `df6acd1`) |
| **PR #268** | **T4a `1rr9f1cc` — LegalPageLayout stories with 3 fixture variants.** LegalPageLayout already conformed to the container/presentational convention (it's the shared layout consumed by all 3 legal routes per the "When NOT to split" list in the convention doc), so this is purely additive: a single new `src/components/LegalPageLayout/LegalPageLayout.stories.tsx`. Three named stories (`Privacy`, `Terms`, `RefundPolicy`) each pass realistic but shortened body content inline as `children`, demonstrating how the layout slots arbitrary children alongside the tag/title/lastUpdated frontmatter. Uses the locked `Layouts/LegalPageLayout` taxonomy group from `docs/STORYBOOK_TAXONOMY.md` (landed in #267). T1b gate: 27/27 stories clean. Pre-push gates green; `code-review` SHIP; `simplify` no findings. | ✅ shipped 2026-06-05 to `release/v1.9.0` (#268, squash `93e0a47`) |
| **PR #267** | **T4d `z30nev3r` — Storybook folder taxonomy audit + hono security bump.** Adopts a closed-set 9-group taxonomy (`Pages`, `Layouts`, `UI`, `Forms`, `Navigation`, `Content`, `Feedback`, `Decorative`, `Smoke`) documented at `docs/STORYBOOK_TAXONOMY.md`. Doc enumerates each group's semantic intent, the full per-story mapping, and a decision tree for picking a group for new stories. 12 story `meta.title` strings rewritten via sed: `Components/{ContactForm,FaqSection,Hero,HowItWorks,ReadingCard,TestimonialCard}` → `Content/*`; `Components/{Footer,Navigation}` → `Navigation/*`; `Components/SectionHeading` → `UI/SectionHeading`; `Form/{DateTimePicker,PickerStacking,Select}` → `Forms/*`; `Gifts/GiftStatusPill` → `Feedback/GiftStatusPill`. `Pages/*`, `Decorative/*`, `Feedback/Loader`, `UI/Button`, `UI/GoldDivider`, `Smoke/RoutingHooks` already conformed. No code-shape changes. Piggyback: bumped hono devDependency from 4.12.18 to 4.12.23 to clear GHSA-f577-qrjj-4474 + GHSA-xrhx-7g5j-rcj5 advisories that landed against the locked 4.12.18 after the last release/v1.9.0 push CI on 2026-06-02 and were tripping security-audit / osv-scan; hono is pure devDep with zero `from "hono"` import matches in `src/` or `scripts/`. Pre-push gates green; `code-review` SHIP; `simplify` no findings on the taxonomy commit. | ✅ shipped 2026-06-05 to `release/v1.9.0` (#267, squash `f70aa75`) |
| **PR #266** | **T3e `68ggveqr` — `/(authed)/gift/intake` container/presentational split.** 139-LOC page split into container (81 LOC) + `deriveGiftIntakeViewProps.ts` (56 LOC) + `GiftIntakeView.tsx` (88 LOC presentational RSC) + `GiftIntakeView.stories.tsx` (3 fixtures: ReturnVisit, WelcomeArrival, NoRecipientEmail) + `deriveGiftIntakeViewProps.test.ts` (7 tests). Container chain preserved verbatim: `export const dynamic = "force-dynamic"` (cookies()-bound), gift-claim cookie verify, `redirect("/gift/claim?expired=1")` on missing/expired cookie, `findSubmissionById` → `notFound()` if missing or not isGift, `redirect("/gift/already-submitted")` if already claimed, parallel `fetchBookingForm` + `fetchGiftIntakePage` + `fetchReading` after the slug is resolved. Derive helper covers Sanity-copy fallback to `GIFT_INTAKE_PAGE_DEFAULTS`, `{readingName}` + `{recipientName}` token substitution (welcome path only — the non-welcome heading never advertised the token, so a literal stays literal), and section filtering via `filterSectionsForReading`. Route-specific labels (empty `nonRefundableNotice`, "Sending your answers…", "Send my answers", `redeemSuccessUrl` derived from `submissionId`) live inside the View, not the prop bag — they're never sourced from Sanity. Only Sanity labels (`nextLabel`, `saveLaterLabel`, `pageIndicatorTagline`) flow through `formLabels`. T3c (IntakeForm refactor) stays won't-do; the form is consumed as-is. 26 stories pass T1b gate locally. Full PR-level CI green: lint+typecheck, test, storybook, security-audit, osv-scanner. | ✅ shipped 2026-06-02 to `release/v1.9.0` (#266, squash) |
| **PR #265** | **T3b `8e5grog2` — `/book/[readingId]/letter` container/presentational split.** 141-LOC page split into container (56 LOC) + `deriveLetterViewProps.ts` (46 LOC) + `LetterView.tsx` (100 LOC RSC, illuminated-letter card) + `LetterView.stories.tsx` (2 fixtures: SoulBlueprint, ShortContent) + `deriveLetterViewProps.test.ts` (6 tests). No behavioral change — `generateMetadata` and its load-bearing `robots: { index: false, follow: false }` no-index gate are preserved. Drop-cap split (`dropCapCta.charAt(0)` + `slice(1)`) kept inside LetterView as a rendering concern; helper passes the raw string. 25 stories pass T1b gate locally. Full PR-level CI green. | ✅ shipped 2026-06-02 to `release/v1.9.0` (#265, squash) |
| **PR #264** | **T3a `v56gbpqw` — `/book/[readingId]` container/presentational split.** First Tier-3 refactor under epic `k7snhn1p`. 234-LOC page split into container (60 LOC) + `deriveBookingEntryProps.ts` (70 LOC) + `BookingEntryView.tsx` (170 LOC RSC) + `BookingEntryView.stories.tsx` (2 fixtures: SoulBlueprint, AkashicRecord) + `deriveBookingEntryProps.test.ts` (7 tests over null + Sanity + defaults matrix). No behavioral change — `generateMetadata` path untouched, the existing 9 `page.test.ts` metadata tests pass unchanged. The Sanity-missing → `ENTRY_PAGE_DEFAULTS` / `BOOKING_INFO_DEFAULTS` fallback matrix that previously lived inline in `page.tsx` is now behind `deriveBookingEntryProps(input): BookingEntryViewProps \| null` with explicit per-field assertions. View is RSC and pure-prop; client-only descendants (`TrackedLink`, `EntryPageView`) nest in the server tree unchanged. 24 stories pass T1b gate locally. Full PR-level CI green. | ✅ shipped 2026-06-02 to `release/v1.9.0` (#264, squash) |
| **PR #263** | **T1b `i9xgm11t` — stories-import-graph CI gate.** New `scripts/check-stories-import-graph.mts` walks each `src/**/*.stories.tsx` transitive import graph using the TypeScript compiler API; fails the storybook CI job when any story directly or transitively imports `@opennextjs/cloudflare`, `next/headers`, `next/cache`, or `server-only`. Wired into the existing `storybook` job before `pnpm storybook:build`, so a workerd-binding leak from a future View refactor blocks deploy. Semantics: **transitive-binding-reach**, not folder-path-prefix (Max-approved this session — pure helpers under `src/lib/booking/*` like constants/giftStatus/sectionFilters remain story-safe; the convention doc justifies the rule as "(these reach bindings)" and this gate enforces that intent semantically). Type-only imports erased correctly (both `import type` and `import { type X }`). Re-exports walked. Dynamic imports with string-literal specifiers walked. Memoised visited-set. 13 unit tests cover direct hits, transitive chains, type-only erasure (both syntaxes), re-export passthrough, dir/index resolution, dynamic imports, memoisation, multi-violation reporting, and file-finder skipping `node_modules` + dot-dirs. New `pnpm check:stories-imports` npm script. 23/23 live stories pass the gate clean at ship. Unblocks all Tier-3 per-page View extractions under the epic. | ✅ shipped 2026-06-02 to `release/v1.9.0` (#263, squash `c4064bc`) |
| **PR #262** | **T1+T2 container/presentational + StyleProvider + v1.9.0 Storybook runtime fixes.** Atomic single PR shipping the architectural convention + the 4 v1.9.0 runtime regression fixes that had blocked the release-to-main merge. Container/presentational convention locked at `docs/CONTAINER_PRESENTATIONAL.md` and added to the project CLAUDE.md required-reading list (page.tsx is container, XxxView.tsx route-folder co-located, workerd-binding import-graph rule, error-boundary carve-out, Storybook contract). `<StyleProvider>` at `src/components/StyleProvider/` is single source of truth for theme + font wiring: exports `styleProviderClassName` (applied to `<body>` in `src/app/layout.tsx`, preserves CSS cascade for `font-body` utility) AND a `<StyleProvider>` component used as global preview decorator in `.storybook/preview.tsx`. `.storybook/storybook-fonts.css` deleted (set wrong CSS var names). Webpack alias in `.storybook/main.ts` switched to `require.resolve` + `resolve.symlinks: false` + sub-path aliases (`react/jsx-runtime`, `react/jsx-dev-runtime`, `react-dom/client`, `react-dom/server`) to traverse pnpm symlinks correctly — fixes HeadManagerProvider `useMemo` null crash. Defensive `useLinkStatus` fallback in `src/components/NavigationButton/LinkSpinner.tsx` (`import * as NextLink` + `?? () => ({ pending: false })`) works in production (Next 16 ships hook) AND Storybook (`@storybook/nextjs` mock doesn't); webpack-alias approach was tried and abandoned because the framework wins over user alias. New shared `withBookingPageShell` decorator at `.storybook/decorators/BookingPageShell.tsx` imports REAL `BookingFlowHeader` + `Footer` from production (single source of truth); per-story copy via `parameters.bookingPageShell` ({eyebrow, title, subtitle, backHref}); applied to IntakeForm + GiftForm stories. Reorient (research-driven): T3c (IntakeForm refactor) + T3d (GiftForm refactor) CANCELLED won't-do per common-practices research (Storybook docs, Claffey 2025, sb.mock blog, storybook-rsc-demo) — keep useState + custom hooks in component, mock via `sb.mock` in `.storybook/preview.ts`, wrap in shared page-shell decorator. Real-browser Playwright smoke: 13 stories PASS (6 ThankYouView, 4 IntakeForm, 1 GiftForm, 2 sanity), zero console errors, `__variable_` count = 1 on every story, page chrome present where expected. CI green: lint+typecheck, test (2257/2257), storybook, security-audit, osv-scanner. 4 new memory files captured (`feedback_research_common_practices_before_refactor`, `feedback_defensive_source_over_webpack_alias`, `feedback_council_research_in_parallel`, `reference_styleprovider_and_container_presentational`). PRD + COUNCIL at `MEMORY/WORK/20260602-095014_container-presentational-split-prd/`. Dex epic `k7snhn1p`: 4 closed (T1a, T2a, T2b, T2c), 2 cancelled won't-do (T3c, T3d), 4 still open for next session (T1b import-graph CI script, T3a/T3b/T3e/T3f per-page refactors that remain warranted, T4a-T4d v1.10+ polish). | ✅ shipped 2026-06-02 to `release/v1.9.0` (#262, squash `b1207d7`) |
| **PR #252** | **Release v1.8.0 → `main`.** Cumulative release of the v1.8.0 follow-up hardening arc. 5 v1.8.0-core sub-PRs (#246, #247, #248, #249, #250) squash-merged into `release/v1.8.0`, 2 cumulative simplify polish commits (`53a2c5c`, `30ab663`) folding 10 findings from the 3-vantage pass on the arc diff, PR #251 test-only coverage backfill landed on top, plus dex auto-close bot commits + bookkeeping. Sub-PR contents already in the ledger: #246 (`scripts/_lib` helpers + `pickDefined` rollout + CSP audit), #247 (sanity-mirror TTL cache + in-flight Promise coalesce + `patchSingleton`/`setIfMatchesDefault` + `audit-defaults-drift.mts`), #248 (studio next-image badge + font fallback + paint guards + workerd edge test), #249 (gift retry + `purchaserSuppliedRecipientName` + `recipientNameFor` listen-page wiring), #250 (sandbox suite `workers: 2`, 14min→10m26s), #251 (7 unit/component/e2e tests for v1.8.0 surfaces, no production code). No Sanity migration or Studio re-deploy required this arc. Full CI green at merge: Playwright chromium 3m56s, GROQ content contract 37s, test, lint+typecheck, storybook, security-audit, sanity-validate-staging, deploy-staging all SUCCESS. Tagged `v1.8.0` at `e6c5bd1`. `release/v1.8.0` deliberately KEPT until post-merge smoke completes. | ✅ shipped 2026-06-01 (#252, squash `e6c5bd1`, tag `v1.8.0`) |
| **PR #251** | **`test(v180): backfill coverage for v1.8.0 surfaces` (7 dex tasks).** Test-only sub-PR on `release/v1.8.0`. 5 unit / component tests + 2 e2e assertions, no production code touched. Unit: `purchaserSuppliedRecipientName` branches in `giftPersonas.test.ts` (`9vb4iz95`); `isMainModule` matches / diverges / undefined argv[1] in new `scripts/_lib/main.test.ts` (`nvbgx1n8`); `patchSingleton` + `setIfMatchesDefault` mutate-null skip / mutate commit / Becky-edit preserve / missing-singleton skip in new `scripts/_lib/seedSingleton.test.ts` (`qqfj212c`); Studio `next-image` stub wrapper + badge + `data-preview-image-hidden` + fill-mode dimensions in new `studio/preview-stubs/next-image.test.tsx` (`jmt52c7r`); `readingRefCache` in-flight Promise coalesce + null-resolution cache-drop in `sanityMirror.test.ts` (`ry40itf2`). E2E: `listen-recipient-greeting` renders "Mira" post-redeem in `gift-roundtrip.spec.ts` recipient-listen test, absent (`toHaveCount(0)`) in `listen-roundtrip.spec.ts` self-purchase (`6hfzgo13`); gift-intake H1 wiring locked via no-literal-`{recipientName}`-leak in `gift-roundtrip.spec.ts` scheduled (vjtos13w soft form: default + current Sanity `headingWelcome` don't include the token; substitution is Becky-opt-in; helper unit tests cover the substitution path itself). Pre-push gates green: lint clean, typecheck clean, vitest 2247/2247. `code-review --effort high` caught one real bug pre-push (greeting assertion expected `recipientFirstName` but `seedGiftIntakeDraft` hardcodes `first_name: "Mira"`); fixed in-diff to "Mira". 3-vantage `/simplify --scope diff` (reuse / quality / efficiency): 1 finding accepted (dropped duplicate `fetch rejects` test, observable-identical to `fetch resolves null`); 5 lows rejected (reuse rejected fakeClient helper as DRY-for-DRY; mock pattern matches sanityMirror.test.ts for consistency). CI: first push e2e failed on the too-strong heading assertion; second push (softened to wiring-only) e2e green; third push (drop dex IDs from describe labels + trim narration comments) green. Squash-merged at `4855a57`; dex auto-close GH Action fired on merge, closed all 7 dex tasks, committed `.dex/tasks.jsonl` update at `b0ef59a` direct on `release/v1.8.0` (corrects an earlier mis-belief that the workflow only fires on PR-to-main — workflow YAML has no branch filter; recent runs against release/v1.8.0 all succeeded). | ✅ shipped 2026-06-01 to `release/v1.8.0` (#251, squash `4855a57`) |
| **PR #245** | **Post-v1.7.0 quick-win batch (8 dex tasks).** Single PR squash-merged at `6f3a0c1` with 10 commits (8 fixes + 2 simplify follow-ups). Drops `ConfirmArmedButton` destructive variant arm (`m7e8iz7y`; no callsite remained after PR #214). Adds `PortableTextBody` unknown-mark plain-text regression test (`vf1seylg`; locks the silent-drop behavior). Adds `EmailPreview` iframe key-remount regression test in `studio/views/EmailPreview.test.tsx` plus vitest `dedupe: ["react", "react-dom"]` so the studio sub-package doesn't load a second React copy (`dn17560j`). Tightens `mirrorSubmissionPatch` to a discriminated union: `art9AcknowledgedAt` now requires `readingSlug` at the type level, and a runtime throw covers type-bypass callers so missing-slug becomes a loud failure instead of silently writing the wrong Art. 9 label (`d5y8qzl5`). Drops 5 reminder-split legacy fields + the empty `legacy` group from `emailGiftClaim` schema (`0h4gbkdo`; `LEGACY_FIELDS_BY_SINGLETON` already lists them so the drift detector ignores the persisted Sanity data harmlessly). Removes the now-resolved `GHSA-jxxr-4gwj-5jf2` brace-expansion ignore from `osv-scanner.toml` (`f9d5zdim`; the lockfile only ships patched versions 1.1.14 / 2.1.0 / 5.0.6). Extracts `SANDBOX_EMAIL_PREFIXES` + `SANDBOX_DOMAIN` to a typed object in `src/lib/booking/sandboxEmails.ts` consumed by 7 Playwright specs and `isSandboxEmail` (`65udsxnp`; a typo or rename now breaks TypeScript compile instead of re-opening the Resend quota-leak failure mode from 2026-05-24). Un-gates the `UA_AUDIT_HASH_DEPLOYED` `test.skip` block on `listen-roundtrip.spec.ts` (`ztpk6mz7`; staging is on v1.7.0 which threads `user_agent_hash` end-to-end). Plus 2 simplify-pass follow-ups: runtime throw if art9 patched without `readingSlug` + updated `env_guard` warning message in `resend.tsx` to point at the new module path. Pre-push gates green: lint 0 errors, typecheck clean, vitest 2160/2160. Post-merge bookkeeping closed epics `23ctexvw` + `dv03dcbj`; reparented 3 still-open subtasks (`09z53mtu`, `5fqrgvia`, `qz94q5g2`) to top-level so they remain discoverable during grooming. | ✅ shipped 2026-06-01 (#245, squash `6f3a0c1`) |
| **PR #244** | **Release v1.7.0 → `main`.** Cumulative release of the v1.7.0 pre-launch hardening arc: 3 sub-PRs (#241, #242, #243) squash-merged into `release/v1.7.0`, 2 CI/bookkeeping commits (`c76387e`, `f1e2fb4`), and the cumulative 3-vantage `/simplify` polish commit `6a0065f`. 6 commits squash-merged at `62a71d0` and tagged `v1.7.0`. Sub-PRs already in the ledger: #241 (`pickDefined` null-filter on Sanity-merge spread sites), #242 (sanity-validate schema-additive drift detector + 30 myGiftsPage contract additions), #243 (Phase 2 security hardening: bfcache + fragment defense in `redactSearchParams` + user-exists guard at `/api/library/redeem`). Plus `6a0065f` simplify polish (2 findings: parallelize `findUserById` + `getRequestAuditContext` after the `verifyLibraryToken` gate in the redeem route; swap `\|\|` to `??` on the three `/my-readings/welcome` fallbacks so PR #241's `pickDefined` empty-string preservation invariant is not defeated). No Sanity migration or Studio re-deploy required this arc (PR #242's 30 contract additions are script-side; no schema change). Post-merge: tagged `v1.7.0` at `62a71d0`; 4 release branches (`release/v1.4.0`, `release/v1.5.0`, `release/v1.6.0`, `release/v1.7.0`) deleted local + remote; tags preserve every SHA. | ✅ shipped 2026-06-01 (#244, squash `62a71d0`, tag `v1.7.0`) |
| **`6a0065f` (simplify polish, no PR)** | **Cumulative 3-vantage `/simplify` pass on `release/v1.7.0 → main` diff per `feedback_simplify_scale_to_change_size`.** 3 parallel Engineer agents (reuse, quality, efficiency vantages) with the canonical-helpers brief per `feedback_canonical_helpers_before_agent_dispatch`. 2 findings accepted: (a) parallelize `findUserById` + `getRequestAuditContext` in `src/app/api/library/redeem/route.ts` after the `verifyLibraryToken` gate via `Promise.all` (trims one D1 round-trip per redemption); (b) swap `\|\|` for `??` on the three `welcomeHeading`/`welcomeSubhead`/`welcomeButtonLabel` fallbacks in `src/app/(authed)/my-readings/welcome/page.tsx` so PR #241's `pickDefined` empty-string preservation invariant survives the per-field fallback. Rejected (7, reasoned in `MEMORY/WORK/20260601-050720_v170-release-to-main/FINDINGS.md`): `redactInParams` micro-tightening (sub-millisecond, below floor); CI-only `sanity-validate.mts` Promise.all (would touch beyond v1.7.0 hunks); out-of-diff `pickDefined` rollout to 5 component files (deferred dex follow-up); verify-then-load-user convergence with magic-link route (out of diff); shared Vary+Cache-Control helper (net 1 LoC saved, abstraction would leak `Clear-Site-Data`); fragment-substring over-match (agent self-rejected); mock-helper type-quality nit (below floor). Net diff: +7 / −5 LoC across 2 files. Gates green pre-push: typecheck clean, lint 0 errors (5 pre-existing warnings, 0 new), vitest 2156/2156, build clean. | ✅ shipped 2026-06-01 to `release/v1.7.0` (commit `6a0065f`) |
| **PR #243** | **`e737za2a` Phase 2 security hardening.** Three defense-in-depth additions deferred from PR #206 review. (a) bfcache: middleware sets `Vary: Cookie` on `/my-readings/*`; redeem-route success redirect adds `Clear-Site-Data: "cache"`. (b) Fragment defense in `src/lib/logging/redactSearchParams.ts`: new private `redactFragmentParams` + shared `redactInParams` helpers redact sensitive params landing in URL fragments (e.g. `#t=...`). Sentry's browser SDK captures full URLs including fragments; this closes that exfil path. (c) User-exists guard at `/api/library/redeem`: `findUserById(verify.userId)` must return non-null before `createListenSessionForUser`. Tokens valid for GDPR-purged users fall through to the uniform 303 (preserves the route's locked-design "no information leak via response shape" invariant). +6 tests. P3-2 ipHash HKDF subkey migration explicitly deferred — different concern, requires audit-log re-keying. | ✅ shipped 2026-05-31 to `release/v1.7.0` (#243, squash `ac3a612`) |
| **PR #242** | **`thdesb5b` schema-additive drift detector + 30 contract additions.** New `scripts/sanity-validate-drift.mts` walks each fetched singleton's actual document keys; warns on any field present on the doc but absent from the contract. Wired into `runSingletonChecks` as a third output stream; logged as `[SCHEMA-DRIFT]` (soft signal); `--strict` flag promotes to hard CI fail. `describeActual` extracted into `scripts/_lib/sanityValueShape.mts` so `[DRIFT]` + `[SCHEMA-DRIFT]` log formats can't diverge. 30 `myGiftsPage` string fields added to contract (edit-recipient drawer, flip-to-scheduled, send-now, resend-throttle, action error surfaces — all verified `type: string` against `studio/schemas/myGiftsPage.ts`). `SANITY_META_FIELDS` covers the six documented system-prefixed keys; `LEGACY_FIELDS_BY_SINGLETON` mirrors only schema-declared `group: "legacy"` flags (emailGiftClaim only). Honest dry-run after this PR: drift=0 missing=0 schema-drift=14 across 24 singletons. 14 remaining drifts split into: 10 `_system` object fields on landing/thankYou/siteSettings + 7 email singletons (likely Sanity plugin metadata, not investigated yet), 4 stale fields on emailGiftPurchaseConfirmation{Scheduled,SelfSend} (`detailLineScheduled`, `detailLineSelfSend`, two `greetings`) — pre-collapse schema leftovers. 9 unit tests. Recurrence prevention for `feedback_static_fallbacks_can_mask_outages`. | ✅ shipped 2026-05-31 to `release/v1.7.0` (#242, squash `a97929f`) |
| **PR #241** | **`j707svbs` defensive null-filter on Sanity-merge spread sites.** New helper `src/lib/sanity/pickDefined.ts` strips null + undefined while preserving empty string + zero + false (Becky-edit semantics). 19 spread sites converted from `{...DEFAULTS, ...(x ?? {})}` to `{...DEFAULTS, ...pickDefined(x ?? {})}` across `loadLibraryData` (x2 — load-bearing for stacked section headings post-PR #208), `welcome` / `listen` / `auth/verify` / `gift/intake` pages, `lib/resend.tsx` (x11), `lib/sanity/fetch.ts`, `lib/emails/render-preview.tsx` (cast tightened to `Partial<typeof PREVIEW_DEFAULTS[K]>`). Risk just escalated by the 2026-05-31 prod seed of 7 new singletons Becky will edit; any null she accidentally sets would have blanked the default. 9 unit tests + 3 integration-shape tests against `loadLibraryData` asserting the real `MY_READINGS_PAGE_DEFAULTS` + `MY_GIFTS_PAGE_DEFAULTS` survive an all-null Sanity payload. | ✅ shipped 2026-05-31 to `release/v1.7.0` (#241, squash `a73a043`) |
| **`c76387e` (CI config, no PR)** | Enable CI + auto-deploy-staging on `release/v1.7.0`. Mirrors v1.6.0 pattern (extends `pull_request.branches` + push-to-release/* deploy-staging condition). | ✅ shipped 2026-05-31 to `release/v1.7.0` (commit `c76387e`) |
| **`6fe47bd` (dex bookkeeping, no PR)** | Close `u0lgsw47` (recipient_user_id corruption data repair) as hold-gate-cleared. Operator dry-runs of `scripts/repair-recipient-user-id.mts` confirmed production carried 0 corrupted rows; the bb5fe157 vector never reached prod. PR #170 instrumentation remains live. Sub-PR B `rncenffc` (CheckEmailCard polish) also closed as deferred-indefinitely. | ✅ shipped 2026-05-31 to `main` (commit `6fe47bd`) |
| **PR #240** | **Release v1.6.0 → `main`.** Cumulative release of the v1.6.0 arc: 10 sub-PRs (#228–#239) squash-merged into `release/v1.6.0`, 2 CI bookkeeping commits, and the cumulative 3-vantage `/simplify` polish commit `7219b5f`. 12 commits squash-merged at `24b53a0` and tagged `v1.6.0`. Sub-PRs already in the ledger: #228 (Hero scroll-down a11y), #229 (DatePicker arrow alignment), #230 (GiftStatusPill hydration + `useIsClient` hook), #231 (404 + under-construction defaults centralized), #232 (Sanity theme color-shape repair script), #233 (comment-narration scanner), #234 (Resend Layer-3 env-gated allowlist), #235 (brand Radix Select primitive in pickers + FormSelect rename), #236 (dex auto-close GH Action), #237 (`dex-audit.mts` reconciler), #238 (gift scheduled preview copy refinement + Sanity migration), #239 (picker Select integration tests). Plus `7219b5f` simplify polish (10 findings: shared `createSelectDropdown(contentNode)` adapter consumed by DatePicker + DateTimePicker, `useMemo`'d `dayPickerComponents`, identity `.map` drop in FormSelect, em-dash + single-hyphen substitutions, `useIsClient` SSR/client test, single-`git-log` hoist in `dex-audit.mts`, single-`dex-list` load in `dex-auto-close.mjs`). Post-merge: tagged v1.6.0; Sanity seed on both datasets (staging: 2 created; **prod: 7 created** because `listenPage`/`myReadingsPage`/`magicLinkVerifyPage`/`emailSharedShell`/`emailStepUpOtp`/`emailNewDeviceNotice` had never been seeded on prod across earlier sessions — static-fallback pattern was masking the gap); theme color repair on both (3 each — **prod carried the same nested-hex bug**, defensive `extractHex` was masking on rendering side); gift scheduled-preview migration on both (misaligned phrase rewritten); Studio deployed (2/2 schemas, ~11s). | ✅ shipped 2026-05-31 (#240, squash `24b53a0`, tag `v1.6.0`) |
| **`7219b5f` (simplify polish, no PR)** | **Cumulative 3-vantage `/simplify` pass on `release/v1.6.0 → main` diff per `feedback_simplify_scale_to_change_size`.** 3 parallel Engineer agents produced 36 candidate findings; 10 accepted after dedup + binding-rule validation (reconciled in `MEMORY/WORK/20260531-130513_v160-simplify-and-merge-pr/FINDINGS.md`). Reuse: extract DayPicker `Dropdown` adapter into shared `createSelectDropdown(contentNode)` in `src/components/Form/DayPickerShared/`, drop identity `.map` in `FormSelect.tsx`. Quality: em-dash → colon in `Select.stories.tsx` caption; single-hyphen sentence dividers → comma/colon in two `defaults.ts` strings (`NOT_FOUND_PAGE_DEFAULTS`, `UNDER_CONSTRUCTION_PAGE_DEFAULTS`); add `useIsClient.test.tsx` covering SSR (`renderToString` returns false) + client (jsdom `renderHook` returns true). Efficiency: `useMemo` the `dayPickerComponents` object in both pickers so `react-day-picker` doesn't see a fresh `components` prop on every parent render; `scripts/dex-audit.mts` hoists `git log` to a single subprocess at startup with in-memory regex filter per task (was 2× spawn per id); single-pass `partitionPathsByExistence` instead of double `existsSync` filters; `.github/scripts/dex-auto-close.mjs` replaces per-id `dex show` shell-out with one `dex list --flat --json` load. Rejected (13): pre-existing em-dashes outside v1.6.0 hunks (minimal-scope), FormSelect-to-Select picker-migration (scope creep), agent-overclaimed test gap, useIsClient JSDoc trim (the WHY is non-obvious per CLAUDE.md), plus low-value or withdrawn findings. Net diff: +137 / −85 LoC across 9 files. Gates green: lint 0 errors, typecheck clean, vitest 2127/2127, main build clean, studio build clean (~12s). | ✅ shipped 2026-05-31 to `release/v1.6.0` (commit `7219b5f`) |
| **PR #239** | **Radix Select picker integration tests.** 3 TimePicker tests + 1 DatePicker test exercising the new Radix Select primitive inside each picker's popover. Locks the ARIA contract via `byRole("combobox", { name: "Hour" / "Minute" / "Month" / "Year" })` — any future picker refactor that breaks the Select integration fails these jsdom tests before reaching CI Playwright. Closes dex `61lzvbjt` (a11y audit) by inspection — Radix gives correct listbox/option semantics; outer input `role="combobox" + aria-haspopup="dialog" + aria-controls + aria-expanded` verified. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#239, squash `96b2a7e`) |
| **PR #238** | **`zkib97ns` — refine scheduled-gift preview copy.** `src/lib/emails/preview-fixtures.ts` `myGiftsUrl` moved from `/my-gifts` (now a 308 redirect) to `/my-readings`. New set-if-matches Sanity migration `scripts/migrate-gift-purchase-confirmation-scheduled-preview-2026-05-31.ts` rewrites Becky's prod-edited `emailGiftPurchaseConfirmationScheduled.preview` from "You don't need to do anything else." (misaligned post-PR #214 — purchaser CAN still manage scheduled gifts) to the code-default phrasing IF the text still matches that exact phrase (idempotent; never clobbers a subsequent Becky edit). 2 email-shell byte-equality snapshots regenerated for the new URL. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#238, squash `6ff253a`) |
| **PR #237** | **`scripts/dex-audit.mts` reconciler.** Read-only sweep. Classifies open dex tasks into `likely-done` (explicit Closes-commit + no open children), `stale-orphan` (old + all paths missing + no open children), `needs-review`. Companion to the auto-close GH Action — together they're the durable mechanical fix for recurring stale dex tasks. First run flagged 8 likely-done (the v1.6.0 sub-PRs), all manually closed during bookkeeping. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#237, squash `7e5d1ed`) |
| **PR #236** | **dex auto-close GH Action.** `.github/workflows/dex-auto-close.yml` + `.github/scripts/dex-auto-close.mjs`. Listens for `pull_request.closed` with `merged==true`; parses PR body + squash-merge commit for `Closes/Fixes/Resolves dex <id>`; runs `dex complete <id> --commit <merge_sha> --force` per match; commits the updated `.dex/tasks.jsonl` back to the base branch. Idempotent — already-completed tasks skip cleanly. Stops the recurring stale-dex pattern (`feedback_dex_create_verify_with_show`) at the source. No human discipline required. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#236, squash `a507bf6`) |
| **PR #235** | **`50dqawuf` — Radix Select primitive + FormSelect rename.** New `src/components/Form/Select/` (Radix-backed primitive, trigger + listbox + portal, listbox width pinned to trigger via `var(--radix-select-trigger-width)`). Existing native-wrapper renamed to `src/components/Form/FormSelect/` and migrated to use `Select` internally — IntakeForm Sanity-driven dropdowns get the visual upgrade too. TimePicker swaps both hour + minute selects; DatePicker + DateTimePicker pass a custom `Dropdown` component to react-day-picker's `components` prop. All pickers pass the popover content node as `portalContainer` so the listbox renders inside the popover stacking context. Hidden input on FormSelect preserves form-submission semantics. +4 Select tests, +1 FormSelect hidden-input test. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#235, squash `c80ad43`) |
| **PR #234** | **`5f0bqd2d` — Layer-3 env-gated production allowlist in `src/lib/resend.tsx`.** In non-production envs (`NEXT_PUBLIC_SANITY_DATASET !== "production"`), rejects all Resend sends to recipients not on a tiny prod allowlist (`hello@withjosephine.com`, `maxgertzen@gmail.com`, `NOTIFICATION_EMAIL`; `+addressing` stripped before matching). New `SkipReason` `env_guard` fires before the existing flag + header checks. `beforeEach` in `resend.test.ts` stubs `NEXT_PUBLIC_SANITY_DATASET="production"` so the 73 existing tests keep their original code path. +6 env_guard tests. Belt-and-suspenders defense after the 2026-05-28 Resend leak (layers 1+2 — prefix list + scanner — still hold; this makes drift structurally impossible). | ✅ shipped 2026-05-31 to `release/v1.6.0` (#234, squash `6a07267`) |
| **PR #233** | **`5cyibtb0` — comment-narration vitest scanner.** `src/scanners/commentNarration.test.ts` walks `src/` (skipping `.test.`, `.stories.`, `.generated/`, `.next/`, `scanners/`) for `.ts` / `.tsx` files; fails CI on `Phase \d+`, `dex [a-z0-9]{6,}`, `epic [a-z0-9]{6,}`, or date-prefix narration inside `//` comments. Recurrence-prevention for `feedback_comments_over_logged`. 0 violations across `src/` at ship. Multi-line `//` block detection arm explicitly deferred — 33 pre-existing candidates need a content-review pass, activating now would block CI on unrelated code. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#233, squash `08b55ca`) |
| **PR #232** | **`axfyutyx` — Sanity theme color-shape repair script.** `scripts/repair-theme-color-shape-2026-05-31.ts` walks the theme singleton's `blush`/`ivory`/`rose` color fields, detects the recursively-nested `{hex:{hex:{hex:{hex:"#…"}}}}` shape (from a re-save loop in the Studio colorInput plugin), and patches each to a flat `hex: "#XXXXXX"`. Idempotent — already-flat shapes are skipped. PR #213 already plugged the build-time generator via defensive `extractHex`; this cleans the data itself so Studio + GROQ both reflect flat color shapes. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#232, squash `482c804`) |
| **PR #231** | **`td3f86z6` — centralize 404 + under-construction defaults and seed.** `NOT_FOUND_PAGE_DEFAULTS` + `UNDER_CONSTRUCTION_PAGE_DEFAULTS` added to `src/data/defaults.ts`; both consumers (`src/app/not-found.tsx`, `src/components/UnderConstruction/UnderConstruction.tsx`) now import from the centralized constants — removes the inline-DEFAULTS violation of the "fallback defaults centralized in defaults.ts" binding constraint. Both singletons appended to `scripts/seed-customer-emails-and-pages.mts` for the post-merge seed run (idempotent via `createIfNotExists`). | ✅ shipped 2026-05-31 to `release/v1.6.0` (#231, squash `d84f1f2`) |
| **PR #230** | **`t380yxay` — GiftStatusPill SSR/client hydration fix.** Date-fns `format(parseISO(iso))` renders in the runtime's local timezone; server (Node, CI=UTC) and client (user TZ; Playwright Chromium pinned to America/Los_Angeles) disagreed, firing React "Hydration failed" on every `/my-readings` render with a scheduled gift. Refactored to `"use client"` pill that splits the label into `{label, momentIso?}` and renders the moment portion only when `useIsClient()` returns true. New shared hook `src/lib/hooks/useIsClient.ts` (react-aria `useIsSSR` pattern using `useSyncExternalStore` — server snapshot returns false, client snapshot returns true; not a real store but the React-blessed hydration-safe primitive). Lint clean (React-Compiler eslint flags the older `useEffect+setState` shape). 11 existing tests pass unchanged. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#230, squash `e31eecc`) |
| **PR #229** | **`sqnozgtu` — DatePicker prev/next arrows aligned with month/year dropdowns.** `dayPickerShared.ts` adds `h-9` to both `nav` and `month_caption`. Both rows now occupy 36px with their existing `items-center` so the arrow buttons (`w-8 h-8` = 32px) and native-`<select>` dropdowns share a vertical centerline. DateTimePicker re-uses these classnames and adds `min-h-8` to month_caption — the new `height: 36px` wins over `min-height: 32px`, behavior preserved. | ✅ shipped 2026-05-31 to `release/v1.6.0` (#229, squash `1aba21b`) |
| **PR #228** | **`ym2efbwn` — Hero scroll-down indicator keyboard-accessible.** `motion.div` with `onClick` (mouse-only) gets `role="button"`, `tabIndex={0}`, `aria-label="Scroll to readings"`, and `onKeyDown` that activates on Enter / Space (`preventDefault` on Space to avoid page scroll). `focus-visible:outline` so keyboard users see focus. Hero test motion mock rewritten to spread `...rest` after stripping `initial`/`animate`/`transition` — previously dropped any non-standard props. +4 a11y tests (keyboard-accessible role, Enter activates, Space activates, other keys ignored). | ✅ shipped 2026-05-31 to `release/v1.6.0` (#228, squash `83f3ac9`) |
| **PR #227** | **Release v1.5.0 → `main`.** Cumulative release of epic `mc6p8m8t` (Phase 7 Studio editor surface + email customer-name token plumbing). 12 commits squash-merged at `a72cf2f` and tagged `v1.5.0`. Sub-PRs already in the ledger: #221 (`tokenReferenceField` generalization), #222 (`{firstName}` → privacy export), #223 (`defineLocations` for 12 emails), #224 (magic-link customer-name tokens), #225 (cross-client eval doc), #226 (Studio "Send preview to inbox" action). Plus `27c65aa` cumulative `/simplify` polish + `a2c0678`/`eb3bd16` CI config + `be89605`/`18ac33c`/`5751451` bookkeeping. ALLOWED_PREVIEW_RECIPIENTS provisioned on both worker envs pre-merge. Studio deployed post-merge (2/2 workspaces, 2/2 schemas) so Becky sees the `tokenReferenceField` banner on `listenPage`/`giftIntakePage`/`thankYouPage`, the "Send preview to inbox…" doc action on 12 email singletons, and the `EnvelopeIcon` entries in Presentation's "Used on" panel. Closes dex `mc6p8m8t`. Post-merge bookkeeping also closed dex `kqb7jntj` subtree (PR-D + PR-E shipped in v1.1.0 via PRs #148 + #150 but never marked complete — surfaced by `feedback_dex_create_verify_with_show`). | ✅ shipped 2026-05-31 (#227, squash `a72cf2f`, tag `v1.5.0`) |
| **`27c65aa` (simplify polish, no PR)** | **`/simplify` 3-vantage pass (reuse, quality, efficiency) on cumulative `release/v1.5.0` diff, 12 findings applied in one commit on the release branch.** **3 HIGH:** (1) em-dashes purged from 5 sites (`studio/actions/sendEmailPreview.tsx` × 2 error toasts; `studio/schemas/deskStructure.ts` "Magic Link: Confirm Email Page" relabel; `studio/presentation.tsx` × 2 siteSettings/theme messages + comment); (2) `sendEmailPreview.ts` rewritten to route through canonical `sendOrSkip` in `resend.tsx` so sandbox-prefix guard + RESEND_DRY_RUN + x-e2e-resend-dry-run header + cached client + redacted logging + serverTrack all apply (closes the same divergence shape that caused the v1.2.0 dry-run-leak incident per `feedback_resend_dry_run_paths`); new `EmailSubType.admin_email_preview` + `EMAIL_LABELS` entry; (3) magic-link fallback DRY'd — single `FIRST_NAME_FALLBACK` exported from `src/lib/booking/submissions.ts`; `MAGIC_LINK_VARS_FALLBACK` from `src/lib/auth/magicLinkVars.ts`; redundant `.catch` in `/api/auth/magic-link` route removed because the helper swallows internally. **9 MEDIUM:** new `findMostRecentPaidByRecipientUserId` repo method (`SELECT * FROM submissions WHERE recipient_user_id = ? AND status = 'paid' ORDER BY paid_at DESC LIMIT 1`) replaces the in-memory `.find(paid)` pattern in `magicLinkVars` (existing `listSubmissionsByRecipientUserId` requires `delivered_at IS NOT NULL`, too narrow for magic-link sign-in flow); `TEMPLATE_LABELS` in `sendEmailPreview.ts` promoted from `Partial<Record>` to exhaustive `Record<EmailTemplateKey, string>` and the `?? template` raw-key fallback dropped (no more machine keys leaking to Becky's subject lines on a missed entry); `EMAIL_PREVIEW_SINGLETON_TYPES` derived from `Object.keys(EMAIL_ALLOWED_SLOTS)` so a new email template + slot allowlist auto-enrolls in the send-to-test action; dispatch object literal → switch statement (no per-call object alloc); `/api/admin/list-preview-recipients` 503 JSON shape `{ error }` → `{ outcome: "refused", reason: "preview-not-configured" }` matching the sibling POST route; noisy block-comment narration trimmed to load-bearing claims on both admin routes + `magicLinkVars`; new shared `studio/components/AdminTokenInput.tsx` lifts the labeled password-input block from both `resendCustomerEmailAction` and `sendEmailPreviewAction` (lightweight partial extraction of the broader `<AdminActionDialog>` pattern; full extraction defers to when third sibling lands); `response.data as { id?: string } | null` cast dropped from `sendEmailPreview` (canonical Resend SDK type now flows through `sendOrSkip`); `/api/privacy/export/route.ts` uses `FIRST_NAME_FALLBACK` constant instead of inline `"there"`. Net diff: 17 files, +194 / −173 LoC. Tests 2105/2105. Lint + typecheck + main build + studio build clean. Closes the `/simplify` findings list end-to-end. | ✅ shipped 2026-05-31 to `release/v1.5.0` (commit `27c65aa`) |
| **PR #224** | **`c3l45o0n` — plumb customer-name tokens into magic-link emails.** Refactors `src/lib/emails/MagicLink.tsx` to the `vars + copy applyTokens` pattern shared by every other branded template (drops the destructured-props shape). `sendMagicLink` in `src/lib/resend.tsx` accepts optional `firstName` / `readingName` / `readingPriceDisplay`; subject now goes through `applyTokens(copy.subject, vars)` (matches OrderConfirmation / GiftClaim / Day7Delivery / PrivacyExport). New `src/lib/auth/magicLinkVars.ts` looks up the user's most-recent PAID submission and derives all three vars via existing helpers (`extractFirstName`, `priceDisplayFor`); fail-safe `'there' / ''` fallback for users with no paid submission. Wired into `/api/auth/magic-link` route (the only production `sendMagicLink` call site; `/api/internal/issue-magic-link` is a test-harness seam that returns the raw token, no `sendMagicLink`). Slot allowlists widened: `emailMagicLink` → `[magicLinkUrl, firstName, readingName, readingPriceDisplay]`, `emailMagicLinkLibrary` → `[magicLinkUrl, firstName]`. `extractFirstName` promoted to exported helper in `src/lib/booking/submissions.ts` (same change also lands in PR #222 — identical lines merged cleanly via rebase). Slots test `slot-free template rejection` migrated from `emailMagicLink` to `emailStepUpOtp` (still slot-free). 2098/2098 vitest. Lint + typecheck + main build + studio build clean. Required a mid-arc rebase onto `release/v1.5.0` after PR #222 merged first (single-line slots.ts conflict, union resolution). | ✅ shipped 2026-05-30 to `release/v1.5.0` (#224, squash `b83b4c7`) |
| **PR #226** | **`cfor9w5f` — Studio "Send preview to inbox…" doc action on 12 customer email singletons.** Becky-facing Sanity action that lets her ship the currently-published copy of any email template to a pre-approved test inbox so she can verify cross-client rendering before she trusts the live customer flow. Action UI: paste admin token → loads `/api/admin/list-preview-recipients` on blur → dropdown populated from env-var allowlist → "Send preview" button. Two new admin routes mirror the existing `/api/admin/resend-customer-email` shape (`authorizeAdminToken` + REFUSED(404) on auth fail; 503 on env-unset). `POST /api/admin/send-email-preview` validates the template against `isPreviewTemplateKey` and the recipient against the env-var allowlist (403 if not on the list), then calls `sendEmailPreview` which fetches PUBLISHED Sanity copy + renders via existing `renderEmailPreview` + sends with `[PREVIEW]` subject prefix so a test render to a shared inbox can never be confused with a real customer email. Audit row written per send (`admin_email_preview_sent` with `success=true|false`). Action label flips to "Send preview… (publish first)" and goes `disabled: true` when `props.draft._rev !== props.published?._rev` (preview always sends the *published* copy). New `ALLOWED_PREVIEW_RECIPIENTS` comma-separated env var documented in `wrangler.jsonc`; unset = 503 + dialog shows "not configured" (fail-closed by design — code can land before Max sets the secret). New audit event type `admin_email_preview_sent` (`src/lib/audit/eventTypes.ts`). New `EMAIL_PREVIEW_SINGLETON_TYPES` set in `studio/schemas/deskStructure.ts` (later derived from EMAIL_ALLOWED_SLOTS via simplify); `sharedActions` resolver in `studio/sanity.config.ts` extended so singleton documents in that set get the action appended (other singletons are unchanged — still get the canonical `publish / discardChanges / restore` filter only). 7 new route tests covering happy + auth-missing + auth-wrong + env-unset + recipient-not-allowlisted + template-not-known + send-failure paths. Security model (locked 2026-05-24 by Max): allowlist lives in Worker env vars not Sanity siteSettings — Studio compromise cannot widen the allowlist; rotating recipients = deploy. Per-recipient cooldown explicitly deferred — WAF rate-limit on `/api/admin/*` (5 req/10s/IP from `rl-josephine-api-write-paths`) + ADMIN_API_KEY + env-var allowlist combination is sufficient defense-in-depth at v1.5.0 volume. Closes dex `cfor9w5f`. | ✅ shipped 2026-05-30 to `release/v1.5.0` (#226, squash `0ed9e77`) |
| **PR #225** | **`47w420v0` — cross-client email testing eval doc (`docs/CROSS_CLIENT_EMAIL_EVAL.md`).** Comparison of Litmus, Email-On-Acid (Sinch), Resend native dashboard preview, Mailchimp Inbox Inspector, and CSS preprocessing tools (Premailer / juice / html-email-comb) for cross-client render validation. **Decision: defer adoption** (~10 customers/month doesn't justify $74–99/mo spend; React Email's table-based render already conservative for Outlook desktop; PR #226's send-to-test gives Max's primary inbox coverage). Promotion trigger documented: ≥1 customer render-bug report in a 30-day window, OR send volume crosses ~100 customer emails/month, OR new template added with non-trivial CSS (gradients, flexbox-gap, complex layouts), OR brand redesign / visual refresh. Pricing flagged as approximate (verify before committing budget); structural recommendation doesn't depend on dollar figure. Closes dex `47w420v0`. | ✅ shipped 2026-05-30 to `release/v1.5.0` (#225, squash `860fd58`) |
| **PR #223** | **`ws8r39uu` — `defineLocations` entries for 12 email singletons in `studio/presentation.tsx`.** Renames `studio/presentation.ts` → `.tsx` so JSX is available for the `EnvelopeIcon` import from `@sanity/icons`. Adds an `emailLocation(title)` factory that returns `defineLocations({ locations: [{ title, href: "#", showHref: false, icon: EnvelopeIcon }] })` and uses it once per email singleton (`emailOrderConfirmation`, `emailDay7Delivery`, `emailGiftClaim`/`Reminder`, `emailGiftPurchaseConfirmationSelfSend`/`Scheduled`, `emailMagicLink`/`Library`, `emailNewDeviceNotice`, `emailPrivacyExport`, `emailRecipientIntakeReceived`, `emailSharedShell`, `emailStepUpOtp`). Surfaces emails in Presentation's "Used on" panel — matches Sanity v5.13 non-URL preview pattern (`href: "#"` is a stub required by the non-optional `DocumentLocation.href` field; `showHref: false` hides the link affordance). Strictly additive — no behavior change for any non-email entry. Single importer (`sanity.config.ts`) uses extensionless import, no call-site change. Main project typecheck + lint + build clean; studio typecheck clean for the new file (pre-existing errors in `preview-stubs` + `StudioPagePreview` unchanged — tracked separately under `kf1nixad` workstream). | ✅ shipped 2026-05-30 to `release/v1.5.0` (#223, squash `725d2a3`) |
| **`eb3bd16` (CI bookkeeping, no PR)** | **Drop deleted `v1.2.0/v1.2.1/v1.3.0` release branches from `.github/workflows/ci.yml`.** Those branches were deleted during v1.4.0 post-merge bookkeeping (SESSION_BOOT 2026-05-30 "Branch cleanup"). Keeps `main` + `release/v1.4.0` (still alive for hotfix per the v1.4.0 retro) + `release/v1.5.0`. Push triggers + deploy-staging conditional updated in lockstep. | ✅ shipped 2026-05-30 to `release/v1.5.0` (commit `eb3bd16`) |
| **PR #222** | **`qvco44xz` — plumb `{firstName}` into `emailPrivacyExport` (GDPR Art. 20 data-export email).** Adds `firstName` to the `emailPrivacyExport` slot allowlist (was `[downloadUrl, submissionCount, expiryDays]`). `PrivacyExportVars` requires firstName. `sendPrivacyExportEmail` in `src/lib/resend.tsx` accepts + forwards firstName via `applyTokens` (subject now interpolated to match OrderConfirmation / GiftClaim / Day7Delivery pattern; was only the body that did substitution before). `/api/privacy/export/route.ts` derives firstName via existing `extractFirstName(submissions[0].responses)` with `"there"` fallback when the user has zero submissions. `extractFirstName` promoted from private to exported helper in `src/lib/booking/submissions.ts` (same change also lands in PR #224 with identical lines — no merge conflict). `PREVIEW_FIXTURE.firstName` already existed in `src/lib/emails/preview-fixtures.ts`; render-preview branch updated to thread it through. +2 new resend.test.ts cases (`substitutes {firstName} in Sanity-edited copy`; `renders with 'there' fallback when no firstName known`). Vitest 2092/2092. Lint + typecheck + main build + studio build clean. | ✅ shipped 2026-05-30 to `release/v1.5.0` (#222, squash `2b002f7`) |
| **PR #221** | **`evpoius0` — generalize `studio/lib/tokenHelp.ts:tokenReferenceField` for non-email pages.** Accepts either an `EmailTemplateKey` (existing 12 email schema call sites unchanged) or `{ tokens: readonly string[] }` for arbitrary documents. `studio/components/TokenCatalogBanner.tsx` updated to accept either source and looks up the slot list or uses the provided array. Wires the catalog banner into 3 page schemas surfaced during `/simplify` of PR #177: `listenPage` ({readingName}), `giftIntakePage` ({readingName}), `thankYouPage` ({purchaserFirstName}, {recipientName}). Net diff: 5 files, +15 / −4 LoC. Vitest 2090/2090. Lint + typecheck + main build + studio build clean. | ✅ shipped 2026-05-30 to `release/v1.5.0` (#221, squash `ef27bb3`) |
| **`a2c0678` (CI bookkeeping, no PR)** | **Enable CI + auto-deploy-staging on `release/v1.5.0` in `.github/workflows/ci.yml`.** Mirrors the v1.4.0 pattern: adds `release/v1.5.0` to the `push.branches` list and to the `deploy-staging.if` ref-check condition so each squash-merge into the release branch triggers the full lint + typecheck + vitest + storybook matrix + auto-deploys to staging. Required per binding rule `feedback_check_release_branch_ci_after_every_merge` so post-merge CI gates catch regressions that PR-level e2e-sandbox skips. | ✅ shipped 2026-05-30 to `release/v1.5.0` (commit `a2c0678`) |
| **PR #218** | **Release v1.4.0 — `release/v1.4.0` → `main` (46 commits across Phases 1-5 of epic `23ctexvw` + PR #214 + PR #217 + spec-backfill commit).** **Phase 1 (`rbl5u2st`)**: HMAC-SHA-256 tokenized listen-link primitive + D1 single-use ledger (`0014_listen_token_redemptions`) + POST-confirm interstitial; admin-resend bug fix (was passing raw R2 URL as listen URL); `Referrer-Policy: no-referrer` on `/listen/*`; Sentry `beforeSend` `?t=` redaction. **Phase 2 (`vwfcb5jr`)**: unified `/my-readings` library + `/my-readings/gifts` deep-link + legacy `/my-gifts` 308; HMAC `library-token` primitive + `0015_library_token_redemptions` + POST-confirm interstitial at `/my-readings/welcome`; tabs component; HKDF-SHA256 master `AUTH_TOKEN_SECRET` with `listen.v1` + `library.v1` subkeys (replaces per-token secrets). **Phase 3 (`khb6hor6`)**: step-up OTP on edit-recipient + send-now; `0016_step_up_otp` + `elevated_at` on `listen_session`; 15-min OTP TTL + 10-min elevation TTL; `stepup.v1` HKDF subkey; `StepUpOtpModal` (role=dialog, focus trap, ESC, backdrop close, ARIA). **Phase 4 (`zgd1sjpu`)**: removed destructive cancel-scheduled route + UI; replaced with claim-for-yourself flow. **Phase 5 (`l0xynlxs`)**: one-tap new-device notice (`0018_new_device_notice` adds `ua_hash` to both redemption ledgers + new `listen_device_notifications` dedup table); `NewDeviceNotice.tsx` + `emailNewDeviceNotice` singleton + `new_device_revoke.v1` HKDF subkey + `/api/auth/revoke-recipient-sessions`; magic-link library collapse (dual `emailMagicLinkMyReadings` + `emailMagicLinkMyGifts` → single `emailMagicLinkLibrary`) — Sanity migration with Becky-edit-preserving plaintext-match guards; day-7 schema collapse (legacy fragment fields removed); day-7 copy rewrite + privacy nudge; 33 em-dashes purged from defaults.ts. **PR #208 (collapsed into release)**: `/my-readings/gifts` route removed in favor of single scrollable `/my-readings` with Mine + For-others sections. **PR #214 (carry-over)**: 4 cancelScheduled* myGiftsPage fields unset + `flipToSelfSendCtaLabel` relabeled "Send the link myself instead" → "Cancel the schedule and send it myself". **PR #217**: purchaser tz on scheduled-gift OC email (`0017_purchaser_time_zone`) + first `(authed)` Next.js route group + brand DateTimePicker replacing native `<input type="datetime-local">` in edit-recipient drawer + FlipToScheduledControl + a11y polish on intake DatePicker/TimePicker. **Spec-backfill commit `1dca541`** (no PR; direct on `release/v1.4.0` pre-merge): 4 preconditions had drifted during the release arc but never exercised because `e2e.yml` only fires on PRs to main — first PR-CI exposure was PR #218 itself. Surgical: `signInViaMagicLink({next:"/my-gifts"})` → `"/my-readings"` × 6 sites; `gift-cancel-auto-send` CTA selector relabeled; `gift-flip-to-scheduled-tz` × 2 datetime-local selector → `input[name="giftSendAt"]` + display-format fill; `gift-cross-purchaser-leak` POST `/cancel-scheduled` → `/cancel-auto-send`. Filed dex `t380yxay` (GiftStatusPill SSR/client TZ hydration mismatch) + dex `eti8rzqv` (retrofit auto-probe onto `UA_AUDIT_HASH_DEPLOYED` — re-files lost `gi0ux0tn` from PR #205, surfaced by a silent-no-op audit pass intersecting 105 claimed dex IDs across SESSION_BOOT + CHANGELOG + memory + recent commits with `.dex/tasks.jsonl`; 1 unknown silent no-op found). Binding rule `feedback_dex_create_verify_with_show` extended with a second checkpoint: cross-check every dex ID mentioned in commit / CHANGELOG / SESSION_BOOT against `dex show <id>` before the line is written. **Migrations applied:** D1 0013-0018 to BOTH staging + production. Sanity migrations applied to both datasets (my-gifts cancel-scheduled cleanup, day-7 body rewrite + legacy field unset, `emailMagicLinkMyGifts` orphan delete, `emailMagicLinkMyReadings → emailMagicLinkLibrary` rename) with per-field plaintext-match guards preserving Becky's editorial work. **Post-merge seed + Studio deploy 2026-05-30**: 6 new Sanity singletons seeded (staging: `emailStepUpOtp`, `emailNewDeviceNotice`; production: `magicLinkVerifyPage`, `emailSharedShell`, `emailStepUpOtp`, `emailNewDeviceNotice` — the first two were stale prod misses from earlier releases that the static-fallback pattern had masked, now closed). Studio deployed to https://withjosephine.sanity.studio/. **Branch cleanup**: tagged `v1.0.0`, `v1.3.0`, `v1.4.0` (the first two had shipped without tags); deleted 5 release branches (local + remote) — `release/v1.0.0`, `release/v1.1.0`, `release/v1.2.0`, `release/v1.2.1`, `release/v1.3.0` — all fully contained in v1.4.0 with 0 commits ahead. `release/v1.4.0` deliberately kept until post-merge real-browser smoke completes. **Pending**: real-browser smoke against deployed prod worker (Max-action; requires receiving + tapping email). | ✅ shipped 2026-05-30 to `main` (#218, squash `b1ebbf8`, tag `v1.4.0`) |
| **PR #217** | **Three follow-up fixes on `release/v1.4.0` bundled in one PR** (per Max's "should have been one combined PR" feedback). **Fix #1 — purchaser tz in scheduled-gift OC email (dex `c1u7yn8g`)**: stop hard-coding UTC in `formatSendAt`. New D1 column `submissions.purchaser_time_zone` (migration `0017_purchaser_time_zone.sql`). `formatSendAt(iso, tz?)` accepts optional IANA zone with UTC default for legacy. Both gift POST + flip-to-scheduled validate via the canonical `isIanaTimeZone` (deduped from a one-off `Intl.DateTimeFormat` try/catch). Webhook reads `submission.purchaserTimeZone` from the row, not Stripe metadata — matches the Calendly/Eventbrite/Airbnb pattern (mid-PR architecture pivot from the locked Stripe-metadata round-trip after Max challenged the locked plan; the metadata round-trip was nobody else's pattern). +5 tests including PT/Asia-Jerusalem render assertion, UTC fallback, 400 on missing/invalid tz at gift POST, 422 on flip-to-scheduled. 33 fixture files updated for the new required field on `SubmissionRecord`. **Fix #2 — auth-gated top nav (dex `gj1s46yv`)**: first `(authed)` Next.js route group. Moved `my-readings`, `listen/[id]`, `gift/intake`, `gift/claim/[token]`, `thank-you/[readingId]` underneath. New `src/app/(authed)/layout.tsx` renders a sticky brand top-bar (✦ Josephine wordmark on the left, Home link on the right, `aria-label="Site"`, Cream bg, semantic `border-j-border-subtle` hairline). URLs unchanged so middleware path-based auth gating still works. **Fix #3 — brand DateTimePicker (dex `ahfwbcot`)**: new `src/components/Form/DateTimePicker/` replacing native `<input type="datetime-local">` in `GiftCardActions.tsx` at the edit-recipient drawer + `FlipToScheduledControl`. Final shape after multiple rewrites: ONE text input opens ONE popover containing the calendar on the left and two scroll-snap HH/MM columns on the right (60 minutes, not 5-min increments). ARIA-APG roving-tabindex listbox on the time columns — arrows move the active option + focus, Enter/Space commits, single-tab-stop via the active option. Done button + Escape both run `closeAndReturnFocus` with `justClosedRef` guard against the input's `onFocus` reopening the popover on the same tick. Partial state renders (`dd/MM/yyyy` if only date picked, `HH:mm` if only time) so selections feel responsive. **Bonus a11y polish on DatePicker + TimePicker** (intake birth-date/time, production-critical TikTok-mobile traffic): both inputs gain `role="combobox"` + `aria-controls` + `aria-expanded` for ARIA-spec correctness; DatePicker today indicator now uses a ring + heading-text instead of gold-on-cream (was 2.6:1 → failed WCAG 1.4.11); day buttons 36×36 → 40×40 for touch; DatePicker calendar grid + nav arrows + month/year dropdowns now have SR labels via `labels` prop; backspace from a valid date now clears `onChange` (silent stale-value fix); partial typed draft preserved on blur until commit or reset; future date no longer triggers "under N" age warning; `useEffect`-month-sync replaced with the React 19 `prevValue` render-time pattern. TimePicker gains a polite `aria-live` region announcing the "I don't know my birth time" toggle state. Shared `src/components/Form/DayPickerShared/dayPickerShared.ts` extracts the duplicated `DAY_PICKER_LABELS` + `DAY_PICKER_BASE_CLASSES` (~55 LoC of duplication removed). New Storybook story `Form/DateTimePicker` with Empty/WithInitialValue/WithMinNow/WithError variants. 15 functionality tests on DateTimePicker covering click+Enter+Escape+Done+Tab+partial-state — previous test suite only covered Tab navigation, missed the actual select+close flow that Max caught manually. `/simplify` (3 parallel agents) + `sentry-skills:code-review` (4 HIGH findings) + a second code-review/simplify round on the rebuild all applied. **Migration apply windows:** staging applied post-deploy via `pnpm migrate:apply:staging`; production at `release/v1.4.0 → main` merge time. **NOT touched without explicit approval (production-intake regression risk):** removing `onFocus` auto-open on DatePicker/TimePicker, iOS native `<select>` wheel risk in TimePicker, replacing TimePicker's `<select>` dropdowns with the scroll-snap UX. Net diff across 5 commits: 73 files, +1047 / −182 LoC. Tests 2067/2067 vitest passing. CI green at 16m23s. | ✅ shipped 2026-05-28 to `release/v1.4.0` (#217, squash `9931964`) |
| **PR #214** | **Phase 4 of epic 23ctexvw — remove destructive cancel-scheduled + bundled fix-arc.** Deletes the cancel-scheduled mechanism end-to-end (route + tests + repo helper `applyGiftCancelScheduled` + submissions wrapper + `CancelScheduledControl` button + 4 cancelScheduled* Sanity copy fields + `tests/e2e/specs/gift-cancel-scheduled.spec.ts` + `GIFT_CANCELLED_REASON` constant + `GiftCancelledReason` type) and relabels `flipToSelfSendCtaLabel` from "Send the link myself instead" to "Cancel the schedule and send it myself" so the existing flip-to-self-send button becomes the cancel-the-schedule affordance (it already calls `/api/gifts/[id]/cancel-auto-send` under the hood, which routes through `flipGiftToSelfSend`). Iteration 2 of the Phase 4 plan: first proposal invented pause/resume semantics (new repo helper, edit-recipient resume branch, two new Sanity fields) → rejected as over-engineering. Final scope is pure deletion + one CTA relabel + one Sanity migration. Sanity migration `scripts/migrate-my-gifts-remove-cancel-scheduled-2026-05-28.ts` unsets the 4 removed fields + applies new flip label only when current value still matches old default (preserves Becky edits); applied to staging during the session (verified: 4 fields unset, label flipped). D1 column `gift_cancelled_at` + every `AND gift_cancelled_at IS NULL` WHERE guard left in place — dead-but-harmless going forward, defensive for any legacy ghost rows. `GIFT_STATUS_KIND.cancelled` + `statusCancelledLabel` + `GiftStatusPill` cancelled-kind also left in place because `listGiftsByPurchaserUserId` doesn't filter cancelled rows. **CI fix-arc bundled in the same PR (Max's no-defer rule):** (1) `tests/e2e/specs/library-token-roundtrip.spec.ts` Scenarios C/D/E asserted `response.headers()["set-cookie"]` undefined, but CF Access on staging injects `CF_Authorization=*` into the response — swap to `not.toMatch(/__Host-listen_session=/)` which preserves the security-meaningful invariant. (2) `library-token-roundtrip.spec.ts` Scenario B asserted `getByRole("tablist")` which no longer exists after PR #208 removed the tab strip — swap to `getByRole("heading", { level: 1 })`. (3) Same set-cookie filter applied to `listen-one-tap-roundtrip.spec.ts` Scenario C. (4) `playwright.config.ts` `globalTimeout` raised 10 → 25min and `e2e-sandbox.yml` `timeout-minutes` raised 12 → 30 because the now-passing tests run ~40s each vs ~30s fast-fail, pushing the 29-test suite past the prior 10-min cap. (5) Deleted orphan `tests/e2e/specs/my-readings-tabs-roundtrip.spec.ts` left over from before PR #208. (6) **Resend email leak plugged**: `SANDBOX_EMAIL_PREFIXES` in `src/lib/resend.tsx` was missing prefixes from Phase 1/2/3 specs (`library-one-tap+`, `listen-one-tap+`, `prod-smoke+`) so the cron/webhook/DO path guard failed to recognize those addresses as sandbox → Stripe webhook OC emails leaked via Cloudflare Email Routing into Max's real inbox for ~2 days. Added the missing prefixes + new vitest scanner `src/lib/resend.sandboxPrefixes.test.ts` that walks `tests/e2e/specs/` and asserts every `*@withjosephine.com` prefix is registered (drift now fails pre-push). New durable memories `feedback_no_emails_in_testing_period` (three-layer defense rule) + `feedback_run_tests_locally_before_push` (stop burning sandbox CI cycles iterating on test fixes) + updated `feedback_resend_dry_run_paths` with the prefix-drift failure mode. Dex `5f0bqd2d` filed (p1, layer-3 env-gated allowlist defense). Three-agent simplify pass run mid-PR surfaced 8 findings; 5 fixed in-PR (orphan migration ref + smoke-doc J5e + em-dash + dead patch type), 3 deferred to dex (`m7e8iz7y` drop ConfirmArmedButton variant=destructive arm; `nkiqfpln` extract patchSingleton+setIfMatchesDefault helpers; sandbox runtime optimization `w48m94vw`). Net diff across 7 commits: −710 / +161 LoC. Tests 2043/2043 vitest passing (was 2046 baseline; net −6 deletions + −1 orphan spec + +3 from new scanner). Sandbox e2e timing-out at the 10-min cap was the original symptom that surfaced the bundled fixes. Follow-up Max-actions: (a) run the same migration against production: `set -a && source .env.local && set +a && pnpm tsx scripts/migrate-my-gifts-remove-cancel-scheduled-2026-05-28.ts`; (b) re-deploy Studio (`pnpm run deploy` from `studio/`) so Becky sees the updated `flipToSelfSendCtaLabel` field title + description. Closes dex `zgd1sjpu`. | ✅ shipped 2026-05-28 to `release/v1.4.0` (#214, squash `aadf98d`) |
| **PR #213** | **The actual fix for the intake picker "stacking" bug: unwrap recursively-nested hex from corrupt Sanity color shapes.** Three prior PRs (#210 z-30 wrapper, #211 hand-rolled `createPortal`, #212 Radix Popover migration) all failed because the popover was never losing the z-order battle — its **background was invisible**. Staging Sanity had `blush`, `ivory`, `rose` stored as recursively-nested objects 4 layers deep (`{hex: {hex: {hex: {hex: "#…"}}}}`), most likely from a re-save loop in the Studio colorInput plugin. The build-time generator's `${color.hex}` template literal stringified the top object as `"[object Object]"`, which landed as `--j-blush: [object Object]` in the generated tokens CSS, so `bg-j-ivory` resolved to invalid color → popover background **transparent** → form FloatingLabels + the unknownToggle checkbox visible through the picker, exactly mimicking a z-index inversion. Local dev was unaffected because it serves the committed `tokens.css` defaults or a clean local Sanity dataset; only the production build pipeline runs `pnpm generate-tokens` against the staging dataset where the corruption lives. Why I missed it for three iterations: I never opened the deployed CSS bundle to follow the `var(--j-ivory)` chain to its resolved value. Fix is a single `extractHex` helper that walks `.hex` until it finds a string starting with `#`, applied to the semantic-color loop, the accent / bg-interactive RGB writers, and the email-tokens emitter so the same shape can't corrupt customer transactional emails. Regression test builds a 4-deep nested shape and asserts the output contains the inner hex with `[object Object]` explicitly absent. Verified end-to-end: deployed CSS now has 0 `[object Object]` (was 3); `--j-blush: #e8d5c4` / `--j-ivory: #fafaf8` / `--j-rose: #bf9b8b` resolve correctly; Max confirmed on staging Chrome that the popover is now opaque. **Follow-up filed:** dex `axfyutyx` — clean up the Sanity content itself (re-set blush/ivory/rose in Studio so the document shape is flat again; defensive code makes it safe regardless, but the data is still misshapen). New durable memory `feedback_check_data_when_ui_fix_keeps_failing`: when a UI bug recurs through 2+ fix attempts at the same layer (component, z-index, portal), the cause is somewhere else — open the deployed bundle, follow the var chain to the resolved value, audit the build pipeline. Closes dex `lc9w5xd1` (re-link, the real fix this time). | ✅ shipped 2026-05-28 to `release/v1.4.0` (#213, squash `906894c`) |
| **PR #212** | **Migrate intake pickers from hand-rolled portal to `@radix-ui/react-popover`.** Second attempt at the picker stacking bug. After #211's manual `createPortal` hook still left visible bleed-through on staging, replaced the whole hand-rolled popover layer with Radix Popover (Floating UI under the hood — Portal + positioning + collision detection + a11y + focus + ESC + click-outside as one primitive). DatePicker, TimePicker, PlaceAutocomplete now use `Popover.Root` + `Popover.Anchor` + `Popover.Portal` + `Popover.Content`; deleted `usePopoverPortal.ts` entirely. `PlaceAutocomplete` combobox semantics preserved: input keeps `role="combobox"` + `aria-controls={listboxId}` + `aria-expanded`; the `<ul role="listbox">` and empty-hint `<p role="status">` live inside `Popover.Content`. `onOpenAutoFocus` prevented (focus stays on input for typing); `onInteractOutside` intercepted when target is the input (no close/reopen flicker). +Tailwind `mergeClasses` adopted in PlaceAutocomplete per simplify Agent 1. **Did not fix the actual bug** — popover background was still invisible because of the upstream Sanity-color data corruption that PR #213 fixed. The Radix migration itself is still a genuine quality improvement (proper a11y, ESC, focus management, no hand-rolled stacking logic), but it was the wrong layer for THIS bug. Closes dex `ieo8rdqa` (Radix migration follow-up that ended up shipping immediately). | ✅ shipped 2026-05-28 to `release/v1.4.0` (#212, squash `28554a6`) |
| **PR #211** | **Replace `z-30` wrapper with hand-rolled `createPortal`-to-`document.body` for picker popovers.** First follow-up after #210's wrapper-z-index trick didn't hold on staging desktop Chrome. New `usePopoverPortal` hook centralized portal target + computed position (anchor `getBoundingClientRect()` + window resize/scroll listeners) + click-outside (anchor + popover refs). Three picker components migrated. **Did not fix the actual bug** — escaping ancestor stacking contexts didn't help because the popover wasn't losing on z-order; it was losing on background (PR #213 root cause). Self-reflection: a previous browser agent's `parentIsBody: true` + "no bleed-through" narration was contradicted by its own screenshot — exactly the trap the saved memory `feedback_settle_counts_with_grep_not_agents` warned about, and the trigger for `feedback_check_data_when_ui_fix_keeps_failing`. | ✅ shipped 2026-05-28 to `release/v1.4.0` (#211, squash `b65fc6b`) |
| **PR #210** | **Lift open picker wrapper into its own stacking context (`relative z-30` while open).** First attempt at dex `lc9w5xd1` ("LAUNCH BLOCKER — intake birth-time checkbox renders over the time/date pickers"). The hypothesis: an ancestor stacking context was trapping the popover's `z-20`, so giving the open picker wrapper its own positive z-index would force the whole `[input + label + popover]` layer above sibling field content. Added regression Storybook story `Form/PickerStacking`. **Did not fix the actual bug** — z-index was a red herring; the popover background was invisible due to corrupt Sanity tokens (PR #213). The wrapper-z-index pattern is now reverted in #212's Radix migration. | ✅ shipped 2026-05-28 to `release/v1.4.0` (#210, squash `af49840`) |
| **PR #209** | **Fix listen-audio first-load request storm with `preload="none"`.** Loading `/listen/[id]` opened ~20 parallel `GET /api/listen/[id]/audio` connections that all canceled at once, threw 42× "Worker exceeded memory limit", and tripped `LISTEN_ASSET_LIMITER` (60 req/60s/IP) so the recipient saw a **429** before audio recovered (dex `pknswojr`, staging smoke 2026-05-27; worst on the 375px mobile/TikTok audience). Root cause: the voice-note `<audio>` used `preload="metadata"`, which probes the media **on page load before any user action**. `ListenView`/`page.tsx` are server components (no `"use client"`/effect/StrictMode), so the burst was browser media behavior, not a React remount. The no-Range probe → full-file `200` stream; ~20 concurrent 4.27MB streams near the 128MB workerd cap → stall→retry cascade → limiter trips. Fix: `preload="metadata"` → `preload="none"` (one attribute) — the browser fetches nothing until the user taps play, then issues sequential `Range` requests from a single action. The proxy, Range forwarding, and the (correctly-sized) 60/60s limiter are untouched: the burst was the defect, not the limit. `preload="none"` is also the mobile-correct default (no surprise 4MB download). Verified in real Chromium against a faithful no-Accept-Ranges `/audio`: `metadata` fires 1 request on load, `none` fires 0 on load, `play()` fires 1 request with `Range: bytes=0-`. New `ListenView.test.tsx` asserts `preload="none"`. typecheck/lint/build green; full suite 2044 pass + 4 confirmed timeout flakes (passed in isolation). Sub-PR under epic `23ctexvw`. Closes dex `pknswojr`. | ✅ shipped 2026-05-27 to `release/v1.4.0` (#209, squash `8357c32`) |
| **PR #208** | **Replace `/my-readings` tab strip with a single scrollable stacked-section page.** The Phase 2 unified library shipped with a Readings/Gifts tab strip whose active tab was derived from a default, leaving gift-only accounts unable to reach the Readings tab (clicking it was a no-op when already on the path — dex `7v1vx0wm`). Redesigned (Max sketch 2026-05-27, dex `0bjxc38w`) into one scrollable page with two always-rendered stacked `<section>`s: **Mine** (own readings) above **For others** (gifts). Both `<h2>` section headings render unconditionally; an empty section shows its existing empty-state copy + CTA, fixing the unreachable-tab defect at the root. `LibraryView.tsx` rewritten as a pure presentational component (no `next/navigation`, no `defaultTab`/`LibraryTabId`); per-card titles + empty-state headings demoted `<h2>`→`<h3>` so the outline stays H1→H2(section)→H3(item). **Deleted:** `src/components/Tabs/` (Tabs + TabPanel, LibraryView was sole consumer), the `/my-readings/gifts` deep-link route, and `pickDefaultTab`. **Repointed:** `/my-gifts` 308 redirect (`next.config.ts`) + the resend email library-button fallback (`resend.tsx`) → `/my-readings`. **Sanity:** `readingsTabLabel`/`giftsTabLabel` label VALUES renamed to "Mine"/"For others" + Studio field titles updated; field KEYS unchanged so no migration (legacy values still resolve). Tests: rewrote `page.test.tsx` (dropped defaultTab assertions), added `LibraryView.test.tsx` (gift-only / readings-only / fully-empty all render both headings + empty-state CTAs). Verified: typecheck/lint clean, 2046/2046 vitest, build green, plus a real-Chromium smoke at 375px across all 4 scenarios (zero console/page errors). Inline Engineer code review applied the heading-hierarchy fix + both-empty test; rejected a "dead next/navigation mock" finding (the mock is required — GiftsCards renders GiftCardActions which calls useRouter). Filed follow-up dex `5fqrgvia` (stale `MyReadingsView` Studio-preview twin) + `laj8x38r` (reconcile defaults.ts with Becky's production Sanity edits). Sub-PR under epic `23ctexvw`. Closes dex `0bjxc38w` + `7v1vx0wm`. Max-action after deploy: Studio re-deploy so Becky sees the renamed section-heading fields. | ✅ shipped 2026-05-27 to `release/v1.4.0` (#208, squash `0132fa7`) |
| **PR #207** | **Phase 3 of epic 23ctexvw — step-up OTP auth for edit-recipient + send-now.** Defense-in-depth: a fresh 6-digit OTP delivered by email establishes a 10-minute elevation window on the listen session before the two high-risk gift mutations pass. **Migration 0016** adds nullable `elevated_at` on `listen_session` + new `step_up_otp` ledger (id, user_id, code_hash, expires_at, consumed_at, mismatch_count, created_at, ip_hash, user_agent_hash). Codes live as `HMAC-SHA-256(stepup.v1 subkey, code)` only; raw never persists. **HKDF subkey** `stepup.v1` added to `TokenSubkeyPurpose` union. **OTP primitive** at `src/lib/auth/stepUpOtp.ts` (421-LoC test coverage): 15-min code TTL, 10-min elevation TTL, 6-digit numeric via `crypto.getRandomValues` rejection sampling, timing-safe equality, 5-attempt poison mirroring magic-link `MISMATCH_LIMIT`, 30s gap + 3-per-30-min throttle cap. Atomic dbBatch in `verifyStepUpOtp` consumes the OTP row AND sets `elevated_at = now` on `listen_session` in one transaction. **Six new audit events** (`step_up_otp_issued`, `step_up_otp_verified`, `step_up_otp_mismatch`, `step_up_otp_poisoned`, `step_up_otp_expired`, `step_up_elevated`). **Endpoints:** POST `/api/auth/step-up/request` (200/401/404/429) + POST `/api/auth/step-up/verify` (200/400/401/422). 422 carries `contactMailto` (mailto:hello@withjosephine.com?subject=Step-up%20help) so failed OTP routes to support, not dead-end Unauthorized. **Gate:** `requireElevation(session)` helper wired into `edit-recipient/route.ts` + `send-now/route.ts` after the existing `authorizeGiftPurchaser` preamble. Per Council 2, `cancel-auto-send`, `resend-link`, and `flip-to-scheduled` deliberately untouched. **Email** (brand-lite, no GoldHero): Sanity `emailStepUpOtp` singleton (subject, heroLine, intro, codeLabel, expiryLine, closingLine, signoff) + `StepUpOtp.tsx` renders the 6-digit code in 28px monospace on cream `#FAF8F4` with 0.25em letter-spacing. `sendStepUpOtpEmail` in `src/lib/resend.tsx` honors sandbox-prefix guard, dry-run header (fail-closes when secret unset), and `RESEND_DRY_RUN` flag. **UI:** `StepUpOtpModal` is a portal-rendered `role="dialog"` with `aria-modal="true"`, focus trap (Tab + Shift+Tab cycle), ESC close, backdrop click close, `aria-labelledby` + `aria-describedby`, and the input carries `inputmode="numeric"` + `pattern="[0-9]*"` + `autocomplete="one-time-code"` + `maxLength={6}`. `useMutationAction` extended additively with `elevationRequired` state + `retry()`. `jsonPost` surfaces `errorBody` on non-2xx so the hook can read the 401 discriminator. `GiftCardActions` + `ConfirmArmedButton` open the modal on 401 elevation_required; the original mutation retries on successful verify. **E2E:** new helper `tests/e2e/helpers/stepUpOtp.ts` reads `devCode` from the `/api/auth/step-up/request` response in mock mode (RESEND_DRY_RUN=1 path returns `kind:"skipped"` so the route surfaces devCode). **Process notes:** 3 worktree-isolated Engineer agents dispatched in parallel (foundation+API, email+Sanity, UI). 2 worktrees were inadvertently based off main; resolved via commit + rebase + cherry-pick. **Mid-PR CI surprise:** sandbox e2e revealed Phase 2's merge commit `da006dd` never triggered ci.yml so staging worker was still on Phase 1 code (raw `LISTEN_TOKEN_SECRET`) while spec at HEAD signed with HKDF subkey. Three fixes landed in this PR: (1) workflow `e2e-sandbox.yml` maps `STAGING_AUTH_TOKEN_SECRET → AUTH_TOKEN_SECRET`; (2) `AUTH_TOKEN_SECRET` rotated atomically on staging worker + GH env; (3) the PR merge itself fired ci.yml which finally deployed Phase 2 + Phase 3 together. Migrations 0014 + 0015 + 0016 applied to staging D1 via `pnpm migrate:apply:staging` (0014 + 0015 had also never been applied). **Tests:** 76/76 ISC complete, 2054/2054 vitest passing (+99 vs 1955 baseline). Filed during merge sweep: dex `dsk9z62m` — backfill `/my-gifts` URL redirect into 4 legacy mock-mode specs. **Pentester / Designer / simplify / code-review** removed from capabilities with documented rationale (Phase 1 listen-token primitive already swept; release-arc deferral per `feedback_simplify_scale_to_change_size`). PRD complete at `MEMORY/WORK/20260526-115511_phase3-stepup-otp-auth/PRD.md`. Closes dex `khb6hor6`. | ✅ shipped 2026-05-26 to `release/v1.4.0` (#207, squash `9238f82`) |
| **PR #206** | **Phase 2 of epic 23ctexvw — unified `/my-readings` library + tokenized purchaser email buttons + HKDF auth-secret consolidation.** Merges `/my-readings` + `/my-gifts` into a single tabbed library at the existing `/my-readings` URL (gifts becomes a tab; `/my-readings/gifts` is the deep-link). Legacy `/my-gifts` 308-redirects via `next.config.ts`. **Library token primitive:** new HMAC-SHA-256 module at `src/lib/auth/libraryToken.ts` (canonical payload `library.v1:<userId>:<jti>:<mintSource>:<expMs>`, 1-year TTL, 5 mint-source values) + single-use D1 ledger (migration `0015_library_token_redemptions`). **POST-confirm interstitial** at `/my-readings/welcome` redeems via `POST /api/library/redeem`, opens a normal listen session via the shared `createListenSessionForUser` helper (`__Host-listen_session` cookie shared by design with Phase 1 listen redemption — both establish a user-scoped session). All failure modes 303 to `/my-readings` byte-identically. **Tabs component:** hand-rolled semantic ARIA at `src/components/Tabs/Tabs.tsx` + `TabPanel.tsx` (`role="tablist"`, `aria-selected`, `aria-controls`, ArrowLeft/Right + Home/End keyboard nav, count badges including 0). First tab pattern in the codebase. **Unified RSC:** `src/app/my-readings/page.tsx` + `gifts/page.tsx` share `_shared/loadLibraryData.ts` which parallel-fetches submissions + gifts + both Sanity copies via `Promise.all`. Default-tab logic: readings if any exist, else gifts. Cancel-scheduled UI stays in this PR (Phase 4 `zgd1sjpu` removes it). **Email integration:** new `LibraryButton.tsx` with `variant?: "primary" \| "secondary"` (default secondary). OrderConfirmation + both `GiftPurchaseConfirmation` variants pass `variant="primary"` (sole CTA in those contexts); `Day7Delivery` keeps secondary. Color references `emailTokens.ink` instead of hardcoded hex. Three recipient-bearer templates (`GiftClaimEmail`, `GiftClaimReminderEmail`, `RecipientIntakeReceived`) carry absence-asserting snapshot tests. **Send-path wiring:** single `tryBuildLibraryUrl({userId, mintSource, siteContext})` helper wraps mint with fail-safe try/catch + console.error, returning `undefined` so emails ship without the button rather than failing entirely. Five call sites use this wrapper (`notifyPaid` OC, day-7 cron, stripe webhook both gift branches, `resendCustomerEmail` for OC + Day-7 admin resend). Stripe webhook gift branches now resolve purchaser via `getOrCreateUser` and persist `purchaserUserId` via new `setSubmissionPurchaserUser` repository helper. **Auth-secret consolidation (D-19, drives Phase 2 → 6 arc):** single master `AUTH_TOKEN_SECRET` replaces the per-token secrets via HKDF-SHA256 in `src/lib/auth/tokenSubkey.ts`. Salt `withjosephine.com.auth-token.v1`, info `josephine.auth-token-subkey.<purpose>`. `listenToken.ts` derives via `"listen.v1"`; `libraryToken.ts` derives via `"library.v1"`. Future phases declare a new subkey label, never a new env var. Drops both `LISTEN_TOKEN_SECRET` (already a misnomer; `requestAudit` + `giftClaimSession` were using it; both now read `AUTH_TOKEN_SECRET` directly — HKDF migration for those two filed under follow-up `e737za2a` P3-2) and `LIBRARY_TOKEN_SECRET` (would have been introduced in this branch otherwise). **Sanity schema (additive, Becky-safe):** `myReadingsPage` singleton gains 5 optional fields (`readingsTabLabel`, `giftsTabLabel`, `welcomeHeading`, `welcomeSubhead`, `welcomeButtonLabel`) with hardcoded fallbacks. Studio re-deploy required. **Sentry redaction:** browser-side `scrubSensitiveData` in `sentry-client.ts` now strips `?t=` query tokens via shared `redactSearchParams`. `SENSITIVE_QUERY_PARAMS = ["t"]` lifted to `src/lib/logging/redactSearchParams.ts` so worker + browser stay in sync. **Middleware:** widens `isMyGifts` → `isMyReadings`; `/my-readings/welcome` joins `/listen/*` in `Referrer-Policy: no-referrer`. **Process notes:** three in-session reviews surfaced 1 P1 security blocker (browser Sentry redaction) + 4 important fixes (LibraryButton token bypass, LibraryButton variant hierarchy, LibraryView redundant useState, welcome page double-fallback). All landed before merge in commit `a28ed45`. `LibraryView` state derivation simplified from `useState`+adjust-during-render to direct derive from `usePathname()`. Welcome page caps `?t=` length at 1024 chars. New e2e specs `library-token-roundtrip.spec.ts` + `my-readings-tabs-roundtrip.spec.ts` use Phase 1's auto-probe pattern. **Tests:** 1964 passing (+68 from Phase 1 baseline). 3 commits: feature (`3a25137`) + HKDF refactor & comment cleanup (`f3cc41f`) + must-fix review pass (`a28ed45`). Operational follow-ups: provision `AUTH_TOKEN_SECRET` on staging worker + GH env before next deploy; apply migration 0015 to staging post-deploy; re-deploy Sanity Studio. Closes dex `vwfcb5jr`. Deferred review items filed under dex `e737za2a` (under parent epic 23ctexvw). | ✅ shipped 2026-05-26 to `release/v1.4.0` (#206, squash `da006dd`) |
| **PR #205** | **Phase 1 of epic 23ctexvw — one-tap day-7 delivery: tokenized listen-link primitive collapses the 2-email delivery + magic-link chain.** Architectural arc Max-approved 2026-05-26 after a 4-persona UX council + library-auth council + 30-surface copy audit. **Mint:** new HMAC-SHA-256 primitive at `src/lib/auth/listenToken.ts` (canonical payload `listen.v1:<submissionId>:<sha256(recipientUserId)>:<jti>:<mintSource>:<expMs>`; `crypto.subtle.verify` constant-time; base64url canonicalization re-check rejects encoding-ambiguity tokens; payload binds `recipientUserId` hash so admin re-ownership of a gift mid-window forces re-mint). **Wire:** cron `email-day-7-deliver` (`route.ts:51` + force path) mints with `mintSource="cron_day7"` + 30-day TTL; admin manual-resend (`resendCustomerEmail.ts:114`) mints with `mintSource="admin_resend"` + TTL capped to `min(30d, msUntilReadingExpires)` so a token can't outlive the reading-retention window. Admin path bug fixed in scope: previously passed the raw R2 URL as the "listen URL" — now correctly produces `/listen/<id>?t=<token>`. **Redeem:** GET `/listen/[id]?t=<token>` verifies the token then renders a `ListenTokenInterstitial` welcome page with a single POST form ("Continue to your reading" button). POST `/api/listen/[id]/redeem` re-verifies, claims the single-use jti via atomic `INSERT OR IGNORE` on new D1 table `listen_token_redemptions` (migration 0014), creates the 7-day `__Host-listen_session` cookie via the freshly-extracted `createListenSessionForUser` helper (refactored out of `redeemMagicLink` to share one writer between magic-link and one-tap paths), and 303-redirects to `/listen/[id]?welcome=1`. The two-tap-perceived UX (interstitial → POST) defeats Outlook Safe Links / Mimecast / iMessage preview prefetchers that would otherwise consume the redemption on a GET. Failure modes (missing token, expired, tampered, recipient_changed, mismatched submissionId, already redeemed) all 303 to `/listen/[id]` without setting a cookie — falls through to the existing magic-link form unchanged, preserving the legacy-email migration path. **Defense in depth:** `Referrer-Policy: no-referrer` on `/listen/*` via middleware; `Cache-Control: private, no-store, no-cache, must-revalidate` + `Vary: Cookie` on the redeem 303; `Set-Cookie` shape extracted to `buildListenSessionCookieHeader` so the verify route + redeem route can't drift; `redactSearchParams(["t"])` wired into the Sentry `beforeSend` scrubber in `custom-worker.ts` so a one-tap token can never reach the issue tracker. New audit events `listen_token_redeemed` (happy path) + `listen_token_id_mismatch` (forensic). **Red-team-driven design**: Pentester surfaced 24 attack vectors / 5 P1 blockers (email-prefetch consumes redemption, 30-day bearer TTL, CF Logpush URL capture, admin re-ownership race, customer phishing trainability); Engineer surfaced 3 blockers + 11 concerns. All P1 blockers either mitigated this PR (POST-confirm, single-use jti, recipientUserId binding, Sentry redaction) or explicitly deferred to Phase 6 with documented rationale (HKDF subkey, kid rotation, AEAD payload, customer-education page). Phase 6 deferrals: drop GET-side verify on `/listen/[id]?t=` (response-shape oracle; would reshape 4 page tests at this stage), `formString` triplication, audit-row copy-paste in `listenSession`. **Tests:** 17 token-primitive cases (round-trip, malformed, expired, tampered, recipient_changed, custom TTL, mint-source roundtrip), 10 ledger cases (single-use, concurrent INSERT-OR-IGNORE, null ip_hash), 10 redact-helper cases, 6 cron cases (URL shape, mint source, force-path parity, pre-flight 500), 6 admin-resend cases (URL shape, TTL cap, reading-expired guard), 13 page + redeem-route cases (interstitial render, valid POST + Set-Cookie, single-use enforcement, all invalid-token branches), 8 e2e roundtrip scenarios gated on `PHASE1_ONE_TAP_DEPLOYED`. Total +60 tests (1836 → 1896). Real-browser smoke against staging deferred until after deploy. **Branch ceremony:** `release/v1.4.0` cut from `release/v1.3.0@2ee6f20`; `ci.yml` push branches + deploy-staging triggers extended; `e2e-sandbox.yml` gained `STAGING_LISTEN_TOKEN_SECRET` wiring for the gated spec. Auto-probe pattern (`29bd0af`) replaces env-var-gated `PHASE1_ONE_TAP_DEPLOYED` flag with a `beforeAll` POST against the redeem route; spec self-activates on first deploy with no manual flip. Post-merge lint hotfix `89768f4`. Filed dex `gi0ux0tn` (retrofit auto-probe onto `UA_AUDIT_HASH_DEPLOYED`) + dex `qz94q5g2` (replace in-spec `mintListenToken` with auth-gated `/api/internal/test-mint-token` endpoint so the spec stops needing secret coordination with staging). Closes dex `rbl5u2st`; epic `23ctexvw` remains open for Phase 2-6. | ✅ shipped 2026-05-26 to `release/v1.4.0` (#205, squash `c792ed5` + hotfix `89768f4`) |
| **PR #204** | **`release/v1.3.0` code-review findings: 1 HIGH + 4 MED + 4 LOW + coverage gaps.** Single sub-PR addressing all sentry-skills:code-review surfaced concerns from the cumulative v1.3.0 diff pass. H1: `buildContentDisposition` now throws on unsafe filename chars (`"`, `\r`, `\n`, `\`) at the helper boundary, closing a header-injection vector even though current callers all sanitize. M1: `downloadFilename.test.ts` gains path-traversal slug, URL-encoded slash, non-ASCII, empty-string, 500-char, and real-submission-id-format cases. M2: `globals.css` cursor rule extends `:not([disabled])` to also exclude `[readonly]` (DOB + birthtime fields no longer show false pointer affordance). M3: `GiftForm` prefill refactored from `useEffect + useRef` to `useSyncExternalStore` with a module-cached snapshot keyed by `lastReadingId|email|firstName`; removes the `react-hooks/set-state-in-effect` eslint-disable that was masking React 19 timing risk. Test-only reset hatch (`__resetPurchaserPrefillCacheForTest`) exposed because the cache is module-scoped. M4: cross-flow draft semantics audited; tests cover empty-string prior email + user-types-before-effect timing. L1: "Overdue by 1 days" plural grammar fixed via `pluralizeDays(n)` helper. L2: future-dated `giftClaimedAt` clamp now logs a `console.warn` gated to `NODE_ENV === "development"` (skipped in test + production to keep output clean). L3: `webm` removed from `voice-note` extension allowlist (was unreachable; Sanity pipeline emits mp3/m4a). Smoke verification via Chromium against a CSS-mirror HTML fixture (worktree had no `pnpm dev` env wiring): all 6 cursor surfaces verified, including readonly DOB → default, disabled submit → not-allowed. Net +325 / −31 across 7 files. Gates green: lint, typecheck, 1836/1836 tests. CI green on PR-level sandbox + post-merge release-branch full pipeline. | ✅ shipped 2026-05-25 to `release/v1.3.0` (#204, squash `2ee6f20`) |
| **PR #203** | **U7 + U8 Studio bundle: gift-claim status label + 7-day delivery countdown in submission preview.** U7 audit found booking + thank-you already wired via Sanity Presentation Tool (`studio/presentation.ts` lines 25-32) + PR #198's `StudioPagePreview` pipeline for auth-gated state-machine routes; no additional code needed. U8 decision LOCKED: compute claimed-vs-paid from `giftClaimedAt` presence — no new enum value, no schema migration. Two pure helpers in `studio/schemas/submissionPreview.ts`: `giftDeliveryCountdown(claimedAtValue, now)` returns `"N days left to deliver"` / `"Due today"` / `"Overdue by N days"` with future-clock-skew clamp; `statusLabel({status, isGift, giftClaimedAt, deliveredAt, now})` returns `"Claimed · ${countdown}"` / `"Paid · awaiting claim"` / `null`. Threaded through `buildPreview` into the deskStructure list subtitle at the leading position so Becky reads the claim status before the existing email + date tokens. `preview.select` gains `giftClaimedAt` + `isGift` (`studio/schemas/submission.ts:402`). 25 new tests in `submissionPreview.test.ts` covering 7 countdown branches + 7 status branches + 5 integration cases; total suite 1816/1816 passing. Closes dex `em3o7rz6`, `m6pqox9o`, `7igo3atm`, `862rgr2z`, `kiu54tp7`. | ✅ shipped 2026-05-25 to `release/v1.3.0` (#203, squash `75daeac`) |
| **PR #202** | **U4 hover affordance audit: systemic cursor rule via base CSS layer.** Single `@layer base` rule in `src/styles/globals.css` covers buttons, anchors, labels wrapping interactive inputs, `role="button"` / `role="option"` elements, and non-disabled inputs/selects. Disabled states map to `cursor: not-allowed`. 24 redundant `cursor-pointer` / `disabled:cursor-not-allowed` Tailwind utility classes removed across 16 component files (Button, Checkbox, ConsentBanner, DiscardDraftButton, FaqSection, FileUpload, GiftForm, Navigation, PageNav, PageValidationSummary, PlaceAutocomplete, QuestionCard, ReadingCard, ReviewSummary, SwapToast, TimePicker). The `label:has(> input:not([disabled]))` selector cleanly covers implicit-association labels (Checkbox, QuestionCard, GiftForm radio groups) without needing `for=` on every label. Playwright Chromium real-browser smoke against `pnpm dev` verified pointer on every interactive surface + not-allowed on disabled submit. Out-of-scope finding filed as `ym2efbwn`: Hero.tsx motion.div with onClick lacks `role="button"` + keyboard handler — a11y semantic gap the cursor rule can't bridge. Closes dex `dtq86ycp`. | ✅ shipped 2026-05-25 to `release/v1.3.0` (#202, squash `53bfa40`) |
| **PR #201** | **U5 gift-purchase form: prefill purchaser email + first name from prior intake draft.** Lifts the localStorage swap-preservation mechanism from self-purchase IntakeForm (`useDraftRestore` + `getLastReadingId` + `restore` + `pickPreservedFields`) into `src/components/GiftForm/GiftForm.tsx` via a one-shot `useEffect` with ref guard. Reads `priorDraft.values.email` + `priorDraft.values.first_name`, seeds only when current state is empty (user-typed values always win). Recipient email + name stay empty by design (different person). 4 new vitest cases: prefill when prior draft exists; recipient stays empty; no-prior-draft branch (no clobber on empty state); user-typed precedence over prefill. Investigation surfaced that "repeat customer" in the dex task description was misleading — the actual mechanism is local-device-only localStorage swap-preservation, not session/cookie-based cross-device persistence. Closes dex `k9x59wwp`. **Note**: the `useEffect`-based implementation was later refactored to `useSyncExternalStore` in PR #204 to remove the `react-hooks/set-state-in-effect` eslint-disable. | ✅ shipped 2026-05-25 to `release/v1.3.0` (#201, squash `4b7c4b6`) |
| **PR #200** | **U3 listen-page download filenames via `Content-Disposition`.** Voice-note + PDF downloads previously arrived at the user's filesystem with Sanity asset hashes (e.g. `voice-note-abc123.mp3`). New `src/app/api/listen/[id]/downloadFilename.ts` helper sanitizes the reading slug + submission ID, derives the file extension from the source URL with a strict allowlist per asset kind (`mp3/m4a/wav/ogg/aac/opus/webm` for voice-note, `pdf` for reading), and builds a readable filename: `soul-blueprint-voice-note-67b20b5e.mp3`, `soul-blueprint-reading-67b20b5e.pdf`. Audio routes keep `inline` disposition (so the player still plays inline) but add the `filename` param — right-click "Save As" + native player "Download" both honor the param. PDF routes use `attachment` disposition. `AssetContext` in `proxySanityAsset.ts` widened with `readingSlug` + `submissionId`; `findSubmissionListenContext` repository function + `submissions.ts` return type widened to expose `reading_slug` from D1. 9 unit-test cases covering safe slug, fallback when slug is unsafe, query strings in source URLs, and exact wire-format Content-Disposition header. Closes dex `wwqgjtjo`. | ✅ shipped 2026-05-25 to `release/v1.3.0` (#200, squash `00bae05`) |
| **PR #199** | **`release/v1.2.1` → `main` cumulative merge — v1.2.1 ships.** 35 commits, 129 files, +8908/-2330. Themes: (1) **Email template Sanity migration + brand shell consolidation** — bodies moved to Portable Text fields; `emailSharedShell` singleton extracted (PR #193); `BrandHeader` / `GoldHero` / `EmailFooter` JSX components extracted from 9 templates (PR #196, -437 LoC); 90-day reading retention enforced (PR #184); magic-link templates split into 3 context-variant schemas (PR #183); gift purchase email split into Scheduled + SelfSend variants (PR #188); titles + Studio editor description affordance landed (PR #194); visible-URL fallback below magic-link button for hostile clients (PR #195). (2) **workerd-compatible PortableText renderer** (PR #189) — hand-rolled hook-free renderer replacing `@portabletext/react` which crashed with `useMemo` null-dispatcher under workerd's `react-dom/server.edge`; the throw was silently swallowed by `notifyPaid.ts`'s `.catch()` on every non-gift template (OC / Day7 / magic-link / privacy-export) so it shipped to staging undetected. Canonical vendor types adopted; `EmailRichText` fuzzy union collapsed to `PortableTextBlock[]`. (3) **Sanity-mirror dataset resolution hotfix** (PR #190) — `getSanityWriteClient()` async, reads `NEXT_PUBLIC_SANITY_DATASET` from `getCloudflareContext().env` at call time; closes silent staging→production misrouting where Next DefinePlugin baked the wrong dataset literal from `.env.local`. (4) **Studio-bundled gated-page preview** (PRs #197 + #198) — 6 surfaces / 22 fixture states render directly inside Studio via Tailwind compile pipeline (55KB CSS); `next/navigation` + `next/image` stubs at `studio/preview-stubs/` keep server-only imports out of the studio bundle; iframe re-keys per render so Sanity edits propagate live. (5) **Data-correctness scripts** — PR #191 orphan-backfill + recipient-user-id repair (D1 migration 0013); commit `bce8b5c` DI refactor on the repair script + 4 atomic write-path tests + fail-closed `assertNotInTestMode()` env-guard inside the real `execD1` + `mirrorToSanity` (closes the wrangler cached-OAuth foot-gun discovered mid-session — a prior test attempt wrote 35 audit rows + 1 user row to staging before stopping; new memories `feedback_wrangler_oauth_test_footgun` + `feedback_vi_mock_node_child_process_unreliable_in_mts`). (6) **Schema-vs-data validation harness** — commits `345e6fa` + `fcb9067`; `pnpm sanity:validate <env>` walks every Studio singleton and asserts runtime field types match the schema contract; wired into CI as a post-deploy job after `deploy-staging`. Three severity tiers: DRIFT fails CI, MISSING (singleton absent) + WARNING (zombie `consent` field type) are advisory. Staging run: drift=0, missing=2 (notFoundPage + underConstructionPage not seeded yet — follow-up `td3f86z6`), warnings=0. (7) **Simplifier pass** — `58d3877` (canonical PortableText types, -18 LoC) + `ba6d0da` (single-use helpers inlined + shared studio workspace config extracted, -19 LoC). Pre-merge code-review surfaced 3 HIGH on `scripts/repair-recipient-user-id.mts` (no write-path test coverage); closed by the DI refactor in (5). 12 MED/LOW follow-ups filed: `td3f86z6`, `w7iz3o2t`, `thdesb5b`, `5imv4l0e`, `35txg0an`, `vf1seylg`, `d5y8qzl5`, `3syvah89`, `dn17560j`, `dsyeraob`, `qhd4j1xk`, `7e3rd74y`. CI on the merge PR: all 14 checks green including first-exposure Playwright (chromium), GROQ content contract, and the new `sanity-validate-staging` post-deploy job. Tag `v1.2.1` at `ec9c181` (release branch HEAD, matching v1.2.0 precedent). Closes dex `f7yhvvc3`, `x2lhyv0j`, `cpwaqf4d`, `o1hejeb1` + epic milestone for `d8pvexdm` (Studio editorial polish), `kf1nixad` epic remains open pending follow-up `h1zu9nkr` image-rendering polish. | ✅ merged 2026-05-25 to `main` (#199, merge commit `5aa49bc`); tag `v1.2.1` pushed |
| **PR #198** | **Studio-bundled gated-page preview across 6 surfaces / 22 fixture states.** Replaces the `/preview/*` worker routes from PR #185 (CF Access blocked the iframe). New `studio/views/StudioPagePreview.tsx` Studio view renders gated user-specific pages directly inside the Studio bundle via a Tailwind compile pipeline (`scripts/build-studio-preview-css.mts` emits 55KB CSS to `studio/.generated/preview-styles.ts` at studio:dev/build/deploy). `next/navigation` + `next/image` stubs at `studio/preview-stubs/` keep server-only Next imports out of the studio bundle; the next/image stub swaps relative-path imgs for transparent SVG data URLs because `images.withjosephine.com` sends `Cross-Origin-Resource-Policy: same-origin`, blocking sandbox-iframe (null origin) loads — follow-up `h1zu9nkr` tracks the rendering polish. Iframe `key={`${surface}-${stateKey}-${renderTick}`}` remounts on every render so Sanity edits don't go stale. 6 surfaces: Listen, MyReadings, MyGifts, Gift Claim, Magic Link Verify, Gift Intake — each with multiple states (logged-out, verifying, sandbox-blocked, valid-not-claimed, claimed-by-other, ready, listening, expired, etc). Net +1122 / -581 LoC; old `src/app/preview/*` worker routes removed. Pre-push gates all green (lint 0 errors, typecheck clean, 1771/1771 tests, both builds ✓). Closes dex `x2lhyv0j` + epic `kf1nixad` PR-2-of-2 (PR-1 was #197). | ✅ shipped 2026-05-25 to `release/v1.2.1` (#198, squash `d9281a8`) |
| **PR #197** | **Behavior-preserving extraction of R2 origin + submission types for Studio bundleability.** Extracts `R2_PUBLIC_ORIGIN` resolution to `src/lib/r2/publicOrigin.ts` and `SubmissionRecord` + companion types (`SubmissionStatus`, `EmailFiredType`, `EmailFiredEntry`, `GiftDeliveryMethod`) from `src/lib/booking/submissions.ts` to a pure-type module `src/lib/page-previews/types.ts`. `submissions.ts` re-exports the types for back-compat; only the 3 gated-page views + `GiftCardActions` updated to import directly from the new module. PR-1-of-2 of the `kf1nixad` preview replan — sets up PR #198 to bundle the page previews into Studio without pulling the full booking-server import graph. Zero behavior change. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#197, squash `3b34657`) |
| **PR #196** | **Extract `BrandHeader` + `GoldHero` + `EmailFooter` JSX components from 9 customer-facing email templates.** Net diff +150 / -587 = -437 LoC (>2× the dex task's ~200 LoC estimate). `GoldHero` accepts an optional `nowrap` prop (5 of 9 templates pin the hero on one line); `EmailFooter` accepts optional `signoffPaddingTop` (PrivacyExport's 20px outlier). Byte-equality snapshot suite (`src/lib/emails/email-shells.snapshot.test.tsx`) captures rendered HTML of all 11 templates at the pre-refactor baseline — the refactor left every snapshot identical, validating react-email's inliner produced byte-equivalent output across the nested-custom-components risk the dex task explicitly warned about. JSX-side complement to PR #193's data-side shared-shell singleton extraction. Closes dex `cxt2ez79`. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#196, squash `38507fb`) |
| **PR #195** | **Magic-link visible-URL fallback line below the button.** Adds a 13px muted "Or copy this link: <URL>" line directly below the magic-link button. URL now renders as both the button `href` AND visible body text so recipients on hostile clients (Outlook Word renderer, dark-mode masking the styled background, anti-spam button rewriters) can still copy and paste. Covers all three magic-link variants (`emailMagicLink`, `emailMagicLinkMyReadings`, `emailMagicLinkMyGifts`) via the shared `MagicLink` component. Closes the code-review MEDIUM finding from PR #193 review. Closes dex `jy9fwtf4`. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#195, squash `d51e1cb`) |
| **PR #194** | **Email title renames + Studio editor description affordance.** 3-persona council (Reni / Brook / Theo) landed Pattern B with arrow ("Action → Recipient") across all 11 customer-facing email schemas. Three title surfaces aligned: schema `title:`, `deskStructure.ts` desk-list titles, and `preview.prepare()` titles (third surface discovered mid-implementation when the editor-pane H1 still showed the old name). New `studio/components/EmailDescriptionBanner.tsx` `form.components.input` detects email-schema document roots and renders the description in a tone=default Card above the form fields, visually differentiated from the tone=primary tokens callout. TTL claims in each description match verified code constants (24h magic links, 7d sign-in, 90d reading access, +7/+14/+21d gift reminders, 7d privacy export download). Closes dex `6qayweun`. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#194, squash `f5e2ebb`) |
| **PR #193** | **Shared brand+footer Sanity singleton + Day-7 button overflow fix + MagicLink/PrivacyExport brand-shell adoption.** Two arcs merged together. Arc 1: extracted `emailSharedShell` Sanity singleton carrying brandName/brandSubtitle/signOffLine1/signOffLine2/footerDisclaimer; 7 customer-facing templates accept an optional `shell` prop with `EMAIL_SHARED_SHELL_DEFAULTS` fallback; senders fetch the shell in parallel via a new `fetchSharedShell()` helper; per-email Sanity schemas keep the 5 fields as `hidden: true, readOnly: true` for data preservation (drop is deferred to `2un31amb`). Arc 2: post-merge iteration — Day-7 button overflow fix (root cause: react-email's `<Button>` renders as `<a>` with default content-box sizing, so Tailwind `w-full + px-8 py-4` summed beyond 100% and overflowed the cream container; replaced with `GiftClaimEmail`'s working pattern of inline-style padding + centered `<div>`); Day-7 price dropped from reading card (the email goes to whoever is the recipient — gift recipients didn't pay, self-purchasers already got the price in OrderConfirmation); MagicLink (3 variants) + PrivacyExport rewritten with the full brand shell (Container + brand header + gold-bordered heroLine + body + button + signoff + footer); new `heroLine` field added to all 4 schemas + `buttonLabel` on the 3 magic-link variants + seeded on staging via `migrate-magiclink-privacy-hero-2026-05-25.ts`. Hotfix `19a6f03` directly on `release/v1.2.1` repaired `tsconfig.scripts.json` typecheck after merge: `scripts/migrate-day7-brand-shell-2026-05-25.ts` and `scripts/render-email-previews.ts` referenced fields that moved to the shared shell (caught only by the chained scripts tsconfig — not by the main one; new durable rule `feedback_use_pnpm_typecheck_not_bare_tsc`). Copy-vs-reality audit verified all 5 hard-TTL claims in customer emails match their code constants (`MAGIC_LINK_TTL_MS=24h`, `SESSION_TTL_MS=7d`, `READING_ACCESS_TTL_MS=90d`, Day-7 cron, one-open magic links). Reviewers (sentry-skills:code-review + code-simplifier) surfaced 1 MEDIUM follow-up (MagicLink lost the visible-URL fallback for click-blocked clients — filed `jy9fwtf4`) + 1 high-value simplifier proposal (extract JSX shared components — filed `cxt2ez79`). Closes dex `9t7143kz`, `pkr6rszw`, `glj8qkob`; supersedes 3 follow-ups under `k6ao10b8`. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#193, squash `7a0e08c` + hotfix `19a6f03`) |
| **PR #192** | **Day-7 Delivery brand-shell adoption.** Day-7 email previously rendered as bare `<Text>` lines inside the default `EmailShell` container — no visible brand wordmark, no gold-bordered hero, no reading card, no formal signoff. Refactor brings it into visual parity with `OrderConfirmation` / `GiftClaim*` / gift-confirmation templates: Container (cream, bordered, max-width 600) + brand header section (brandName/brandSubtitle) + gold-bordered heroLine ("Your reading is ready") + body section (preserves folded `bodyIntro`/`bodyPostButton` fallback) + reading card (bg-warm rounded) + italic-serif signoff + Hr + footer with mailto/url + disclaimer. 8 new Sanity fields on `emailDay7Delivery` singleton: brandName, brandSubtitle, heroLine, cardLabel, cardDeliveryLine, signOffLine1, signOffLine2, footerDisclaimer. Fieldsets added (envelope/header/body/card/footer) mirroring OrderConfirmation. Legacy `signOff` field flipped to hidden+readOnly. Migration `migrate-day7-brand-shell-2026-05-25.ts` is idempotent (`setIfMissing` so editor changes survive re-runs) — applied to staging Sanity; production runs at main-merge. 12/12 tests on Day7Delivery (4 new shell-coverage tests for brand header, hero line, reading card, footer); full suite 1752/1752; lint + tsc clean. Filed 3 follow-ups under `k6ao10b8`: MagicLink, PrivacyExport, Day7OverdueAlert brand-shell reviews (first two later absorbed into PR #193's scope expansion). Closes dex `6eeo28cm`. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#192, squash `6bc4cf1`) |
| **PR #191** | **Data-correctness: orphan-backfill + recipient-user-id repair.** Two scripts + one D1 migration. `scripts/backfill-misrouted-sanity-mirror.mts` enumerates production-Sanity submissions in the 2026-05-25T04:39Z–07:00Z misroute window (PR #190 root cause), cross-checks against staging D1, and applies one of four classifications: `orphan` (mirror to staging + delete from prod), `sandbox-residue` (delete from prod only — for e2e test rows whose staging D1 has already been wiped by the cleanup helper), `cross-pollution-skip` (doc exists in both datasets — manual review), `non-orphan-skip` (in prod Sanity only, not sandbox — manual review). Applied against production Sanity 2026-05-25: 6 in-window rows, all 6 classified `sandbox-residue` (zero real customers), 6/6 deleted, post-check GROQ returns 0. `scripts/repair-recipient-user-id.mts` (Sub-PR D for U2/U6) repoints `submissions.recipient_user_id` where the FK resolves to a user whose email doesn't match `recipient_email`. Decision tree per the session PRD D-1 covers bb5fe157-shape (current = purchaser), FK-orphan (current = NULL), third-party-claim (current = some third email — ambiguous-skip), with B5b guard for `gift_claimed_at IS NULL` rows. ISC-B5a (`delivered_at` guard) was dropped mid-EXECUTE after dry-run revealed it was blocking `bb5fe157` — over-cautious; the third-party-claim guard (B5c) already covers the forwarded-gift case it was meant to protect. Staging apply: 7 detector rows → 5 repaired (including the canonical `bb5fe157`, now pointing at `1a87c68d` = mgertzen2's id) + 2 ambiguous-skip preserved. Audit lifecycle (pending → completed) recorded in new `recipient_user_id_repair_log` table (migration 0013, purpose-built — overloading `deletion_log` would have violated the `action IN (started, completed, failed)` CHECK constraint). Production runs HELD per U2/U6 PRD Phase G — fire at main-merge time behind the additional `--i-understand-this-is-production` flag. Vitest 10/10 green on classifier unit tests. Follow-up dex `e0hourn7` queued for within-PR DRY (lift execD1, quoteSql, writeCsv, buildSanityClient to scripts/_lib/). Closes dex `gai69xwr`, `n1ef2zvo` + 4 subtasks. Hold-gate items #10 (U2) + #14 (U6) closed for staging; production-side stays pending. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#191, squash `206b517`) |
| **PR #190** | **Sanity mirror dataset-resolution hotfix.** Resolves the staging→production Sanity dataset misrouting that started 2026-05-25T04:39Z when a manual `pnpm cf:build && wrangler deploy --env staging` ran from a local checkout whose `.env.local` carried `NEXT_PUBLIC_SANITY_DATASET=production`. Next's DefinePlugin baked `dataset:"production"` as a string literal into the worker bundle; the `wrangler.jsonc env.staging.vars.NEXT_PUBLIC_SANITY_DATASET=staging` runtime override was ignored at execution because the read site had been replaced before runtime could intervene. Result: every D1-mirrored submission since that timestamp landed in `e8jsb14m/production` instead of `e8jsb14m/staging` — silent, no error logs, surfaced only when PR #189's workerd fix unblocked the gift-roundtrip e2e specs past the `regenerateGiftClaim` step and they timed out polling `findSubmissionIdByStripeSessionId` against staging Sanity (where the docs weren't). Fix: `getSanityWriteClient()` async, resolves dataset at call time from `getCloudflareContext({ async: true }).env.NEXT_PUBLIC_SANITY_DATASET` (six call sites updated to await), runtime wrangler var now wins. Singleton cache became a per-dataset `Map` so future env switches don't return stale clients. Captured live via wrangler-tail instrumentation (`[sanityMirror] mirrorSubmissionCreate resolved … resp._id=… rev=…`) cross-referenced against GROQ on both datasets — same `_rev` confirmed in production, absent in staging. Post-fix verification: submission `09e4813e-d268-4b4a-a356-1fb3ba18178d` landed in staging, absent from production. ~10 orphan rows from the bad-bake window are stranded in production; backfill tracked in dex `gai69xwr`. New durable rule `feedback_never_local_deploy_cross_env`: NEVER `wrangler deploy --env <target>` from a checkout whose `.env.local` doesn't match — use CI deploy via git push. Build-bake-vs-runtime drift covered. CI: lint-and-typecheck ✅, security-audit ✅, vitest 1738/1738 ✅, storybook ✅, deploy-staging ✅, e2e-sandbox ✅ (first all-green PR-CI of the arc). Closes the Sanity mirror silent-fail blocker. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#190, squash `29413e1`) |
| **PR #189** | **Workerd PT renderer + canonical vendor types audit.** Replaces `<PortableText>` from `@portabletext/react` with a hand-rolled hook-free renderer in `src/lib/emails/PortableTextBody.tsx`. The upstream component calls `useMemo` at the top of render; `@react-email/render`'s workerd entry initialises a null React dispatcher at that point, causing `TypeError: Cannot read properties of null (reading 'useMemo')`. Same throw fires for OC / Day7 / magic-link / privacy-export email sends, but `notifyPaid.ts:76-78,101-103` wraps each in `.catch()` — webhook returns 200 with a warn. Gift purchase + gift claim sends have no catch — throw reaches route handler = 500. Root-caused live via wrangler tail against staging at `/api/internal/gift-claim-regenerate` with two existing gift submissions. The replacement renderer walks `PortableTextBlock[]` and emits the same JSX shape (`<Text>` body / `<Fragment>` inline / `<strong>` / `<em>` / `<Link>` marks). Zero hooks. Also collapses `EmailRichText = string | string[] | PortableTextBlock[]` to `PortableTextBlock[]` only — the fuzzy union shielded a real schema-mismatch bug that shipped to staging in PR #188; `SanityEmailMagicLink.body` was hand-typed `string[]` despite the block-array schema (also fixed). 29 default values in `src/data/defaults.ts` migrated to PT blocks via the existing deterministic `stringToPortableTextBlocks` helper. Internal `sanitizeToPortableTextBlocks` keeps a defensive coercion path that warns in dev only (NODE_ENV-gated) so stray strings from unmigrated prod Sanity records don't crash render. `isPortableTextBlock` guard tightened from `typeof _type === "string"` (permissive) to `_type === "block"` (matches `portableTextToPlainText`'s implicit contract). `SanityPortableTextBlock` now aliases `PortableTextBlock` from `@portabletext/types`. Cloudflare types swap: `D1Database` full vendor swap; R2 leaf types (`R2HTTPMetadata`, `R2UploadedPart`, `R2MultipartOptions`) move to `@cloudflare/workers-types`; `R2Bucket` / `R2MultipartUpload` / `R2Object` / `R2ObjectBody` / `R2PutOptions` stay hand-rolled because vendor's `R2Bucket.put` bakes in non-generic `ReadableStream` that conflicts with DOM `ReadableStream<Uint8Array>` at three consumer sites. `cloudflare:workers` virtual-module shim stays ambient but inline interfaces replaced with `import("@cloudflare/workers-types").X` type queries. Empirical probe verified per-file `import type` is conflict-free for leaf types. 5 regression tests added under `gift email send paths (PR #188 regression)` in `src/lib/resend.test.ts` + structural guard asserting no re-import of `@portabletext/react`. CI: lint ✅, typecheck ✅, vitest 1734/1734 ✅, next build ✅. PR e2e-sandbox merged with 2 known-orthogonal failures (Sanity mirror, fixed by PR #190). | ✅ shipped 2026-05-25 to `release/v1.2.1` (#189, squash `fea50ce`) |
| **PR #188** | **Email body consolidation + variant split.** Collapses 3–5 per-email Portable Text body fields into one `body` PT field across 9 email schemas (`emailOrderConfirmation`, `emailRecipientIntakeReceived`, `emailDay7Delivery` + `emailPrivacyExport` button-split with `bodyIntro` / `bodyPostButton`, three magic-link variants, plus the now-split gift emails). Splits the variant-overloaded `emailGiftClaim` and `emailGiftPurchaseConfirmation` schemas into single-purpose documents (`emailGiftClaim` for first-send + new `emailGiftClaimReminder`; `emailGiftPurchaseConfirmation` → `emailGiftPurchaseConfirmationSelfSend` + `emailGiftPurchaseConfirmationScheduled`). Caller code in `src/lib/resend.tsx` branches by variant internally — route handlers (`flip-to-scheduled`, `cancel-auto-send`, `resend-link`, `send-now`, `stripe/webhook`) keep their existing input shape. Legacy fields stay `hidden: true` + `readOnly: true` so existing data isn't lost; React templates render the new body with `<PortableTextBody>` and fall back to the legacy layout when `body` is empty. `signOff*` fields stay separate (italic-serif flourish preserved). `scripts/migrate-email-body-consolidate-2026-05-25.mts` is idempotent + dataset-aware (concatenates legacy fields into `body` for single-variant emails; clones the legacy gift-purchase document into the two new singletons). Seed defaults gained a `normalizePtFields()` step that converts string / string[] PT defaults to proper PT blocks via `stringToPortableTextBlocks` before `createIfNotExists`. `scripts/fix-staging-email-pt-fields-2026-05-25.mts` patched the four newly-created staging singletons after the seed-vs-schema mismatch surfaced (claimUrlHelper / bodyFirstSend / greeting / body shapes). Test count 1719 → 1731 (+12). Staging Studio deployed; staging migration ran (7 mutated + 2 cloned, 1 deferred-then-fixed). **Prod deploy + migration deferred until end of Sanity arc.** Sub-PRs queued behind for PR-Email-2 cleanup (drop deprecated fields, remove fallbacks). 4 follow-ups filed under dex `k6ao10b8`: visual uniformity, shared brand+footer singleton, canonical vendor-type tightening (durable rule `feedback_canonical_vendor_types`), email titles/descriptions for Becky's clarity. | ✅ shipped 2026-05-25 to `release/v1.2.1` (#188) |
| **PR #178** | **Release merge — `release/v1.2.0` → `main` (merge commit `82755fd`).** 81 commits, 274 files, +12865/-1722. Carries everything v1.1.x (Phase 3 scheduling rebuild, U2/U6 lock+422 gate, recipient-email integrity) plus v1.2.x (launch-readiness e2e epic Sub-PRs A/B/C, smoke-walk fix arc β/δ/α/γ/ε, AI accessibility, Sanity migration runner + seed-script extension, MANUAL_SMOKE_TEST rewrite, U2/U6 investigation cluster, qs CVE hotfix, Resend hardening, Phase 7 customer-facing emails, Studio desk restructure, Day 2 removal + universal slot rendering, readingPriceDisplay token catalog). **Pre-merge ops applied to production:** D1 migrations 0004→0012 applied (`pnpm migrate:apply:prod`; `submissions` row count 0→0); 27 Sanity migrations re-run idempotently (8 mutated additive `setIfMissing` closing staging→prod drift, 19 noop, 0 failed; log at `docs/prod-migration-run-2026-05-24.log`); 10 customer email/page singletons seeded to prod dataset via `scripts/seed-customer-emails-and-pages.mts production` (createIfNotExists, GROQ-verified 10/10); Sanity Studio deployed to https://withjosephine.sanity.studio/ (2/2 schemas, picks up PR #174 customer email schemas + PR #175 desk restructure + PR #176 Day 2 removal + PR #177 token catalog). **Hold-gate items still open after this merge:** apex unpark + Stripe live-mode (deferred), U2/U6 Sub-PR D data-repair script, U3/U4/U5/U7/U8 UX promotions, pre-prod data cleanup (D1 has 0 rows; R2 + Sanity may have test residue), reading-price reconcile. **CI gate:** PR #178 lint-and-typecheck ✅, security-audit ✅, test (vitest) ✅, storybook ✅, deploy-staging ✅, Playwright (chromium) ✅ 4m4s after companion hotfix #179. | ✅ shipped 2026-05-24 to `main` (#178, merge `82755fd`) |
| **PR #179** | **Hotfix — `ci(e2e): stub R2_* env vars` for booking-photo-upload spec (squash `1c9f5d3`).** `tests/e2e/specs/booking-photo-upload.spec.ts` (added in #154) hits `/api/booking/upload-url` which `requireEnv`s `R2_ACCOUNT_ID` + `R2_ACCESS_KEY_ID` + `R2_SECRET_ACCESS_KEY` + `R2_BUCKET_NAME`. `.github/workflows/e2e.yml` triggers only on PRs to `main` (path-filtered), so Sub-PRs against `release/v1.2.0` never ran Playwright, hiding the env gap until PR #178 (release → main) fired it for the first time. Added 4 dummy `e2e-dummy-*` env vars next to the existing Stripe / Sanity / Turnstile dummies. aws4fetch signing is offline (pure HMAC math) so the URL-shape assertion passes with dummy creds; the second test's PUT is intercepted by Playwright `page.route(uploadUrl, fulfill 200)` before any real network call. Failure pattern matches `feedback_validate_active_branches.md`. | ✅ shipped 2026-05-24 to `release/v1.2.0` (#179, squash `1c9f5d3`) |
| **PR #175** | **Studio desk restructure — Pages / Emails / Form Building Blocks.** Restructures `studio/schemas/deskStructure.ts` into three top-level groups separating singletons (configured surfaces) from document-type collections (reusable atoms). 📄 Pages = 9 customer-facing browser surfaces (Landing, Listen, MyReadings, MyGifts, Magic Link Verify, Gift Claim, Gift Intake, Under Construction, 404) with nested 📋 Booking Flow (Booking Page, Thank You Page, Booking Form, Booking Gift Form). ✉️ Emails = 8 email singletons with Preview tab preserved via `emailSingletonListItem` (Phase 7 surface). Titles cleaned: dropped "Email — " prefix since the group label already disambiguates. 🧩 Form Building Blocks = Form Fields + Form Sections. Root unchanged: Site Settings, Theme · Readings, Testimonials, FAQ Items · Legal Pages · Submissions. Bundled into v1.2.0 (rather than deferred to v1.3.x) to ship one coordinated Studio deploy (dex `ath44ovq`) alongside Phase 7 rather than two close-together changes for Becky. Zero data risk — singleton documentIds unchanged, only list-item arrangement. Closes dex `c1ekzdbi`. | ✅ shipped 2026-05-24 to `release/v1.2.0` (#175, squash `42d5ac2`) |
| **`503fd71`** | **docs(dex): file Studio deploy task bundled with desk restructure.** Bookkeeping commit direct to `release/v1.2.0` per project convention. Adds `ath44ovq` (Deploy Studio) under epic `mc6p8m8t`, blocked-by `c1ekzdbi` (Studio desk restructure). Rationale: production Studio is a single Sanity-hosted deployment; one coordinated deploy (Phase 7 preview surface + desk restructure together) is less disruptive for Becky than two close-together ones. | ✅ shipped 2026-05-24 to `release/v1.2.0` (`503fd71`) |
| **PR #174** | **Phase 7 PR-A1 — Sanity-editable customer emails + Studio live preview.** Ships the Sanity-editable surface for all 8 customer-facing transactional/reminder emails (`emailOrderConfirmation`, `emailDay2Started`, `emailDay7Delivery`, `emailGiftPurchaseConfirmation`, `emailGiftClaim`, `emailMagicLink`, `emailRecipientIntakeReceived`, plus new `emailPrivacyExport` replacing inline HTML in `resend.tsx`) with a live Preview tab in Studio (`S.view.component(EmailPreview)` + `sanity-plugin-iframe-pane@5.0.12` for auto-refresh on draft save). Slot validation (`Rule.custom` block-on-publish via `studio/lib/validateSlots.ts` + canonical allowlist in `src/lib/emails/slots.ts`) catches typoed `{token}` placeholders before they ship. New worker route `/api/email-preview/[type]` renders any of the 8 templates as text/html with merged Sanity-draft + code-defaults copy + fixture vars (`PREVIEW_FIXTURE`). Auth via `@sanity/preview-url-secret` standalone (outside Presentation Tool): Studio's `createPreviewSecret` mints a short-lived secret per render via the Sanity-authenticated session client; worker validates server-side via `validatePreviewUrl`. CSP `frame-ancestors` tightened for email-preview path to `STUDIO_ORIGIN_ALLOWLIST` (prod) + `STUDIO_LOCAL_DEV_ORIGINS` (dev-only). 503 fail-loud on missing `SANITY_READ_TOKEN` instead of silent defaults. Pentester audit ran on the diff — 1 HIGH + 2 MED + 3 INFO findings, ALL closed before push (preview-url-secret integration fixed F1; F2/F3 fixed inline; F7 dropped origin gate as redundant post-preview-secret). Verified live on staging worker + end-to-end in local Studio→local Next.js iframe roundtrip (Becky-shape UX confirmed: edit field, save, iframe auto-refreshes with new copy). Production Sanity seeded (11 singletons created idempotently). Worker code only — Studio deploy bundled with dex `ath44ovq` (blocked on `c1ekzdbi` desk restructure). | ✅ shipped 2026-05-24 to `release/v1.2.0` (#174, squash `1fa138a`) |
| **`17da7b9`** | **chore(deps): override qs to ^6.15.2 — close GHSA-q8mj-m7cp-5q26.** Bookkeeping commit direct to `release/v1.2.0` per project convention. osv-scanner started flagging `qs@6.15.1` on 2026-05-23 after the CVE was disclosed on 2026-05-22; the dep was unchanged in the repo but the live advisory DB caught up. Added `"qs@<6.15.2": "^6.15.2"` to the existing `pnpm.overrides` block (mirroring js-yaml/fast-xml-parser/postcss/etc.). Transitive-only, no direct consumer. Unblocked `deploy-staging` for the post-merge CI of PR #169. | ✅ shipped 2026-05-24 to `release/v1.2.0` (`17da7b9`) |
| **PR #169** | **Sub-PR A — capture userAgentHash on listen_audit link_issued rows.** Forensic instrumentation from the U2/U6 PRD. `issueMagicLink` threads a new `userAgentHash` arg into its internal `writeAudit({eventType: link_issued})` call. Both call sites updated: `/api/auth/magic-link/route.ts` pulls from `getRequestAuditContext`, `/api/internal/issue-magic-link/route.ts` pulls from `authorizeAdminToken.audit`. Vitest covers all three paths (positive UA hash, null UA, both routes). E2e adds `queryStagingD1` + `escapeSqliteLiteral` helpers in `tests/e2e/helpers/stagingApi.ts`, extracts shared `execWranglerD1` primitive (`sandboxResidueCleanup.ts` now delegates), and adds a gated audit-row assertion in `listen-roundtrip.spec.ts` skipped behind `UA_AUDIT_HASH_DEPLOYED` until the worker is on the new code (un-gate task on BACKLOG). Closes the gap discovered in U2 investigation where issue-time UA was always NULL. Staging smoke confirmed end-to-end with cryptographic proof — POSTed `/api/auth/magic-link` with a distinctive UA, local sha256 of the UA string matched the value persisted to `listen_audit.user_agent_hash`. | ✅ shipped 2026-05-24 to `release/v1.2.0` (#169, squash `364ea77`) |
| **PR #168** | **X-E2E-Resend-DryRun helper wiring.** Companion to #167. Adds `resendDryRunHeaderOrEmpty()` + `sandboxRequestHeaders()` composer in `tests/e2e/helpers/stagingApi.ts`. All 4 sandbox specs (`gift-roundtrip`, `listen-roundtrip`, `stripe-roundtrip`, `v120-smoke-validation-roundtrip`) updated to use the composed helper in `test.use({ extraHTTPHeaders })`. `.github/workflows/e2e-sandbox.yml` passes `STAGING_RESEND_E2E_DRY_RUN_SECRET` into the runner env. Result: every sandbox request to staging now carries the header, the worker matches it against `RESEND_E2E_DRY_RUN_SECRET` and skips Resend at the `sendOrSkip` seam. PR sandbox check proved the end-to-end path with zero Resend hits. Staging humans never set the header → real emails unaffected. | ✅ shipped 2026-05-21 to `release/v1.2.0` (#168, squash `f2fb367`) |
| **PR #167** | **Per-request Resend dry-run header (worker scaffold).** Adds `shouldDryRunFromRequestHeader()` in `src/lib/resend.tsx` that reads `X-E2E-Resend-DryRun` via `next/headers` and compares against `RESEND_E2E_DRY_RUN_SECRET`. `sendOrSkip` OR's the new check with the existing `RESEND_DRY_RUN` global flag. 4 new vitest cases assert: matching header → skip, header absent → send, header mismatch → send, secret unset → ignore header. Solves the e2e-burns-Resend-quota problem — today's smoke-walk arc + QATester walks burned ~80% of the 100/day free tier from cumulative sandbox runs. After #168 wires the helper through, sandbox runs cost zero Resend. Touches `src/lib/resend.{tsx,test.ts}` only — neither path triggers `e2e-sandbox.yml`, so this PR shipped Resend-safe. | ✅ shipped 2026-05-21 to `release/v1.2.0` (#167, squash `339df47`) |
| **`e45d4fc`** | **Narration-comment sweep across the smoke-walk arc.** Bookkeeping commit direct to `release/v1.2.0` per project convention. Stripped task-ID test name suffixes (`(C1)`, `(C2)`, `(C5 instrumentation)`, `(C4 regression net)`), removed comment blocks narrating the smoke walk / dates / persona names / PR numbers across 7 merged files (`notifyPaid.ts`, 5 test files, `gift-roundtrip.spec.ts`), trimmed the 10-line WHY-comment in `notifyPaid.ts` to 3 lines carrying only the load-bearing rationale (gift email is purchaser, not recipient). Added cleaned `v120-smoke-validation-roundtrip.spec.ts` (the QA validation harness, Story 2 deferred to the unit test). PR #166 closed as superseded. Triggered `ci.yml` only — no sandbox, no Resend. Per `feedback_comments_over_logged.md`. | ✅ shipped 2026-05-21 to `release/v1.2.0` (`e45d4fc`) |
| **PR #165** | **Sub-PR ε — smoke-walk gift-recipient-listen sandbox roundtrip.** Closes the load-bearing E2E gap from the 2026-05-20 gap-analysis: no existing spec walked scheduled-gift purchase → recipient claim+intake → recipient listens, which is why the staging walk found C2 + C4 while the CI suite was green. Extends `gift-roundtrip.spec.ts` with the recipient-listens leg (cold visit /listen → magic-link with RECIPIENT email → verify lands on `welcome=1` in one round → welcome ribbon + audio + PDF render). Also asserts thank-you body never contains 'thank you, there' (C4 net). Standing regression net for α's notifyPaid fix + δ's recipientNameFor fallback. Runs nightly weekday 12:00 UTC + on every `release/*` PR touching booking paths. | ✅ shipped 2026-05-21 to `release/v1.2.0` (#165, squash `3166faf`) |
| **PR #163** | **Sub-PR γ — smoke-walk C1+C5 instrumentation.** Both `/api/booking/gift` and `/api/booking/gift-redeem` returned 400 in the 2026-05-20 smoke walk with zero log surface. Adds 6 tagged `console.error` lines on every silent 400 branch (`[booking-gift] invalid_json | invalid_body_shape | honeypot_tripped | validation_failed | turnstile_rejected` + `[gift-redeem] turnstile_rejected`). Payloads carry `cfRay` + `ip` + field NAMES (schema slugs only, no user input). 5 vitest regression assertions. Pure observability — no behavior change. Strong hypothesis from investigation: Turnstile token expiry (300s TTL); instrumentation lets the next failure self-identify before attempting a root-cause fix. | ✅ shipped 2026-05-21 to `release/v1.2.0` (#163, squash awaited) |
| **PR #164** | **Hotfix — recipient-name fallback crash on undefined email.** Sub-PR δ (#161) extended `recipientNameFor` to call `recipientFirstNameFromIntakeResponses(responses, submission.email)`. 3 gift-purchaser test cases in `src/app/thank-you/[readingId]/page.test.ts` mock submissions without an `email` field, causing `firstNameFromEmailLocal(undefined)` to crash on `.split()`. Production unaffected (real submissions always populate email at create). Two-layer defence: `recipientNameFor` coalesces undefined email to `""`; `firstNameFromEmailLocal` short-circuits on falsy. Restored release/v1.2.0 CI to green after δ + α deploys had skipped for ~9 hours. | ✅ shipped 2026-05-21 to `release/v1.2.0` (#164, squash `29f925e`) |
| **PR #162** | **Sub-PR α — smoke-walk C2 + partial C3.** `applyPaidEvent` was resolving a user from `submission.email` and writing that userId to `recipient_user_id`. For gifts, `submission.email` is the PURCHASER — clobbered `recipient_user_id`, broke the listen-page session-vs-submission linkage on scheduled gifts (`submission.recipientUserId !== session.userId` → infinite magic-link loop, J4d from the smoke walk). Self-send "worked" only because purchaser and recipient emails happened to match. Fix: skip the `getOrCreateUser` for gifts; `redeemGiftSubmission` populates `recipient_user_id` correctly from intake. Side benefit: same column drives the `/my-readings` listing scope, so this also un-empties the actual recipient's list. | ✅ shipped 2026-05-21 to `release/v1.2.0` (#162, squash `81c4e91`) |
| **PR #161** | **Sub-PR δ — smoke-walk C4 thank-you surfaces.** `/api/booking/gift-redeem` overwrites `submission.responses` wholesale with the recipient's own intake, wiping the purchaser-typed `recipient_name`. `recipientNameFor` was returning the legacy `"there"` sentinel → "Thank you, there." on J4 thank-you, blank on J3. Fix: extend `recipientNameFor` with a fallback chain through `recipientFirstNameFromIntakeResponses` (first_name → legal_full_name first-token → email-local). Closes 2 of 4 C4 surfaces (J3 + J4 thank-you); J3 intake heading + listen-page greeting need separate Sanity schema plumbing (deferred). | ✅ shipped 2026-05-20 to `release/v1.2.0` (#161, squash `84bdeeb`) |
| **PR #160** | **Sub-PR β — smoke-walk S1 + S2 + U1.** Adds `giftClaimedAt` (datetime) + `recipientUserId` (string) to `studio/schemas/submission.ts` — both are mirrored from D1 by `mirrorSubmissionPatch` but were undeclared, surfacing "unknown fields" warnings on every gift recipient submission. Adds `recipient_intake_received` to the `emailsFired` enum so Becky's publish stops red-flagging the row. Strips leading "The " from listen-page `readingName` so the title no longer reads "Your The Birth Chart Reading is ready" (U1). 3 vitest schema-shape assertions (with `vi.mock("sanity")` pattern established in `submission.test.ts`) + 1 listen-page case. Max-action post-merge: `pnpm studio:deploy`. | ✅ shipped 2026-05-20 to `release/v1.2.0` (#160, squash `6bae7f3`) |
| **PR #159** | Comprehensive rewrite of `docs/MANUAL_SMOKE_TEST.md` (item 6 of hold-gate). Previous version had 9 journeys but stopped J3/J4 at recipient intake submit, missed Becky's delivery loop for gift recipients, missed Phase-3 /my-gifts actions (send-now D-10, cancel-scheduled D-11), and skipped admin-side emails entirely. **Changes:** J1 split into J1a (purchase) / J1b (Becky delivers) / J1c (listen); J3 split into J3a/b/c with the recipient-delivery loop; J4 split into J4a/b/c/d; J5 restructured into J5a (sign-in) / J5b (repeatable actions: view-link, edit-recipient, reschedule, regenerate) / J5c (cancel-auto-send) / J5d (send-now D-10) / J5e (cancel-scheduled D-11, includes "non-refundable" wording check for EU CRD waiver) / J5f (Sanity copy-edit reflectivity proof). New J10 mobile viewport at iPhone 13 (390px) — primary traffic per CLAUDE.md. New J11 admin-email checklist (Josephine notification, Recipient Intake Received, Day-2 Started flagged disabled per #149). J6 retired as standalone since listen-page magic-link now exercised via J1c/J3c/J4d. Thank-you copy variant checks called out in each purchase journey (4 variants: self-purchase, gift self-send purchaser, gift scheduled purchaser, gift recipient). Audit-trail surface eyeball in J1b. Photo-renders-in-Studio check in J1a. Cross-user leak watchfulness explicit in J5a + J3c. Suggested suffix table expanded for the 3 terminal-action test pairs. 214 insertions / 75 deletions. No production code | ✅ shipped 2026-05-20 to `release/v1.2.0` (#159, squash `b579768`) |
| **PR #158** | Add `myGiftsPage` + `emailGiftPurchaseConfirmation` to `scripts/seed-customer-emails-and-pages.mts` SEEDS array. Audit-driven fix: prod-migration runner (PR #157) revealed both singletons were missing on production, so `migrate-my-gifts-cancel-scheduled-copy-2026-05-19.ts` silently skipped (item 9 of hold-gate, the D-11 cancel-scheduled copy seed). Executed against production immediately after merge — turned out **9 customer singletons were missing entirely** (not just the two gift-related ones): `listenPage`, `myReadingsPage`, `myGiftsPage`, `magicLinkVerifyPage`, `emailOrderConfirmation`, `emailDay2Started`, `emailDay7Delivery`, `emailMagicLink`, `emailGiftPurchaseConfirmation`. Site rendered fine off `src/data/defaults.ts` fallbacks, but Studio had no editable doc for any of those surfaces. `createIfNotExists` is idempotent — re-runs on datasets that already have the docs are no-ops. After seed, re-ran the prod-migration runner: previously-skipped D-11 copy migration applied 4 cancel-scheduled fields successfully. Closes hold-gate item 9 | ✅ shipped 2026-05-20 to `release/v1.2.0` (#158, squash `c5b9a5f`) |
| **PR #157** | `scripts/apply-all-sanity-migrations-to-prod.mts` — one-shot wrapper that runs all 27 idempotent migration scripts against the production Sanity dataset in a chronologically-pinned `MIGRATION_ORDER`. Per-script stdout capture, MUTATED/noop/FAIL classification via regex on known mutation-log patterns, full log written to `docs/prod-migration-run-YYYY-MM-DD.log`. Pre-flight: requires `SANITY_WRITE_TOKEN` + `NEXT_PUBLIC_SANITY_PROJECT_ID`, fails fast if `MIGRATION_ORDER` drifts from on-disk `migrate-*.{ts,mts}` files (forces future additions to update the list). **Audit conducted 2026-05-20** to validate idempotency across all 27 scripts: 4 patterns identified — `setIfMissing` (additive), drift-detect-then-set (`migrate-gift-copy-2026-05.ts` shape — returns "drift" if mismatch), `createOrReplace` with canonical content, `unset` (no-op when field absent). 3 scripts had prior "ran on production" markers in CHANGELOG (`strip-consent`, `restore-time-unknown`, `refund-policy-no-refunds`); re-running them in the batch is harmless. Executed against production 2026-05-20: first pass 9 MUTATED / 18 noop / 0 failed; surfaced 2 missing singletons → fixed via #158 → second pass after seed 8 MUTATED / 19 noop / 0 failed. Closes hold-gate audit-trail for the "have all Sanity migrations actually run on prod?" question | ✅ shipped 2026-05-20 to `release/v1.2.0` (#157, squash `8eb27bb`) |
| **PR #156** | `src/app/robots.ts` — Next.js App Router convention builds `/robots.txt` at the edge. Decision locked 2026-05-20: **allow** citation bots (OAI-SearchBot, PerplexityBot, Claude-SearchBot, Claude-User) so the site surfaces in AI-powered search; **block** training bots (GPTBot, ClaudeBot, Google-Extended, CCBot) so content isn't ingested into training corpora without permission. Also disallows private routes (`/api`, `/auth`, `/my-gifts`, `/my-readings`, `/gift/claim`, `/gift/intake`, `/gift/already-submitted`, `/listen`, `/thank-you`) for all crawlers as defense-in-depth beyond per-route `metadata.robots = { index: false }`. Closes hold-gate item #7 | ✅ shipped 2026-05-20 to `release/v1.2.0` (#156, squash `c418915`) |
| **PR #155** | v1.2.x launch-readiness e2e epic **Sub-PR C — cleanup tail**. Closes ISC-14..24 (11 cleanup ISCs), epic now **24/24 complete**. Tagged `v1.2.0` at squash commit `94e9d5d`. **ISC-14** drop MSW dead weight — delete `tests/e2e/mocks/external.ts` (3 hand-rolled http handlers for Resend / Stripe API / buy.stripe.com that were never reached; the actual mock seam is fixture sidecar + `page.route` + signed webhook + Turnstile bypass env) + `tests/e2e/global-teardown.ts` (MSW-only), strip MSW init from `global-setup.ts`, drop `msw@^2.14.6` from devDependencies + pnpm-lock. `tests/e2e/README.md` updated with the real mock-seam table. **ISC-15** `waitForDraftRestore` recursive `setTimeout` polling → `expect(locator).not.toHaveValue("", { timeout: 5000 })` — idiomatic Playwright assertion. **ISC-16** `turnstileStub.execute(string \| Element)` — strict id-key lookup via `widgets.get(id)` on string, exact-match-or-contains on Element. Drops the "fall back to first widget" silent-success path that would mask multi-widget bugs. **ISC-17** new `assertWranglerAuthenticated()` preflight in `global-setup.ts` when `isSandboxMode()` and not `isProdTarget()`. Runs `pnpm exec wrangler whoami` via `spawnSync` (15s timeout), throws with explicit remediation if non-zero exit. Compares whoami stdout against `CLOUDFLARE_ACCOUNT_ID` if set — refuses to run if authenticated to a different account. One clear failure beats 14 spec-level "wrangler login required" errors mid-suite. **ISC-18** `x-e2e-reset-token` no longer broadcast via `playwright.config.ts:53 extraHTTPHeaders` — new `tests/e2e/helpers/e2eReset.ts` exports `resetE2EDatabase(request)` + `resetTokenHeaders()`. `helpers/giftClaimCookie.ts` merges `resetTokenHeaders()` into its POST headers. **14 spec call-sites migrated** from `await request.post("/api/e2e-reset").catch(() => undefined)` to `await resetE2EDatabase(request)` (`gift-cancel-auto-send`, `gift-cancel-scheduled`, `gift-claim-replay`, `gift-cross-purchaser-leak`, `gift-flip-to-scheduled-tz`, `gift-local`, `gift-redeem-consent-shape-local`, `gift-redeem-local`, `gift-redeem-self-send-local`, `gift-regenerate-claim-link`, `gift-send-now`, `listen-local`, `my-gifts-local`, `stripe-local`). **ISC-19** `signInViaMagicLink(page, {email, next})` swaps `page.waitForURL(\`**${next}**\`)` for a regex with escaped meta-chars (`next.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")`). The glob form silently misbehaves when `next` carries `?`, `*`, `[`, `(`, etc. **ISC-20** new `tests/e2e/constants.ts` centralizes `FIXTURE_SIDECAR_PORT = 47391`, `E2E_RESET_TOKEN_FALLBACK = "e2e-reset-stable-token"`, `E2E_ADMIN_API_KEY_FALLBACK = "e2e_admin_api_key_dummy"`, `E2E_RESET_TOKEN_HEADER = "x-e2e-reset-token"`, and a `TIMEOUTS` bucket (capture / expect / action / navigation / test / testLong / testExtraLong). `playwright.config.ts` + `fixtures-server.ts` import from here; the latter re-exports the port for back-compat. **ISC-21** `assertEnvGuards()` now also throws if `STRIPE_WEBHOOK_SECRET` is set but doesn't start with `whsec_` — `Stripe.webhooks.constructEvent` would reject anyway; fail-fast at setup beats a 400 deep inside a spec. **ISC-22** new binding constraint in project-root `CLAUDE.md` (file lives outside the `www/` git repo so this edit is local-only, not in the PR diff): test-naming convention is the mode discriminator — `*-roundtrip.spec.ts` → sandbox (`SANDBOX_SPECS_PATTERN`), `*-readonly-smoke.spec.ts` → prod-smoke (`PROD_SMOKE_PATTERN`), everything else → mock. Source-of-truth: `tests/e2e/helpers/e2eMode.ts`. **ISC-23** drop `app.get("/vX/data/live/events/:dataset", sseHeartbeat)` debug leftover from `fixtures-server.ts:266-267`; the `v\\d+/...` route handler immediately below is the real one. **ISC-24** rename `clickThroughIntakePages(page, expectedPageCount)` arg → `maxClicks` since it's a safety belt against runaway loops, not a hard count assertion. Call sites use positional args; no migration. **Pre-merge gates:** pnpm lint clean, pnpm typecheck clean, vitest 1608/1608, playwright mock 56/56 in 1.9 min, pnpm build clean. **Process notes:** Sub-PR C shipped on feature branch `feat/sub-pr-c-cleanup-tail` → PR → squash-merge — Sub-PR B's workflow violation (direct-to-release commit, force-push recovery) memorialized as `feedback_sub_pr_via_feature_branch.md` and applied here without incident. **Tagging convention introduced:** `git tag v1.1.0 882752d`, `git tag v1.2.0 94e9d5d` — release-branch HEAD gets the version tag at cycle end. Future hotfix flow: `git checkout -b hotfix/vX.Y.1 vX.Y.0` → patch → `git tag vX.Y.1` on the resulting commit | ✅ shipped 2026-05-20 to `release/v1.2.0` (#155, squash `94e9d5d`); tagged `v1.2.0` |
| **PR #154** | v1.2.x launch-readiness e2e epic **Sub-PR B — P0 spec coverage**. 5 of 24 epic ISC complete (ISC-9..13); cumulative 13/24. Five new mock-mode specs filling the security + replay + upload-contract gaps reviewers flagged in the Sub-PR A audit. **ISC-9 gift-cross-purchaser-leak** (`tests/e2e/specs/gift-cross-purchaser-leak.spec.ts`, +111 LoC): 3 sequential purchases (A→A→B with `clearCookies` between to defend against future session side-effects), then `signInViaMagicLink(B) → /my-gifts`. Asserts (a) only B's recipient renders on /my-gifts, A's two recipients do not, (b) unauthenticated `POST /api/gifts/<A-id>/cancel-scheduled` returns 401, (c) authenticated-as-B same POST returns **404** (NOT 401/403 as the ISC text originally said — `authorizeGiftPurchaser.ts:57` deliberately collapses unknown-id and cross-user to the same 404 + timing-parity placeholder lookup so attackers cannot enumerate gift IDs by status code; spec guards this as a regression net). ISC text revision documented in epic PRD. **ISC-10 gift-cancel-auto-send** (+81 LoC): mirrors `gift-cancel-scheduled` shape — book scheduled, sign in, arm via `getByRole("button", { name: /send the link myself instead/i })`, confirm via "tap again to confirm", `page.route` intercept fulfills `/api/gifts/<id>/cancel-auto-send` with 200 + claimUrl, assert URL+method captured. **ISC-11 gift-regenerate-claim-link** (+130 LoC): self-send gift → webhook issues token1 (captured email's html carries the `/gift/claim?token=<token1>` URL) → admin POST `/api/admin/regenerate-gift-claim` with `x-admin-token: ADMIN_API_KEY` → outcome=regenerated + token2 issued in 2nd captured email. Assertions: token2 !== token1; navigating `/gift/claim?token=<token1>` lands at `/gift/claim?invalid=1` via the verifyGiftClaimToken redirect chain; new token routes through `/api/gift/claim` → `/gift/intake?welcome=1`; immediate re-issue returns 409 cooldown (reason=cooldown); wrong admin token returns 404 (silent — matches authorizeAdminToken's identical-shape failure contract). **ISC-12 gift-claim-replay** (+84 LoC): full self-send flow — webhook → real claim navigation → seedIntakeDraft → clickThroughIntakePages → consents → submit → assert `/thank-you/.*gift=1.*redeemed=1`. Replay step: clearCookies, re-navigate `/gift/claim?token=<same>` → assert `/gift/claim?invalid=1` (token now resolves a submission with `giftClaimedAt` set, so verifyGiftClaimToken returns null, route 303s to invalid). Deterministic — not 500, not silent-success. **ISC-13 booking-photo-upload** (+128 LoC, 5 sub-tests): (a) happy path mints signed R2 URL matching `^https://[^.]+\.r2\.cloudflarestorage\.com/[^/]+/submissions/[^/]+/photo-\d+-[a-z0-9._-]+\?[^"\s]*X-Amz-Expires=` and key `^submissions/.../photo-<ts>-<filename>`; (b) browser PUT against the signed URL — intercept captures method + Content-Type + body byte size (Content-Length is a forbidden header in browser fetch; aws4fetch signs against the byte count which IS what gets carried via Blob, so the production assertion is body-size not header-value); (c) missing turnstileToken → 400 even with BOOKING_TURNSTILE_BYPASS=1 (the typeof string check fires BEFORE verify); (d) unsupported MIME (application/pdf) → 415; (e) oversize (100MB) → 413. Scope note: ISC text's "token scoped wrong → 403 no CORS" is real-R2 behavior captured in `feedback_r2_cors_content_length_header.md`; not directly testable in mock mode, deferred to sandbox if it ever regresses. **Test infrastructure:** dry-run resend capture (`src/lib/resend.tsx:88`) and the fixture-server sidecar's CapturedEmail type + POST handler (`tests/e2e/fixtures-server.ts`) extended to include `html`. Unblocks token extraction from captured emails — load-bearing for ISC-11 + ISC-12 since `regenerateGiftClaim`'s route response intentionally does NOT include claimUrl (admin contract returns redacted email + delivery method only). Production paths unchanged — the capture branch only fires when `RESEND_DRY_RUN=1` AND `E2E_CAPTURE_URL` are set, which they only are in the playwright webServer.env. **Workflow self-correction:** initial commit landed directly on `release/v1.2.0` (workflow violation — PRs #152, #153 went through feature branch → PR → squash-merge). Force-pushed release back to `cb185c5`, recreated commit on `feat/sub-pr-b-p0-coverage`, opened PR #154, merged via squash. Documented for future runs: every Sub-PR ships on a feature branch first. **Pre-merge gates:** pnpm lint clean, pnpm typecheck clean, vitest 1608/1608, playwright mock 56/56 (5 new + 51 existing) in 1.7 min, pnpm build clean (with `NEXT_PUBLIC_R2_PUBLIC_HOST=images.withjosephine.com`). **CI:** PR #154 lint-and-typecheck + test + storybook + security-audit + deploy-staging all green; deploy-production/smoke-production skipped (PR not on main). Sub-PR C (P1 cleanup tail, ~2h, ISC-14..24) remains as the last slice of the v1.2.x epic | ✅ shipped 2026-05-20 to `release/v1.2.0` (#154, squash `b6225db`) |
| **PR #153** | v1.2.x launch-readiness e2e epic **Sub-PR A — orchestration infra**. 8 of 24 epic ISC complete (ISC-1..8). **ISC-1** `globalSetup` hard-fails on missing sandbox env vars; replaces module-scope `test.skip(!env)` across the 3 roundtrip specs (was pass-as-skipped on missing secrets — silent rot). Required sandbox env: `CF_ACCESS_CLIENT_ID`, `CF_ACCESS_CLIENT_SECRET`, `ADMIN_API_KEY`, `DO_DISPATCH_SECRET`; prod-target short-circuits the check. **ISC-2** new `.github/workflows/e2e-sandbox.yml` — weekdays 12:00 UTC cron + `release/*` PR trigger paths-filtered + `workflow_dispatch`. Replaces the manual `E2E_SANDBOX=1 source .env.staging && pnpm exec playwright test` loop. **ISC-3 (Max-action shipped)** 6 GH staging-environment secrets staged: `STAGING_CF_ACCESS_CLIENT_ID`, `STAGING_CF_ACCESS_CLIENT_SECRET`, `STAGING_ADMIN_API_KEY`, `STAGING_DO_DISPATCH_SECRET`, `SANITY_WRITE_TOKEN`, `STAGING_CRON_SECRET`. (Originally claimed 4, then 5; `CRON_SECRET` surfaced from env-var audit during CI debug — see learning below.) **ISC-4** `/api/e2e-reset` + `/api/e2e-gift-claim-cookie` gates: deny-list (`ENVIRONMENT !== "production"`) → allow-list (`staging | development` + `E2E=1`). Shared `isE2ERouteGateOpen` at `src/lib/e2e/routeGate.ts`. Behavioral tightening: `preview` and any unrecognized env no longer pass; intentional. **ISC-5** `scripts/assert-staging-bindings.mts` extended to fail PR if `wrangler.jsonc` production `vars` ever carries any `E2E*` key. **ISC-6** new `smoke-production` job in `ci.yml` after `deploy-production`. Read-only per **D-5 + D-5.1** (locked during execute): 3 reliable signals against prod apex — entry page renders Sanity content, `reading.stripePaymentLink` matches `buy.stripe.com` regex via Sanity CDN, `/api/booking` returns 4xx for dummy Turnstile token. Defers full POST-with-paymentUrl-shape until authorized Turnstile bypass exists. **ISC-7** randomize `STRIPE_WEBHOOK_SECRET` per playwright run. **Worker-config re-eval gotcha:** Playwright workers re-import `playwright.config.ts` on spawn — module-scope `randomUUID()` produces a DIFFERENT secret per worker. Fix: carrier pattern via `process.env.STRIPE_WEBHOOK_SECRET ?? randomUUID()` — runner generates once, workers inherit + reuse the value across config re-evaluations. **ISC-8** generalize `listenRoundtripCleanup` → `sandboxResidueCleanup({ emailPrefix })`. Migrated listen + gift + stripe roundtrip call-sites; each spec's `beforeAll` calls with its identifying prefix. Plus-addressing enforcement (`endsWith("+")`). **DRY:** new `.github/actions/playwright-setup/` composite consolidates 3 copies of the Playwright install+cache block (`e2e.yml` + `e2e-sandbox.yml` + `smoke-production`). **CI-debug arc — 4 iterations to green:** (a) **Bot Fight Mode** blocked the GH runner before CF Access headers could evaluate — Free plan can't skip BFM via WAF rule. Disabled BFM zone-wide; acceptable trade-off given Turnstile + honeypot + WAF rate-limit rule remain. (b) **Stripe Checkout now collects phone number** on the staging Payment Link; `fillStripeCheckout` extended to fill via `input[autocomplete="tel"]`. (c) **Stripe Pay button "not stable"** — `SubmitButton--complete` class confirms enabled, but Playwright stability check trips on the CSS transition; fix is `click({ force: true })`. (d) **Two more env vars missing from initial workflow:** `SANITY_READ_TOKEN` (post-Stripe Sanity-read helpers — reused `SANITY_WRITE_TOKEN`'s value since Sanity Editor tokens have read access) and `CRON_SECRET` (listen-roundtrip's `forceD1Mirror` hits the day-7-deliver cron endpoint with Bearer auth). **Final state:** 4/4 sandbox specs green in 2.9 min on CI run 26155214119 against `staging.withjosephine.com`. Mock e2e 47/47 in 1.4 min. vitest 1608/1608. lint + tsc + build clean. PRD `MEMORY/WORK/20260520-070000_launch-readiness-e2e-epic/PRD.md` 8/24 ISC complete. Sub-PR B (P0 spec coverage, ~5.5h) + Sub-PR C (cleanup tail, ~2h) remain | ✅ shipped 2026-05-20 to `release/v1.2.0` (#153, squash `54d6958`) |
| **PR `feat/listen-roundtrip-reenable`** | Re-enables all three sandbox specs against staging (`listen-roundtrip`, `stripe-roundtrip`, `gift-roundtrip`) — substitute for hold-gate item #3 (Sub-PR #4a 1-week bake). All 4 tests now pass end-to-end against `staging.withjosephine.com` in 1.8 min (booking POST + Stripe checkout + webhook + Sanity asset upload + D1 mirror + magic-link verify + /listen page + audio Range fetch + listenedAt mirror; gift purchase scheduled + self_send + recipient redeem). **Real root cause** (not the BACKLOG speculation about `/my-readings`): IntakeForm's Turnstile widget is configured `execution: "execute"` + `appearance: "interaction-only"`, which defers iframe injection until the form's submit handler calls `.execute()`. In headless Chromium against Cloudflare's always-pass test sitekey, that deferred iframe injection silently no-ops — `requestFreshTurnstileToken()` hangs forever, `/api/booking` never POSTs, the test times out at `fillStripeCheckout`'s `waitForURL`. GiftForm uses `size: "invisible"` (auto-execute) and dodges the bug entirely. **Fix:** new `tests/e2e/helpers/turnstileStub.ts` stubs `window.turnstile` via `page.addInitScript` BEFORE any page script loads — research-validated pattern from Cloudflare's E2E guidance + marsidev/react-turnstile community. The server-side `verifyTurnstileToken` still runs against Cloudflare with staging's always-pass test secret (`1x0000000000000000000000000000000AA`), which accepts any token value — so the dummy token (`XXXX.DUMMY.TOKEN.XXXX`) lands fine. **Secondary fixes uncovered by real run:** (a) `listen-roundtrip` engineering seam `issueMagicLink` returns a verify URL with only `?token=`; real magic-link emails include `&next=/listen/<id>`. Spec now appends `next` so `safeNext()` resolves to the delivered surface instead of `/my-readings` default. (b) `gift-roundtrip` recipient-redeem step had stale `#field-cooling-off-consent` check — gift-redeem mode drops cooling-off ack per PR #118 (purchaser waives at purchase, recipient acks art9 only). One-line surgical remove. **Cleanup helper** `tests/e2e/helpers/listenRoundtripCleanup.ts` — preflight wipe of `listen-roundtrip+%` rows from staging D1 (5 tables in dependents-before-referenced order) via `pnpm exec wrangler d1 execute withjosephine-bookings-staging --remote --command …` (30s timeout, exit propagation) AND from staging Sanity submission docs (`email match "listen-roundtrip+*"`, transaction delete). Hard-gated on `NEXT_PUBLIC_SANITY_DATASET === "staging"`. `beforeAll` invokes cleanup once per spec run. `beforeEach` calls `context.clearCookies()` + `stubTurnstile(page)`. **Files touched:** new `tests/e2e/helpers/turnstileStub.ts` (+95 LoC), new `tests/e2e/helpers/listenRoundtripCleanup.ts` (+95 LoC), `listen-roundtrip.spec.ts` (fixme removed + beforeAll + beforeEach + next= fix), `stripe-roundtrip.spec.ts` (beforeEach stub), `gift-roundtrip.spec.ts` (beforeEach stub + cooling-off removal), BACKLOG entry deleted, SESSION_BOOT hold-gate item #3 rewritten with manual-run command. **Run:** `E2E_SANDBOX=1 source .env.staging && pnpm exec playwright test`. Verified locally 2026-05-20: 4 passed 1.8 min. Mock-mode 47 tests still excluded per `testIgnore: SANDBOX_SPECS_PATTERN`. vitest 1608/1608, lint + tsc clean. **Adjacent ops shipped in same session (no code change):** WAF rate-limit rule `rl-josephine-api-write-paths` deployed on the `withjosephine.com` Cloudflare zone — single rule covering `/api/booking`, `/api/contact`, `/api/booking/upload-url` via OR-joined `eq` expression. Threshold 5 requests / 10 seconds / IP characteristic, Block action with 10s duration. Single-rule consolidation forced by CF Free zone plan (1 rate-limit rule cap; Pro=2, Business=5, Enterprise=100); 3-rule per-path version stays as the post-Business-upgrade target. Verified 2026-05-20 from iOS Shortcuts on cellular hitting `/api/contact` 8 times — request 8 returned Cloudflare error 1015 ("you are being rate limited"). Closes SESSION_BOOT Max-action #5 + BACKLOG S-3 | ✅ shipped 2026-05-20 to `release/v1.2.0` (#152, squash `86549e9`); CI run 26147183329 ✅ 3m6s |
| **PR #150** | v1.1.x Phase 3 **Scope 3 polish bundle** — closes the scheduling rebuild. Six commits on `feat/v1.1.x-phase3-polish` off `release/v1.1.0@33135fc`. **ConfirmArmedButton extraction** (`src/app/my-gifts/ConfirmArmedButton.tsx`, +95 LoC) collapses the triplicated arm-state + ARM_RESET_MS timeout + onConfirm-with-disarm-on-error skeleton out of FlipToSelfSendControl + SendNowControl + CancelScheduledControl. GiftCardActions.tsx -75 LoC (614 → 539). 8 new tests pin the disarm-on-any-error contract (R-1 anchor) including http_409 + http_401 paths + sessionExpired override surfacing + destructive variant rendering. **P-6 WCAG 2.2.1 reduced-motion** — new `src/lib/a11y/useReducedMotion.ts` via `useSyncExternalStore` over `window.matchMedia("(prefers-reduced-motion: reduce)")` with deterministic `false` SSR snapshot. `ARM_RESET_MS_REDUCED_MOTION = 15000` wired into ConfirmArmedButton — OS-level toggle IS the user's extension switch per WCAG Timing Adjustable Level A. Fake-timer tests verify the 5s vs 15s disarm window. **giftMutationGate helper** (`src/app/api/gifts/[id]/_lib/giftMutationGate.ts`) extracts the preflight-409 ladder across all 4 gift-mutation routes (send-now, cancel-scheduled, cancel-auto-send, flip-to-scheduled). Signature: `giftMutationGate(submission, { requireMethod: "scheduled" | "selfSend", checkSentNow?, checkAlarmFired? })`. Exports `GIFT_GATE_409` constants for 6 distinguishable reason strings (notScheduled / notSelfSend / cancelled / claimed / alarmAlreadyFired / sentNow). cancel-auto-send opts out of `checkSentNow`; flip-to-scheduled opts out of both with `requireMethod: "selfSend"`. **Atomic WHERE-guarded UPDATE in each route remains the real race-stop** — this gate is for audit clarity + saves a SQL roundtrip on already-closed rows. **Note on 409 reason strings:** scope-3 standardizes the audit strings as exported constants. Pre-PR each route used its own free-text mix (`"Not scheduled"`, `"Already sent"`, `"Closed"`); post-PR they're now `GIFT_GATE_409.*` codes. No tests asserted exact strings before adoption (grep confirmed) — log scrapers should pivot to the constant names. **7-spec interceptStripeCheckout sweep** — migrates remaining hand-rolled `page.route("https://buy.stripe.com/**")` blocks to the helper landed in PR #148: stripe-local.spec.ts, gift-local.spec.ts (3 sites — PRD/Explore count was off-by-one), gift-redeem-local.spec.ts, gift-redeem-self-send-local.spec.ts, gift-redeem-consent-shape-local.spec.ts, intake-page.spec.ts (assertion reshaped from raw-URL regex to `intercept.captured.submissionId` — semantically equivalent, submissionId IS the `client_reference_id` value). Skip: `gift-flow.spec.ts:47` (regex + sync route.fulfill + UI-only assertion, incompatible with async closure-capture helper). **Narration strip** — `gift-flip-to-scheduled-tz.spec.ts` per `feedback_comments_over_logged.md` (17 blocks + citation tags ISC-18b/c, ISC-18g removed). **Refund audit pass #2** — Explore-agent surfaced 2 candidate drift sites; both verified already compliant on `release/v1.1.0` (PR-B #144 had shipped the rewrites; Explore was scanning `main`). Grep across `src/**`, `studio/schemas/**`, `scripts/**` confirms zero non-policy-aligned strings. **`docs/OPERATIONS.md` un-cancel procedure** — new section with D1 SQL to clear `gift_cancelled_at` flags, explicit "DO alarm not re-armed by SQL" warning (DO storage is isolated from D1), and 2 recovery paths (purchaser self-service via flip-to-scheduled, or send-now). **/simplify scope-3 pass** — 4 findings applied: shared `actionErrorLabel.ts` module kills `mapError` dup across ConfirmArmedButton + EditRecipientControl + FlipToScheduledControl + ResendLinkControl (reuse + quality double-flag); `variant` prop simplified from `"default" \| "destructive"` to `"primary" \| "destructive"` matching Button's actual variant names (drop translator); 3-line refactor-history JSDoc trimmed; `useReducedMotion` matchMedia-per-call documented as testability trade-off; `useRealTimers` cleanup hoisted to outer describe. **Defensive reviewers (background Agents):** `/review` 0 BLOCKERs + 2 MED audit-string-drift findings (deliberate per gate JSDoc — surfaced in CHANGELOG above) + 4 LOWs filed as smells; `/security-review` 0 CRITICAL/HIGH + 1 MED on rate-limit absence already tracked as BACKLOG S-3 Max-action (WAF rules). Auth-then-gate-then-mutate ladder verified; SameSite=Lax + `__Host-` + HttpOnly cookies block CSRF structurally; `Idempotency-Key` intact in send-now; TOCTOU window closed by atomic UPDATE; no PII in logs. Vitest 1608/1608 (+27 from 1581 post-#148 baseline); lint + tsc + build clean. CI run 26118274895 ✅ 3m20s, E2E run 26118276618 ✅ 3m38s, both via workflow_dispatch. **After this PR merges, Phase 3 scheduling rebuild is fully shipped.** Hold-gate item 4 (release/v1.1.0 → main) is the gating step to production | ✅ shipped 2026-05-20 to `release/v1.1.0` (#150, squash `4edad74`) |
| **PR #149** | v1.1.x **email-day-2 cron dispatch disabled** per Max directive 2026-05-20. Removes `/api/cron/email-day-2` from `CRON_DISPATCH` in `src/lib/cron-routes.ts` while keeping the cadence trigger `"0 */6 * * *"` in place (reconcile, reconcile-mirror, email-day-7-deliver still fire on it). Route handler stays in place — only the cron-driven dispatch is suppressed. CRON_SECRET-bearer-curl manual-trigger fallback still works if Josephine needs to fire day-2 emails by hand for a specific cohort. Re-enable by restoring the single dispatch line + the wrangler.jsonc cadence-comment edit. Standalone branch off `release/v1.1.0@33135fc`; does not depend on or conflict with the scope-3 polish branch. CI run 26118179993 ✅ 3m3s, E2E run 26118182222 ✅ 4m0s | ✅ shipped 2026-05-20 to `release/v1.1.0` (#149, squash `9f23d82`) |
| **PR #148** | v1.1.x Phase 3 **PR-Bundle** — PR-Lift e2e helpers + D-11 cancel-scheduled. Three originally-planned sub-PRs (PR-Lift / PR-D / PR-E) collapsed into one bundle on `feat/v1.1.x-phase3-pr-bundle` per `feedback_no_more_splitting_and_deferring.md` (default to FINISH not slice). Scope 3 (P-6 reduced-motion + ConfirmArmedButton extraction + giftMutationGate + sweep + /simplify #3 + refund audit) deferred to next session as a separate branch. **PR-Lift (test-infra refactor, no production change):** new `tests/e2e/helpers/auth.ts` exporting `signInViaMagicLink(page, {email, next})`; extended `tests/e2e/helpers/stripeCheckout.ts` with `interceptStripeCheckout(page, {readingSlug, gift?})` returning a Promise-based `captured` + `unroute()` disposer (protects D-11's beforeEach pattern from O(n) handler leak). Migrated `gift-send-now`, `my-gifts-local`, `gift-flip-to-scheduled-tz` (2 tests) to the helpers. Net diff -21 lines, same Playwright test count (4 in 3 files). **D-11 cancel-scheduled:** new `POST /api/gifts/[id]/cancel-scheduled` — auth via `authorizeGiftPurchaser` → preflight 409s (not-scheduled, already-fired, already-sent-now, already-cancelled/claimed) → `cancelGiftAlarm()` FIRST (race-safe, idempotent) → WHERE-guarded UPDATE setting `gift_cancelled_at` / `gift_cancelled_by` / `gift_cancelled_reason = 'purchaser-request'`. rowcount 0 → 409. `applyGiftCancelScheduled` repo helper + `submissions.ts` wrapper; `mirrorSubmissionPatch` patch type expanded with the 3 cancel fields. `CancelScheduledControl` in `GiftCardActions.tsx` reuses the 5s confirm-armed pattern; new **`destructive` Button variant** (j-rose `#BF9B8B` armed state — Quiet Archivist forbids alarm-red) replaces inline `bg-j-rose` className override. Armed copy locked under wholesale-no-refunds policy: "Tap again to confirm — your reading will not be sent. This purchase is non-refundable." 4 new `myGiftsPage` Sanity copy fields + idempotent seed script `scripts/migrate-my-gifts-cancel-scheduled-copy-2026-05-19.ts` (uses existing `scripts/_lib/seedSingleton.mts` helper) — pending Max-action to run on staging then production. **DO defense-in-depth:** `giftClaimDispatch.ts:48` already short-circuits on `giftCancelledAt`, no code change needed. **Tests:** 9-case unit-test suite on the route (401×2, 404, 409×5 across all preflight branches + race-lost, ordering assertion). New e2e spec `gift-cancel-scheduled.spec.ts` uses the PR-Lift helpers — first new caller of the bundled abstraction. Asserts the armed CTA surfaces "non-refundable" (load-bearing for EU CRD waiver). **`makeScheduledGift` fixture factory** under `src/lib/booking/__fixtures__/` replaces 23-field inline duplication; sets the pattern for the ~12 other route tests that copy the same shape. **Typed `GIFT_CANCELLED_REASON` constant** in `booking/constants.ts` — `purchaserRequest`/`fraud`/`refund` union threaded through route + repo + submissions wrapper. **/simplify pass #1:** dropped hand-rolled `escapeForRegex` (waitForURL glob); converted `interceptStripeCheckout` from closure-getters to Promise-based `captured` (kills `string\|null` + `!` non-null at every call site) + added `unroute()` disposer. **/simplify pass #2:** typed-reason constant, destructive Button variant, fixture factory, seedSingletonFields refactor (script went from hand-rolled to 1-helper-call), e2e schema-description pointer comment. **CI follow-up:** `playwright.config.ts` `timezoneId: "America/Los_Angeles"` pin — `gift-flip-to-scheduled-tz` spec was authored in PR-A (#142) and never previously ran in CI (e2e workflow only fires on PRs targeting main with IntakeForm changes); Chromium reports `"UTC"` on CI runners which fails the `^[A-Za-z]+\/` IANA-shape assertion. Pinning to LA matches dev-machine behavior. **CI:** workflow_dispatch run 26113216922 green; e2e workflow_dispatch 26114308155 green post-TZ-fix. Pre-existing vitest concurrency flake (2-4 timeouts at 5000ms in `src/app/page.test.ts` / `my-readings/page.test.tsx` / `gift-redeem/route.test.ts` under full-suite parallelism, pass in isolation, 0 in this diff) noted but not fixed in this PR | ✅ shipped 2026-05-19 to `release/v1.1.0` (#148, squash `4bff524`) |
| **PR #147** | v1.1.x Phase 3 **PR-C-ii** — D-10 send-now route + UI + Resend idempotency. New `POST /api/gifts/[id]/send-now` lets purchasers fire the recipient claim email immediately on `scheduled` gifts. Race-safe ordering: auth → 5 preflight 409s (not-scheduled, alarm-already-fired, cancelled/claimed, prior send-now, missing recipient) → `Promise.all([issueGiftClaimToken, cancelGiftAlarm])` → WHERE-guarded UPDATE on the 5 audit columns landed by migration 0012 → `sendGiftClaimEmail({ variant: "first_send", idempotencyKey: "gift:{id}:claim" })` → `appendEmailFired`. Dispatcher defense-in-depth: new `reason: "sent_now"` stop branch in `giftClaimDispatch` short-circuits any alarm that enters mid-flight. `applyGiftSendNow` repo helper + `submissions.ts` wrapper + Sanity-mirror patch wire the 3 nullable audit columns (`giftClaimSentNowAt`, `giftClaimSentNowActor`, `giftClaimPriorAlarmAt`) through `Row` → `SubmissionRecord` → mirror. `sendOrSkip` + `sendGiftClaimEmail` accept optional `idempotencyKey` and forward to the Resend SDK's second-arg options (SDK emits `Idempotency-Key` HTTP header). `SendNowControl` component on `/my-gifts` reuses the 5s confirm-armed pattern from `FlipToSelfSendControl`; `http_401` (session expired during arm window) maps to `sendNowSessionExpiredError` copy instead of generic. 4 new Sanity-editable copy fields on `myGiftsPage` (sendNowCtaLabel / sendNowConfirmCtaLabel / sendNowSendingLabel / sendNowSessionExpiredError). 10-case unit-test suite (401×2, 404, 409×4 including double-click idempotency, race-lost, 502, idempotency-key assertion, ordering). New e2e spec `gift-send-now.spec.ts` walks book → pay → sign-in → arm → confirm → assert. 22 fixture files backfilled with 3 new `null` SubmissionRecord fields. **/simplify outcomes:** distinguished 409 error strings (`"Email already sent by alarm"` vs `"Already sent via send-now"`) for audit clarity, parallelized token + cancelAlarm (no data dep, race-safety preserved), mirror columns typed `string \| null` to match runtime. Skipped scope-creep refactors (shared `useConfirmArmed` hook, `assertGiftScheduledAndOpen` helper). **Comment-rot follow-up commit** (`7ce70c8`) stripped 44 lines of narrating comments after Max flagged a 4th-flag violation of `feedback_comments_over_logged.md`; memory updated with sharper "comment is a symptom — cure is a named helper, not just delete" framing. Vitest 1576 / 1576 (+12 vs PR-C-i); tsc + lint + build clean. No D1 migration (schema landed in PR-C-i 0012) | ✅ shipped 2026-05-19 to `release/v1.1.0` (#147, squash `9dbdd86`) |
| **PR #145** | v1.1.x Phase 3 **PR-C-i** — D1 migration 0012 (gift scheduling audit columns + send-now idempotency index). Single migration prepares schema for D-10 (send-now) AND D-11 (cancel-scheduled) audit columns so both sub-PRs can deploy without further schema work: `gift_claim_sent_now_at`, `gift_claim_sent_now_actor`, `gift_claim_prior_alarm_at`, `gift_cancelled_by`, `gift_cancelled_reason`. UNIQUE partial index `idx_gift_sent_now_once` on `id WHERE gift_claim_sent_now_at IS NOT NULL` — belt-and-suspenders so a double-click race can never set sent_now_at twice. Applied to staging D1 via `pnpm migrate:apply:staging` post-merge (7 commands executed); schema verified via `pragma_table_info` + `sqlite_master`. Production D1 apply pending (Max-action filed in SESSION_BOOT). Migration-only split was chosen over the originally-planned PR-C bundle so D-10's route + UI gets full attention in a fresh session | ✅ shipped 2026-05-19 to `release/v1.1.0` (#145, squash `1ca2aff`) |
| **PR #144** | v1.1.x Phase 3 **PR-B** — D-12 recipient-email pre-fill + P-4b refund-policy wholesale rewrite. **D-12:** `IntakeForm` accepts `prefilledEmail?: string | null`; when `mode === "redeem"` and value is truthy, seeds the email field's default via new shared `normalizeEmailForm()` helper (NFKC + trim + lower, no `+suffix` strip) and marks the field readOnly via `readOnlyFieldKeys` threaded through `RenderContext`. NULL self-send claims keep the field editable. `Input` grew a `readOnly` prop; `EMAIL_FIELD_KEY` const replaces magic-string. `gift/intake/page.tsx` renders new `RecipientEmailEscapeHatch` component with two-branch copy (prefilled vs NULL self-send), each linking to `/contact`. `gift-redeem/route.ts` now NFKC-normalizes BOTH submitted email AND stored `recipient_email` at the strict 422 mismatch gate (Rook hard-stop preserved across Unicode variants). **P-4b — wholesale-non-refundable policy locked 2026-05-19:** `/refund-policy` page fallback fully rewritten (drops 14-day window, intake-form-as-gate, 5–10 business-day timing; keeps duplicate-charges + delivery-issues + dissatisfaction + statutory rights; new opening frames the intake-checkbox waiver under EU CRD Art. 16(m)). `/terms` refund clause aligned. `seed-faqs.ts` "Can I get a refund?" rewritten. `seed-legal-pages.ts` refund-policy body cleared (page fallback is now load-bearing). `emailGiftPurchaseConfirmation.ts` `refundLine` initialValue aligned. **NEW** `scripts/migrate-refund-policy-no-refunds-2026-05-19.ts` — idempotent Sanity migration; clears legacyPage body when matches seed stub or legacy v1 14-day-window markers; logs for manual review otherwise. Ran against staging (cleared) + production (cleared) + idempotency verified by no-op re-run. Vitest 1564 / 1564; lint + typecheck + build clean; gift-related e2e 20 / 20 | ✅ shipped 2026-05-19 to `release/v1.1.0` (#144, squash `636216b`) |
| **PR #143** | v1.1.x **PR-A CI follow-up** — TypeScript narrowing + `ws@8.20.1` override. Two CI gates failed on the PR #142 post-merge run: (1) TS2769 in new `gift-flip-to-scheduled-tz.spec.ts:141:28` (`expect(...).toMatch` doesn't flow through TS narrowing — local pre-commit typecheck was stale, ran before the second test was added that duplicated the same shape). Fix: narrow `sent.giftSendAt` to a typed local (`flipUtcIso` / `editUtcIso`) immediately after the `toMatch` assertion. (2) OSV GHSA-58qx-3vcg-4xpx (medium, 4.4) on `ws` 8.18 / 8.20 via dev-only transitive paths (happy-dom, storybook, vitest, wrangler→miniflare). Fix: `pnpm.overrides: { "ws@<8.20.1": "^8.20.1" }` — upstream fix exists, pinning preferable to filtering | ✅ shipped 2026-05-19 to `release/v1.1.0` (#143, squash `cc8c8a9`) |
| **PR #142** | v1.1.x Phase 3 **PR-A** — D-8 gift status pill + D-9 datepicker TZ hardening. **D-8:** new `GiftStatusPill` component for 6 `GiftStatus.kind` variants (lucide icons, brand-tinted via semantic CSS tokens). Status visuals are theme-owned: `--j-status-{kind}-{bg\|fg}` in `tokens.css` keyed verbatim to `GIFT_STATUS_KIND` values; pill carries only `Icon` refs + base classes — palette tweaks no longer require a component diff. New `GIFT_STATUS_KIND` const in `lib/booking/constants.ts`; type union + `giftStatusFor` + `GiftCardActions` + tests all reference it. `/my-gifts` response gets `Cache-Control: private, no-store` via middleware (P-1 — prevents cross-purchaser cached snapshots). **D-9:** `@date-fns/tz` pinned as direct dep. New `lib/booking/scheduling/` namespace: `bounds.ts` (15-min floor + `purchasedAt`-anchored 365-day ceiling; shared validator helper for flip-to-scheduled AND edit-recipient routes), `timezone.ts` (IANA detection + `datetime-local → UTC` via `TZDateMini` + iOS-Safari-pre-17 generic-offset rejection), `useEffectiveTimeZone` hook (`useSyncExternalStore` for hydration safety). `validateGiftRecipientFields` refactored to `{now, purchasedAt}` context shape. Both submit paths in `GiftCardActions` capture browser TZ + submit deterministic UTC ISO; `TimezoneFallbackPicker` renders when browser can't expose IANA zone. `TimezonePreview` echoes IANA zone via new `{tz}` template token. **E2E:** new `gift-flip-to-scheduled-tz.spec.ts` (2 tests: flip + edit-recipient TZ-aware submit paths with UTC round-trip assertions). Fixed pre-existing `gift-local I-1` self_send-subheading failure via E2E-gated `?submission=` URL fallback in thank-you page (production unaffected). `playwright.config workers: 1` to eliminate parallel D1 state contamination. Vitest 1564 / 1564 (+24 new); Playwright (mock) 45 / 45 | ✅ shipped 2026-05-19 to `release/v1.1.0` (#142, squash `e7c1f86`) |
| **PR #140** | v1.1.1 follow-up 3a + **alarm-scheduling root-cause fix**. I-13 (recipient name + email on `/my-gifts`), I-14 (self-send indicator badge in edit-recipient drawer), I-15 (readable date+time on scheduled gift purchase confirmation email + /my-gifts label). Silent-failure repair: `scheduleGiftAlarm` failure now returns 502 (was silently OK pre-fix), surfaced via diagnosis of stuck staging gift `81ea25b7`. **Real alarm fix: `giftClaimSchedulerClient.ts` migrated from `globalThis.GIFT_CLAIM_SCHEDULER` (returns undefined in OpenNext route runtime) to `getCloudflareContext({ async: true })` matching the stripe webhook pattern.** Three test files (flip-to-scheduled, edit-recipient, cancel-auto-send) migrated to mock `@opennextjs/cloudflare`; reproducer test added that asserts 502 when binding absent on env. Vitest 1533 → 1535 | ✅ shipped 2026-05-18 to `release/v1.1.0` (#140, squash `c513e40`) |
| **PR #139** | v1.1.1 follow-up 2 — null-handling on `migrate-thank-you-gift-recipient-name-2026-05-18.ts` (first run with strict-equality predicate didn't match `field=null`; second run after relaxing did) + hardcoded fallback for `giftRecipientHeading` in `/thank-you/[readingId]/page.tsx` | ✅ shipped 2026-05-18 to `release/v1.1.0` (#139, squash `93ca63d`) |
| **PR #138** | v1.1.1 follow-up 1 — recipient-side improvements. I-9 (recipient name on gift thank-you), I-10 (sendRecipientIntakeReceived email after intake submit + new `emailRecipientIntakeReceived` singleton), I-12 (editable recipient + flip-to-scheduled action on self_send_ready gifts). I-11 closed as not-a-bug (test-setup confusion — Roland's gift was purchased by `+marco`, viewing `/my-gifts` as `maxgertzen@gmail.com`). Seed + 1 migration ran on staging | ✅ shipped 2026-05-18 to `release/v1.1.0` (#138, squash `6ed06ff`) |
| **PR #137** | v1.1.1 hot-fix bundle — 8 issues from staging smoke. I-1 (gift purchaser thank-you self_send vs scheduled copy branching + 3 new thankYouPage fields), I-2 (per-reading Art. 9 consent label — Council ran with GDPR Art. 9(2)(a)/WP259/EDPB 05-2020 citations), I-3 (hide price + drop duplicate `{deliveryDays}` paragraph on recipient thank-you), I-4 (SubmitOverlay loader size md → lg), I-5 (`/gift/already-submitted` page + redirect), I-6 (nav mobile-menu breakpoint md → lg), I-7 (Akasha hero mobile clamp 200/55vw/300), I-8 (workflow_dispatch on ci.yml). 2 Sanity migrations queued for staging-first then production. 2 new e2e regression specs. Vitest 1518/1518 | ✅ shipped 2026-05-18 to `release/v1.1.0` (#137, squash `eb459fd`) |
| **PR #132** | Docs — manual smoke test + launch plan retirement + cascade-delete runbook + BACKLOG audit. **`docs/MANUAL_SMOKE_TEST.md`**: 9-journey audience-agnostic staging walkthrough (self-purchase, gift self-send, recipient claim, gift scheduled, manage scheduled gift edit/reschedule/cancel/regenerate, listen-page magic-link, GDPR Art. 20 export, contact form, Studio content edit). Each journey lists what-to-do / expect (✅) / watch-for (❌). Tester reports pass/fail + screenshots + time window; **maintainer runs cleanup** (D1 + Sanity + R2 wipe via `scripts/cleanup-test-submissions.mts` + CF dashboard — tester doesn't need any of those surfaces). Plus-addressing on the tester's personal Gmail (`yourname+suffixN@gmail.com`) so CF Email Routing isn't a dependency. **`docs/runbooks/CASCADE_DELETE_SMOKE.md`**: GDPR Art. 17 cascade-delete smoke extracted into its own runbook — irreversible, maintainer-only, was the only LAUNCH_SMOKE_TEST_PLAN section without coverage elsewhere. **`docs/LAUNCH_SMOKE_TEST_PLAN.md` deleted**: framed around the retired `release/v1.0.0 → main` workflow; Stage A's privacy-migration check already ran; Stages B-1..B-3, B-5, B-6, C-1 covered by MANUAL_SMOKE_TEST + SANITY_BACKUP_RUNBOOK; C-4 apex-lockdown + C-3 prod Stripe round-trip live in BACKLOG apex-unpark entry. **BACKLOG trim 705 → 611 lines (-13%):** Sub-PR #4 family deleted (4a shipped via PR #74; 4b/4c folded into master plan per CLAUDE.md); B-1 sha256Hex + B-2 normalizeEmail + B-5 iter-2 markers verified shipped and deleted; PR-F1 entry trimmed (dropped design-decision memo, kept 3 pending triggers); Code-quality preamble deleted (11 items shipped via PRs #76–#79); Phase 5 LOW L-1 collapsed to one-line regression watch; Reading-prices "Code surfaces fixed" paragraph dropped; Clarity "Wiring shipped" paragraph dropped; Phase 4 + Phase 1 LAUNCH-BLOCKING framings reframed (Phase 1 shipped via PR #130); duplicate `## Security` heading collapsed. **README**: documentation map updated to point at MANUAL_SMOKE_TEST + CASCADE_DELETE_SMOKE | ✅ shipped 2026-05-17 to `main` (#132) |
| **PR #131** | Phase 6 cleanup — restore time-of-birth toggle + comments/magic-strings/CI sweep. **Critical fix:** Bundle F production migration (`migrate-strip-consent-fields-2026-05.ts`) ran against production Sanity and deleted 2 docs as designed — but `formField-timeOfBirthUnknown` was a UI-state holder misclassified as `type: "consent"` (not a legal acknowledgment). Deletion silently broke the "I don't know my birth time" toggle on staging AND production. **Fix:** new `checkbox` field type on `SanityFormFieldType` union + Studio schema; filter branches in submissionSchema, buildPageSchema, useIntakeSchema, RenderedSection, ReviewSummary + audit-trail filters in /api/booking + /api/booking/gift-redeem extended; new `scripts/migrate-restore-time-unknown-checkbox-2026-05.ts` recreates the doc + attaches `formSection-birth` ref (ran against staging + production 2026-05-17); new Playwright spec `tests/e2e/specs/intake-time-unknown-toggle.spec.ts` exercises the toggle E2E. **Bundle Q (consent dead-code):** `"consent"` removed from `SanityFormFieldType` union; `case "consent":` block in `buildFieldSchema` deleted; filter branches narrowed across 7 sites; Studio deprecation validator removed. **Bundle C (comments sweep):** 4 parallel agents over `src/lib/`, `src/components/`, `src/app/`, `studio/`. 315 files scanned, 24 modified, ~130 LoC deleted. Strong preservation of load-bearing WHY (Stripe sig verification, GDPR Art. 6/9/20, atomic-flip-first race, magic-link enumeration defense, R2/workerd quirks, Clarity CSP, accessibility role="alert"). Two persona-name `description:` strings (Alice/Bob) in submission.ts fixed → purchaser/recipient per memory rule. **Bundle M (magic-strings sweep, 4 of 6 domains):** new `src/lib/http/headers.ts` (ADMIN_TOKEN, DO_SECRET, CF_CRON, CF_CONNECTING_IP, X_FORWARDED_FOR, USER_AGENT, AUTHORIZATION, BEARER_PREFIX); new `src/lib/audit/eventTypes.ts` (`AUDIT_EVENT_TYPE` const + derived union; 19 callsites migrated); new `src/lib/http/routes.ts` (8 API routes + BOOKING_PAGE_ROUTES — 13 consumers migrated); `SUBMISSION_STATUS` const-object reshape on submissions.ts. M-3 (Sanity doc types) skipped — literals are GROQ syntax; M-6 (analytics events) skipped — `ClientEventMap` already enforces names via TS keys. **Bundle T (CI):** Node 22 → 24 on `actions/setup` + `e2e.yml`; wrangler compat_date NOT bumped (independent risk profile); `outputFileTracingExcludes` narrowing dropped (load-bearing comment in next.config.ts cites the AsyncLocalStorage duplication bug). **/simplify pass:** new `scripts/_lib/loadDotenv.mts` helper (consumed by the restore migration); `USER_AGENT_HEADER` added; DatePicker setState-in-effect comment tightened to WHY-only. **CI cleanup:** dropped `release/v1.0.0` from push triggers, PR triggers, deploy-staging if-condition, D-4 grandfathering check — main is the only branch now; staging + production both deploy from main pushes (deploy-production needs deploy-staging). 8 commits, 64 files / +340 / -313. Vitest 161/1446 at every gate. Production toggle UI verified restored via staging deploy probe | ✅ shipped 2026-05-17 to `main` (#131, squash `091168d`) |
| **PR #130** | Release branch → main — Phase 1–5 bundle squash-merge. Carries everything from `release/v1.0.0` to production: intake-architecture-repair arc (Phases 0–5: e2e foundation, compliance decoupling on booking + gift, IntakeForm god-component refactor, per-field error surfacing, Becky bug fixes), Phase 5 gifting feature (Sessions 1a–7), listen redesign + magic-link auth, Sanity Studio dashboard restructure, dataset backup cron + webhook mirror, GDPR cascade delete (Art. 17), Art. 20 export. 66+ commits across 3 months of work. Production D1 + Sanity migrations queued for the merge (0004–0009, gift-related migration scripts). After this merge `release/v1.0.0` was retired (CI yaml cleanup in PR #131) | ✅ shipped 2026-05-17 to `main` (#130, squash `c58273e`) |
| **PR-A** | Schema seed + v6 form migration | ✅ shipped |
| **PR-B** | IntakeForm refactor — paginated render, per-page Zod, PageNav, §4 enforce-3, §3 birth-time toggle, helperPosition, PlaceAutocomplete | ✅ shipped |
| **PR-C** | v6 entry page (vellum-letter folio + drop-cap CTA) | ✅ shipped |
| **PR-D** | localStorage save/resume, reading-swap preservation, SwapToast, SubmitOverlay | ✅ shipped |
| **PR-D-2.0** | Floating labels + soft <18 DOB warning | ✅ shipped |
| **PR-E** | SPEC §13 verbatim email helpers · `emailsFired` audit array · 3 crons (day-2 / day-7 alert / day-7 deliver) · `/listen/[token]` HMAC-gated page | ✅ shipped |
| **PR #34–#41** | Initial deploy infra (Workers + D1 + R2 + Turnstile + Resend wiring) | ✅ shipped |
| **PR #42–#44** | Apex 500 debug + final fix (drop `outputFileTracingExcludes`) | ✅ shipped |
| **PR #45–#47** | Same-day post-fix follow-ons (Turnstile in CI; image preview + draftMode gate; CSP `blob:` for img-src) | ✅ shipped |
| **PR #48** | Logo refresh — high-res Hero + responsive mobile/desktop horizontal | ✅ shipped |
| **PR #49** | D1 client REST → workerd binding refactor + R2 preview-bucket cleanup | ✅ shipped |
| **PR #54** | Forward `NEXT_PUBLIC_UNDER_CONSTRUCTION` to CI build env | ✅ shipped |
| **PR #55** | Middleware-level apex lockdown (closed the `/book/*` + `/api/*` leak) | ✅ shipped |
| **PR #56** | Docs — staging-tier scope + Phase-2 fully-booked toggle | ✅ shipped |
| **PR #57 (PR-F)** | Mixpanel client + 13 SPEC §15 events + geo-conditional consent banner + Sanity-editable banner copy + non-prod opt-in gate + dead BookingForm component purge + privacy policy patch | ✅ shipped 2026-05-02 |
| **PR #58–#65 (PR-S series)** | Dev/staging/prod environment separation: D1 migration runner, Sanity staging seed, wrangler env split, staging Custom Domain, RESEND_DRY_RUN gate, `/api/sanity-sync` auto-mirror | ✅ shipped 2026-05-06 |
| **PR #66** | Sanity `priceDisplay`↔`price` validation rule + prebuild bake of reading prices to `src/data/readings.generated.ts` | ✅ shipped 2026-05-06 |
| **PR #67 (PR-F2)** | Server-side Mixpanel events (payment_success, payment_expired, email_sent ×6 sub-types, delivery_listened) via `Sentry.withSentry`-style fetch helper + `submission_id` identity threading | ✅ shipped 2026-05-06 |
| **PR #68** | Weekly `/api/cron/check-price-drift` (prod-only schedule) firing `pricing_drift_detected` to Mixpanel + CF analytics token CI plumbing | ✅ shipped 2026-05-06 |
| **PR #69** | Backlog: drop Mixpanel-scheduled-report follow-up (free tier paywalls subscriptions) | ✅ shipped 2026-05-06 |
| **PR #70** | Rip unused CF Web Analytics; add `@sentry/cloudflare` server-side error capture via OpenNext customWorker pattern + PII-scrubbing `beforeSend` | ✅ shipped 2026-05-06 |
| **PR #71** | `@sentry/browser` client-side error capture (consent-gated via `<AnalyticsBootstrap>`) + sentry-cli client-bundle sourcemap upload (`productionBrowserSourceMaps: true` + `scripts/upload-sourcemaps.mjs` with finally-block public-output strip) + `deriveEnvironmentFromHost` shared util | ✅ shipped 2026-05-06 |
| **PR #72** | Hotfix: `@sentry/cli` named-import shape (`{ SentryCli }`, not default) — fixes `SentryCli is not a constructor` on staging deploy. End-to-end verified: 43 `.map` files stripped, sourcemaps uploaded to Sentry, no public sourcemap leak | ✅ shipped 2026-05-06 |
| **PR #73 (Item #4)** | `@react-email/components` migration of all 6 transactional emails. TDD-first. `<Tailwind config={emailTailwindConfig}>` brand-token provider | ✅ shipped 2026-05-07 |
| **PR #74 (Sub-PR #4a)** | Sanity file-upload UI for Day-7 delivery. `voiceNote` + `readingPdf` as `file` fields + cross-field validation. New `isDeliverable()` predicate + GROQ helpers. Day-7-deliver cron sources from Sanity → mirrors to D1 → sends (asset existence is the readiness flag — no TOCTOU). Day-7 alert cron queries Sanity for `!defined(deliveredAt)` to avoid race | ✅ shipped 2026-05-07 (commit `2d183f4`) |
| **PR #75** | Integration branch `feat/operational-completeness` → main. Carried Item #1 (OpenNext `scheduled` handler dispatch in `custom-worker.ts`; wrangler crons map `event.cron` → `/api/cron/*` via `src/lib/cron-routes.ts` with `cf-cron: 1`), Item #2 (`/api/cron/reconcile-mirror` D1 → Sanity diff cron, walks last 7 days, uses pure `diffSubmission` helper), PR #73 (react-email migration), PR #74 (Sub-PR #4a) | ✅ shipped 2026-05-07 (commit `e3331dd`) |
| **PR #76 (Bundle 1)** | F-11 constant-time cron-auth via `timingSafeStringEqual`; submission orphan-field cleanup (`clientReferenceId`, `abandonmentRecoveryFiredAt`) including D1 migration `0003_drop_orphan_columns.sql`; CI-gated build-time Turnstile bypass assertion; TrustLine deletion; `letter/page.tsx` IIFE simplification | ✅ shipped 2026-05-07 |
| **PR #77 (Bundle 2)** | `trackThrottled(event, props, intervalMs)` analytics helper; `intake_save_auto` 30s rate-limit; `<SavedIndicator>` extracted from IntakeForm; `consentEffectRanRef` ref-guard in AnalyticsBootstrap | ✅ shipped 2026-05-07 |
| **PR #78 (Bundle 3)** | `experimental_taintObjectReference` markers via `src/lib/taint.ts` on Sanity write client / Stripe / R2 / D1 SQL client; `@next/bundle-analyzer` + `pnpm analyze` script; inferred-return-type sweep on 6 named functions (with `deriveEnvironmentFromHost` narrowing from `string` to a 4-literal union) | ✅ shipped 2026-05-07 |
| **PR #79 (Bundle 4)** | Broader inferred-return-type sweep across `src/lib/**` (~20 more function signatures). Promise-returning async exports kept for IDE-hover docs. Reverted attempted `useMemo` hoist of `process.env.NEXT_PUBLIC_*` reads after simplify pass — Next's DefinePlugin already inlines these as string literals, so inline reads are optimal | ✅ shipped 2026-05-07 |
| **PR #81** | Clear form button (inline morph, idle ⇄ confirming, focus mgmt, Esc handler — replaces ad-hoc `window.confirm()` after Designer + a11y council debate) + 72h idle TTL flip + `valuesUntouched` save-gate (fixes ghost empty-defaults autosave on mount) + BookingFlowHeader wordmark→home. Two `/simplify` passes + durable rule `feedback_no_native_browser_dialogs.md` | ✅ shipped 2026-05-07 |
| **PR #82 (bundle)** | Items 5+6 from the quality sweep. **Item 4 dropped** (Mixpanel CWV duplicates Clarity's native LCP/INP/CLS widget). **Item 5** — `<DelegatedTracking>` document-level listener reading `data-mp-event` + `data-mp-*` snake_case props → fires new `trackUntyped()` escape hatch. **Item 6** — Microsoft Clarity wired (external `<Script src>` form for CSP compliance, mounted from `<AnalyticsBootstrap>` post-consent, gated via shared `shouldEnableClientObservability` helper, CSP `*.clarity.ms` allowlist). Privacy policy patch script staged (`scripts/migrate-privacy-clarity.ts`, idempotent) | ✅ shipped 2026-05-07 |
| **PR #83** | Clarity EU-traffic hardening (closes 4 gaps surfaced by deeper research after PR #82 merged). Consent API v2 wiring (mandatory since 2025-10-31 for EEA/UK/CH per Microsoft) — `clarityConsent(true)` called at each grant site; CSP allowlist extended with `https://c.bing.com` (Microsoft beacon endpoint Clarity routes telemetry through); `<ClarityRouteTracking>` for App Router soft-nav so the multi-step intake doesn't read as one page in replays; `data-clarity-mask="True"` on the intake form + photo-preview container as belt-and-suspenders alongside Strict masking. Shared `src/lib/clarity.ts` types + `CLARITY_MASK_PROPS` constant prevent capital-T typos. Privacy patch script updated to name Microsoft Ireland Operations Limited as EU controller. Durable rule saved: `feedback_csp_check_before_script_tag.md` | ✅ shipped 2026-05-07 |
| **PR #84** | Migrate Clarity Consent API v1 → v2 surfaced by verify-against-docs research. PR #83 used `clarity("consent", true)` (v1, legacy); Microsoft now recommends `clarity("consentv2", { ad_Storage, analytics_Storage })` (v2, Google-Consent-Mode shape). Clean migration with no legacy v1 overload retained — Clarity wasn't yet live in prod when v2 landed (project ID provisioned same day, still in initial-install ingestion window). `ad_Storage: "denied"` hardcoded (site doesn't run ads); `analytics_Storage` reflects the consent state. Helper signature `clarityConsent(granted: boolean)` preserved; only the internal API call shape changed. Durable rule saved: `feedback_verify_vendor_claims_against_docs.md` | ✅ shipped 2026-05-07 |
| **PR #85** | Quality sweep — projection cleanup + copy carry-over + return-type sweep bundled into one branch. **Schema deletions** (9 PROJECTED-BUT-UNUSED fields with zero consumers): `bookingPage.{emailLabel, emailDisclaimer, securityNote, closingMessage}`, `bookingForm.{title, intro, description, confirmationMessage}`, `thankYouPage.steps`. **Wire-up**: `bookingPage.paymentButtonText` → IntakeForm `submitLabel` → PageNav. **6 new editable knobs**: `bookingForm.{nextButtonText, saveAndContinueLaterText, pageIndicatorTagline}` + `landingPage.contactSection.{successHeading, successBody, sendAnotherButtonText}`. **Audit corrections**: 4 fields the audit had marked "ambiguous" verified CONSUMED (`bookingForm.entryPageContent.*`, `pagination.overrides`, `loadingStateCopy`, `nonRefundableNotice` — schema description fixed + `validation: required`). **Inferred-return-type sweep on components/app**: Explore-agent enumeration found 1 actual gap (`StarField.tsx round`); Bundle 4 + earlier sweeps had already covered the rest. **Seed script** `scripts/seed-quality-sweep-fields.mts` (manual run by Max against prod + staging). 799/799 tests green | ✅ shipped 2026-05-07 |
| **PR #86** | Staging-smoke follow-ups to PR #85 surfaced during Max's staging verification: (1) **Contact form 500 fix** — route was treating `sendContactMessage`'s `null` resendId as a config failure even under `RESEND_DRY_RUN=1` (staging's intentional setting). Route now checks `isFlagEnabled("RESEND_DRY_RUN")` and treats null-id-under-dry-run as 200 success. New test covers the dry-run success path. (2) **Seed script paymentButtonText force-set** — `setIfMissing` couldn't update the pre-PR-#85 default (`Continue to Payment`, capital P, no arrow); switched to fetch-then-set so the canonical `Continue to payment →` form lands. Idempotent. (3) **New `scripts/cleanup-orphan-field-values.mts`** — unsets the 14 orphan field VALUES on bookingForm / bookingPage / thankYouPage docs that triggered Studio "Unknown fields found" warnings after the schema deletions in PR #80 + PR #85. (4) **Staging runbook hardening** — § 3 R2 bucket step now has explicit token-bucket-scope expansion + full CORS JSON example + write-probe verification, so the next env doesn't repeat the staging upload diagnostic that ate hours on 2026-05-07. (5) `/simplify` follow-ups: TODO comment at the route's dry-run check pointing at the deferred `EmailSendResult` discriminated-union backlog item; explicit `vi.stubEnv("RESEND_DRY_RUN", "")` on the existing 500 test for env-pollution hygiene; `ORPHANS as const satisfies Record<string, readonly string[]>` for typo-catch. Three new backlog items captured: defaults-merge convention drift, `EmailSendResult` discriminated union, `scripts/_lib/sanity-write-client.mts` helper extraction. Plus pre-checkout review-step backlog item queued for next session. 800/800 tests green | ✅ shipped 2026-05-08 |
| **PR #87** | Sanity Studio multi-workspace config (Production + Staging). Single-workspace Studio was binding the dataset at deploy time via `SANITY_STUDIO_DATASET`, so editors couldn't tell which dataset they were editing. Now `defineConfig([{...production}, {...staging}])` — sidebar shows a workspace switcher labeled Production / Staging with distinct preview URLs (`withjosephine.com` / `staging.withjosephine.com`) per workspace. URL changed: `withjosephine.sanity.studio/` → `…/production` and `…/staging` (root now shows a workspace picker). Pre-existing `studio/schemas/submission.ts` file-validation type errors remain — Studio's deploy script doesn't run typecheck so they don't block | ✅ shipped 2026-05-08 |
| **PR #88** | Pre-checkout review step on intake form + DOB/TOB autoformat. **ReviewSummary** card stack on the final intake page above the consent block — per-section cards with the user's filled answers and an Edit link that snaps back to the matching page (state preserved by the existing localStorage draft). Skips consent + companion fields (`_unknown`, `_geonameid`). multiSelect renders as a list, file uploads as an 80×80 thumbnail (R2 public origin), dates as Locale long form without timezone shift. **DatePicker** auto-inserts slashes as digits are typed; displays DD/MM/YYYY consistently; calendar overlay tracks the typed value via controlled month state. **TimePicker** auto-inserts the colon as digits are typed; non-digits stripped. **PageNav** submit + next buttons get `whitespace-nowrap` so the "Continue to payment →" arrow doesn't wrap. New `COMPANION_SUFFIX_UNKNOWN` / `COMPANION_SUFFIX_GEONAMEID` and shared `FieldValue` / `FieldValues` types extracted | ✅ shipped 2026-05-09 (commit `12b4bca`) |
| **PR #89** | Thank-you page Sanity wiring + GDPR preview toggle + Clarity img-src CSP fix. **Thank-you page**: 5 new Sanity fields on thankYouPage (readingLabel, confirmationBody, timelineBody, deliveryDaysPhrase, contactBody) + small `renderWithSlots` helper to substitute `{deliveryDays}` and `{email}` inline. Mailto now reads `siteSettings.contactEmail` with the existing `CONTACT_EMAIL` constant as fallback. **GDPR preview toggle**: `siteSettings.consentBanner.hideInPreview` boolean — when ticked, the banner is suppressed inside Studio Presentation iframes so editors can edit pages underneath. Real GDPR/CCPA visitors are unaffected. **CSP fix**: `img-src` extended with `https://*.clarity.ms` so the c.gif heartbeat pixel stops being blocked (the downstream "a[c] is not a function" TypeError observed in staging logs was a symptom of this same block — Clarity's tag throws when the prerequisite request fails). New scripts: `seed-content-wiring-sweep.mts` (idempotent setIfMissing for canonical defaults), `promote-staging-to-production.mts` (one-time staging→prod copy via createOrReplace, with explicit type allowlist that REFUSES to touch the `submission` type — customer PII protection) | ✅ shipped 2026-05-09 (commit `7941744`) |
| **PR #90** | Phase 1 session 1 — user-model magic-link foundation. D1 migration 0004 adds `user` table (id, email UNIQUE, name, created_at, updated_at) + 3 listen_* tables (magic_link, session, audit) + `submissions.recipient_user_id` column with index. `src/lib/auth/users.ts` exports `getOrCreateUser({email, name})` via single `INSERT…ON CONFLICT…RETURNING id` (1 RTT) with UUID-discriminator isNew detection (immune to clock collisions). `src/lib/auth/listenSession.ts` is user-keyed: `issueMagicLink`, `redeemMagicLink` (atomic single-use, 5× mismatch poison, Level 1 email-match, narrow `findSubmissionRecipientUserId` for the hot path), `getActiveSession`, `requireListenSession` (emits `listen_session_invalid` / `listen_cross_user_denied` on every denial). `applyPaidEvent` resolves user FIRST then writes `markSubmissionPaid` + recipient_user_id atomically via COALESCE. 11 ship-blocker + should-fix review items all landed in same PR via 5-vantage code review pass | ✅ shipped 2026-05-09 (#90) |
| **PR #91** | Phase 1 session 2 — auth routes + /my-readings + listenedAt mirror. `POST /api/auth/magic-link` (uniform 303-or-204, strict email regex via `isValidAuthEmail`, `next` allowlist via new `safeNext` util, `runMirror`-deferred send to defeat timing oracle); `POST /api/auth/magic-link/verify` (POST-only consumer, redeem + `__Host-` cookie set + 303). New `/auth/verify` Next page replaces inline-HTML route handler (memory rule `feedback_no_inline_html_in_routes`). File proxy `/api/listen/[id]/{audio,pdf}` via shared `gateListenAssetRequest` (rate-limit + cookie + session + asset URL in one D1 round-trip via `findSubmissionListenContext`); Range header forwarded; provenance headers stripped. `/my-readings` index for repeat customers (server component, discriminated-union state). `mirrorMarkSubmissionListened` setIfMissing Sanity patch fires from audio route on first-byte Range only (collapses N chunks → 1 mirror write). 3 new Sanity doc-types (`myReadingsPage`, `magicLinkVerifyPage`, `emailMagicLink`) under "✉️ Customer copy" deskStructure group. Pentester GO with 5 conditions met; /simplify 3-reviewer pass applied | ✅ shipped 2026-05-10 (#91) |
| **PR #93** | Phase 2 — Sanity Studio dashboard restructure + customer-emails-and-pages Sanity wiring. **Submissions** promoted to root sidebar entry ("📬 Submissions") OUT of the old "Bookings" group; 3-slice workflow (🟡 Awaiting payment / 🟠 Paid awaiting delivery / 🟢 Delivered with ✓ Listened + ○ Not listened sub-panes distinguished by `defined(listenedAt)`). "📋 Bookings" renamed "📋 Booking Configuration" — schema-driven content only. **List preview rewritten** for Becky: full-name title (extracted from intake `responses[]` `first_name` + `last_name`); state-aware subtitle in email-first / dates-second order; clock-skew-safe `Day N of 7` / `Day N — overdue` counter from `paidAt`; email omitted from subtitle when title falls back to email (no duplication). **Photo R2 preview component** (`studio/components/PhotoR2Preview.tsx`) renders the intake photo from `images.withjosephine.com/<photoR2Key>` (200×200, lazy) in the detail view. **Per-reading thank-you overrides** — new `overrides[]` array on the `thankYouPage` singleton with per-reading optional override for the voicey fields (heading / subheading / confirmationBody / timelineBody / contactBody / closingMessage); page consumer (`src/app/thank-you/[readingId]/page.tsx`) merges override on top of page defaults. **Order Confirmation + Day 2 Started emails Sanity-wired** (closes Phase 1 ISC-49 + ISC-50): new `emailOrderConfirmation` + `emailDay2Started` doc-types; templates refactored to `{ vars, copy }` shape mirroring Day7Delivery + MagicLink. **Customer copy group renamed** to "✉️ Customer emails & pages" (Becky-readable name). **`emailsFired.type` enum drift fixed** — added `day7-overdue-alert` to match `EmailFiredType` in code (was throwing Studio validation errors). **`emailsFired` array items now write with `_key`** — `mirrorAppendEmailFired` was inserting without `_key`, so Studio rendered the audit log as the "Missing keys" warning instead of as rows. Stable content-derived key (`type-sentAt` sanitized to `[A-Za-z0-9_-]`) keeps reconcile-cron re-mirrors from duplicating. **Studio typecheck cleanup landed in-PR** — refactored the pre-existing `requireWhenDeliveredAtSet` ↔ `FileRule` type mismatch (deferred since PR #87) to a `CustomValidator<unknown>` factory + inline `rule.custom(...)`; `cd studio && pnpm typecheck` returns 0 errors for the first time in months. **Singleton "Untitled" H1 bug fixed** — Sanity Studio v5's `useDocumentTitle` short-circuits when `editState.draft/.published` are both undefined (`preview.prepare()` never runs); seed script `scripts/seed-customer-emails-and-pages.mts` now bootstraps all 7 customer-facing singletons via `createIfNotExists` so they render proper titles on first visit. **One-shot cleanup script** `scripts/cleanup-submission-orphans.mts` backfills `_key` on existing `emailsFired` entries + unsets orphan `clientReferenceId` / `abandonmentRecoveryFiredAt` values left over from PR #76's column drop. /simplify cleared 2 batches (one in-PR, one post-batch); Pentester gate not applicable (Studio-only PR, no public surface) | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#93, commit `bf61e8d`) |
| **PR #92** | Phase 1 session 3 — listen page rebuild + Day-7 email rewrite + HMAC token rip. New `/listen/[id]` server page (6-state machine: delivered + `?welcome=1` ribbon, signIn, checkEmail, rested, throttled, assetTrouble per D-4) replaces legacy `/listen/[token]`. Audio/PDF embed `/api/listen/[id]/{audio,pdf}` only — Sanity CDN URLs never reach the browser. New `listenPage` + `emailDay7Delivery` Sanity doc-types. Day-7 email rewritten to UX-locked verbatim copy; props restructured to `{ vars, copy }`; Sanity-driven via lazy-import + `EMAIL_DAY7_DELIVERY_DEFAULTS` fallback; subject templated with `{readingName}`. HMAC token rip: `src/lib/listenToken.{ts,test.ts}` + `/listen/[token]/` deleted (`src/lib/hmac.ts` retained for cron-auth + magic-link session). Magic-link routes adjusted to keep listen-flow in-context: rate-limit surfaces `?error=throttled` (was silent `?sent=1`); verify route appends `?welcome=1` on listen-next success + redirects `/listen/[id]?error=rested` on listen-next failure. `safeNext` extended with `isListenNext()` predicate consolidating path-shape rules. /simplify 3-reviewer pass applied in-PR (5 fixes incl. a real silent-no-op functional bug: `RestedCard` had no email input → reviewer caught it before merge); 6 deferrals filed in BACKLOG. Pentester GO with conditions: HIGH-1 timing oracle accepted-and-documented, MEDIUM-1 cron `SITE_ORIGIN` env-driven fixed in-PR | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#92, commit `3018483`) |
| **PR #94** | Phase 3 — weekly Sanity dataset backup cron. NDJSON streaming export → R2 multipart upload at `backups/{weekly\|monthly}/<YYYY-MM-DD>/`. Schedule `0 3 * * 1`; first-Monday-of-month rolls to `monthly/` prefix (separate Bucket Lock + lifecycle). Second pass walks NDJSON for `voiceNote`/`readingPdf`/`photoR2Key` refs + mirrors each asset binary at concurrency 6. New lib under `src/lib/backup/` (period, sanityExport, r2Multipart, ndjsonAssets, photoKeyValidation, types, sanityAsset). Gated behind `SANITY_BACKUP_ENABLED` flag. `/simplify` GO (6 in-PR fixes incl. `@sanity/asset-utils` direct dep, parallel asset walk, period-prefix helper); Pentester GO (MEDIUM-1 `isValidPhotoR2Key` predicate fixed in-PR) | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#94, commit `4af2fb3`) |
| **PR #95** | Phase 3.5 — Sanity outbound webhook → R2 mirror (sub-seconds RPO on voice/PDF binaries). New route `/api/sanity-backup-webhook` receives Sanity asset webhooks and HEAD-then-PUT mirrors each binary to `BACKUPS_BUCKET` at `backups/live/<assetId>` (content-addressed, idempotent). Drops voice/PDF RPO from 7d (weekly cron) to seconds. Same `SANITY_BACKUP_ENABLED` flag. New secret `SANITY_BACKUP_WEBHOOK_SECRET` (distinct from `SANITY_WEBHOOK_SECRET`). /simplify GO (QUAL-1 cron `AbortController` timer-vs-stream bug fixed in-PR + `SanityFileRef` alias); Pentester GO (MEDIUM-1 body-size pre-check before HMAC fixed in-PR on BOTH backup-webhook AND existing `/api/sanity-sync` route). Council decision A-NOW-B-deferred: Becky-direct-to-R2 (Option B) sits behind triggers (≥50 readings/month sustained, OR Sanity binary-asset incident, OR forced Studio refactor) | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#95, commit `47d3010`) |
| **PR #96** | Phase 4a foundation — GDPR compliance hardening, low-blast-radius layer. D1 migrations `0005_financial_records.sql` (separable 6yr HMRC tax record, denormalized email so the row survives user cascade) + `0006_deletion_log.sql` (append-only audit with vendor tracking-ID columns: `stripe_redaction_job_id`, `brevo_smtp_process_id`, `mixpanel_task_id`, `ip_hash`). `src/lib/compliance/retention.ts` exports `TAX_RETENTION_YEARS=6` + `READING_CONTENT_RETENTION_YEARS=3` + `computeFinancialRetainedUntil`. `src/lib/compliance/intakeConsent.ts` exports Privacy Counsel verbatim `ART6_CONSENT_LABEL` + `ART9_CONSENT_LABEL`. Sanity `consentSnapshot` schema extended with `art6Consent`/`art9Consent` sub-objects (legacy fields readOnly). Mirror writer + `ackBlock` helper write both with locked labels. IntakeForm: two new mandatory consent checkboxes, session-fresh state (NOT persisted in draft — fresh ack per submission), per-checkbox `aria-invalid` errors. `/api/booking` POST guard rejects 400 on missing/false flags. Stripe webhook + `applyPaidEvent` write `financial_records` in atomic `dbBatch` with paid UPDATE (no observable half-applied window); country extracted from `customer_details.address.country`. Vendor research locked in PRD `## Decisions` (Stripe Redaction Jobs / Brevo dual-call / Mixpanel v3) | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#96, commit `ea8f0e3`) |
| **PR #97** | Docs — Brevo Phase 1 pre-vetting via support ticket (LAUNCH-BLOCKING for newsletter). Direct read of Brevo Acceptable Use Policy PDF (Section E) revealed "Clairvoyance, fortune telling and astrology" is named as regulated/sensitive — mandatory pre-vetting via `support@brevo.com` before first send. Not optional ("must pass through a vetting process"). New backlog section captures: full ticket-content checklist, Section C opt-in spec verbatim (unchecked-default + descriptive label + proof-of-opt-in retention), suspension thresholds (complaint >0.2%, hard-bounce >2%, unsubscribe >1%), three-step Max-action sequence (3a vetting → 3b sender setup → 3c newsletter form). Process miss captured as incident #0 in `feedback_verify_vendor_claims_against_docs.md` | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#97, commit `ae387f0`) |
| **PR #98** | Phase 4b — cascade delete + admin endpoint + Art. 20 export. Three vendor helpers under `src/lib/compliance/vendors/`: Stripe Redaction Jobs (`POST /v1/privacy/redaction_jobs` two-phase async, `validation_behavior: "fix"`), Brevo dual-call (sync `DELETE /v3/contacts/{email}` + async `DELETE /v3/smtp/log/{email}` → `process_id`), Mixpanel data-deletions v3 (US region hardcoded, distinct_ids = submission_ids). `cascadeDeleteUser` orchestrator: predicate `recipient_user_id = userId` (Phase 5-ready); per-submission order R2 photo → Sanity DOC (first — Sanity rejects asset delete while referenced) → Sanity assets → D1 row (financial_records preserved); user-level vendor cascades parallel via `Promise.all`; D1 user/listen_session/listen_magic_link cleanup last; `deletion_log` written at start + completed with all tracking IDs. `wasUserDeleted(userId)` query exported. Sanity Studio doc action `deleteCustomerData` with branded two-step typed-DELETE confirmation (no native dialog per ISC-A6). `POST /api/admin/delete-user` X-Admin-Token gated via `timingSafeStringEqual` from `src/lib/hmac`; every refusal returns empty-body 404 (Pentester LOW-6 — indistinguishable from auth failure); every failed auth writes `admin_auth_failed` audit row (Pentester HIGH-2). `POST /api/privacy/export` magic-link-gated; `wasUserDeleted` idempotency + 5-min throttle via `listen_audit` lookup + 50-submission cap (Pentester HIGH-1); parallel asset fetches with post-fetch budget reconciliation (no race); fetches live Sanity `consentSnapshot` per submission so `consent.json` carries Art. 6 + Art. 9 verbatim (Quality HIGH — Art. 20 portability); ZIP via fflate → R2 with 7-day pre-signed URL emailed via Resend. Reuse cleanups: `timingSafeStringEqual` + `sha256Hex` consolidated in `src/lib/hmac.ts`. /simplify 3-reviewer pass + Pentester pass — all HIGH + auth-sensitive findings fixed in-PR; 10 MED/LOW filed in BACKLOG with explicit trigger conditions | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#98, commit `55ae8bc`) |
| **`84c77e2`** | Fix — Mixpanel auth is HTTP Basic with username:secret pair (not Bearer with single token). Caught at provisioning when Mixpanel showed Max username + secret. Earlier OBSERVE-phase research flagged the ambiguity ("smoke-test both auth shapes before locking the code") but Bearer shipped without smoke-test. Direct commit to parent (process miss — should have been a fix PR). 2 files changed, 29+/12-, 1094/1094 tests green. Per `feedback_verify_vendor_claims_against_docs` | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (direct commit `84c77e2`) |
| **PR #99** | Docs — Phase 4 production secrets + R2 lifecycle runbook (post-merge-to-main actions). New section in BACKLOG.md: `wrangler secret put` commands for `ADMIN_API_KEY` + `MIXPANEL_SERVICE_ACCOUNT_USERNAME` + `MIXPANEL_SERVICE_ACCOUNT_SECRET`; R2 lifecycle rule `exports-7d-expiry` on `exports/` prefix (7-day delete); `wrangler secret list` verify; 8-step end-to-end smoke-test on production Studio. `BREVO_API_KEY` explicitly commented-out and gated on the Brevo Phase 1 vetting ticket clearance | ✅ shipped 2026-05-11 to `feat/listen-redesign-and-gifting` (#99, commit `fd778c0`) |
| **PR #110** | Release cut — Phases 1–5 bundle on `release/v1.0.0`. True-merge (not squash) of 66 commits from `feat/listen-redesign-and-gifting` into the new `release/v1.0.0` branch (cut from main 2026-05-13). Bundles magic-link auth + listen redesign + Sanity Studio dashboard + dataset backup cron + live webhook mirror + GDPR cascade delete + Art. 20 export + Gifting feature (Sessions 1a-7) + today's Phase 3 staging provisioning verification commits. Also: Sentry secret-name typo fix (`SENTRY_DNS` → `SENTRY_DSN`) restored server-side error capture on both workers (silently disabled since project inception). `requireEnv("NEXT_PUBLIC_*")` refactored to literal `process.env.X` access at 3 call sites so DefinePlugin can inline; ESLint `no-restricted-syntax` rule guards against recurrence. New `docs/SANITY_BACKUP_RUNBOOK.md` (CF Access Service-Auth + Bypass patterns, Bucket-Lock cleanup gate, re-upload-preserves-history note); STAGING_RUNBOOK §9 CF Access patterns; LAUNCH_SMOKE_TEST_PLAN B-6. Surfaced + fixed: `wrangler deploy` overwrites dashboard-set Worker Variables — `SANITY_BACKUP_ENABLED` moved to declarative `wrangler.jsonc env.staging.vars`. Surfaced + documented: R2 Bucket Locks are not S3-Compliance-equivalent (no mode toggle, removable by admin token) — filed under Security backlog as "R2 API token scoping for backup buckets". `_simplify` of staging cron returned `partial: true` on stale orphan `photoR2Key` refs — by-design Promise.allSettled behaviour; Bucket Lock prevents same-period re-runs (Pentester LOW-3 prediction confirmed) | ✅ shipped 2026-05-13 to `release/v1.0.0` (#110, merge commit `7100642`) |
| **PR #111** | Release-prep: docs cleanup + README rewrite + CI yaml hardening. **Docs:** deleted `MASTER_PLAN_LISTEN_DASHBOARD_BACKUP_COMPLIANCE.md` and `PLAN_OPERATIONAL_COMPLETENESS.md` (shipped-plan narratives); renamed `PHASE10_CLOUDFLARE_SETUP.md` → `CLOUDFLARE_SETUP.md` (de-phase-numbered); SECRETS_CHECKLIST title de-phase-numbered; cross-ref repointing across STAGING_RUNBOOK, SANITY_BACKUP_RUNBOOK, LAUNCH_SMOKE_TEST_PLAN, BACKLOG. **README:** rewritten for engineering audience — corrected tech-stack table, accurate CI deploy semantics, Documentation Map linking 10 operational docs, conventions section. **CI yaml:** push trigger + deploy-staging if-condition tightened to `main` + `release/v1.0.0` only; drops three feat branches that auto-deployed during Phase 1-5 rollout | ✅ shipped 2026-05-13 to `release/v1.0.0` (#111, merge commit `44c000f`) |
| **PR #112** | Docs — delete 2026-05-07 audit snapshots. Removed `docs/audits/orphan-schema-fields.md` (227 lines) + `docs/audits/hardcoded-copy.md` (80 lines); both frozen historical artefacts with every finding tagged RESOLVED. Findings shipped via PR #85 + #86. Two stale "Full landscape in audits/…" pointer sentences in BACKLOG trimmed; resolution narrative stands on its own | ✅ shipped 2026-05-13 to `release/v1.0.0` (#112, merge commit `8eff7d3`) |
| **PR #113** | Docs — consolidate ops trio into OPERATIONS.md, move runbooks to `docs/runbooks/`, drop BECKY_SMOKE_TEST_GUIDE. **Consolidate:** `D1_MIGRATIONS.md` + `SANITY_DATASETS.md` + `MIXPANEL_FUNNELS.md` (3 files, 377 lines) merged into single `docs/OPERATIONS.md` with three sections. **Organize:** `STAGING_RUNBOOK.md` + `CLOUDFLARE_SETUP.md` + `SANITY_BACKUP_RUNBOOK.md` moved into `docs/runbooks/` subdir (provisioning-grade procedures separated from ops references). **Delete:** Becky-facing customer-journey guide (362 lines) — wrong audience for the repo, lives better in shared Notion / Google Doc. Cross-references updated across 7 files plus runbook-internal `./` → `../` path adjustments. End-state: 13 → 8 tracked docs files | ✅ shipped 2026-05-13 to `release/v1.0.0` (#113, merge commit `520153d`) |
| **PR #114** | Docs — backlog prune (509 lines removed) + rename `POST_LAUNCH_BACKLOG.md` → `BACKLOG.md`. Per project rule "Remove items when shipped; CHANGELOG is the trail." 28 SHIPPED / RESOLVED / CLOSED / ARCHIVED sections deleted: Phase 3.5 SHIPPED, Phase 5 Sessions 1a-5 + Multi-agent review ARCHIVED + carry-overs, Apex 500 FIXED 2026-04-30 (PR #44), three Session 7 /simplify resolutions, two 2026-05-07 audit RESOLVED entries, CWV→Mixpanel CLOSED-AS-DROPPED, staging-tier runbook DOCUMENTED 2026-05-07, GDPR sub-processor migrations (PR #103 ran 2026-05-11), Phase 1.5 + Phase 2 forward-looking enumerations (roadmap, not backlog). Rename + global cross-reference repoint across 15 files (.md/.ts/.tsx/.jsonc/.yml/.mts code comments). BACKLOG.md final state: 620 lines, 32 active deferrals, all genuine pending items with explicit triggers | ✅ shipped 2026-05-13 to `release/v1.0.0` (#114, merge commit `649e29e`) |
| **PR #115** | Docs — CHANGELOG ledger catch-up for PRs #110–#114. Cherry-pick of `641678b` onto release branch | ✅ shipped 2026-05-13 to `release/v1.0.0` (#115, merge commit `cc8ec9e`) |
| **PR #116** | Phase 0 — integration-test foundation. Playwright E2E + Hono fixture sidecar + env-gated Sanity client + 15 Playwright tests (3 GREEN + 12 TDD-target `test.fixme`) + CI workflow. New deps: `@playwright/test 1.60` + `msw 2.14` + `hono 4.12` + `@hono/node-server 1.20`. `tests/e2e/fixtures-server.ts` (Hono on port 0) serves GROQ from `src/__fixtures__/sanity/e2e/*.json`. `src/lib/sanity/client.ts` honors `SANITY_API_HOST` override (E2E-only). `scripts/generate-e2e-fixtures.ts` strips PII via 8-key + email-regex + 7-name denylist (idempotent). `.github/workflows/e2e.yml` gates PRs to main/release/v1.0.0 across intake/gift/booking/sanity paths. Cross-process MSW limitation documented (D-19); Stripe `sk_test_` env-guard enforced; D-4 grandfathering for release/v1.0.0 → main merge. `/simplify` 3-vantage pass with 7 in-PR fixes (anonymize `_id` correctness fix, shared `READING_SLUGS` module, switch→lookup-table for sidecar, Promise.all parallelization ×2, one-pass name regex, drop TOCTOU mkdir, teardown cleanup). Branch `feat/phase-0-e2e-foundation` cut from `release/v1.0.0` @ `cc8ec9e`; 4 commits squash-merged. Tests: 3 green / 12 fixme local + CI green (Playwright 1m5s, 22.4s test runtime). PRD: `www/MEMORY/WORK/20260515-200000_intake-architecture-repair/PRD.md` Phase 0 (27/27 ISC) | ✅ shipped 2026-05-15 to `release/v1.0.0` (#116, merge commit `2e16aff`) |
| **PR #117** | Phase 1 — compliance decoupling + content contract on booking flow. Hardcoded GDPR Art. 6 + Art. 9 + cooling-off acknowledgments via new `<LegalAcknowledgments />` component; eliminated CMS-conditional gating on the intake form. Shared predicate `isFullyConsented(snapshot, requireArt9)` at `src/lib/compliance/intakeConsent.ts` consumed by client, server (3 booking routes), D1 mirror, Sanity mirror. D1 migration `0010_add_cooling_off_ack.sql` (additive nullable column). Sanity enum `"consent"` removed from `studio/schemas/formField.ts` `options.list`; custom validator surfaces deprecation when legacy doc loads. GROQ-CI invariants shipped: every reading slug has every required field; no two readings share `stripePaymentLink`; no orphan consent-type fields. Migration `scripts/migrate-strip-consent-fields-2026-05.ts` ran against staging. Phase 0 e2e Tests 1+2 un-fixme'd. PRD: `…/intake-architecture-repair/PRD.md` Phase 1 (46/46 ISC) | ✅ shipped 2026-05-15 to `release/v1.0.0` (#117, merge commit `44f4ccb`) |
| **PR #118** | Phase 1.5 — gift-form consent parity. Applied D-1a to the gift purchase flow: hardcoded Art. 6 + cooling-off (no Art. 9 — purchaser is not providing birth/intake data; recipient acks Art. 9 at redeem). Dropped third `termsConsent` checkbox (T&C bundled into Art. 6 label per booking pattern). New `<LegalAcknowledgmentsGift />` component. Sanity schema `bookingGiftForm` stripped of `art6ConsentLabel` / `coolingOffConsentLabel` / `termsConsentLabel` fields — labels now in source. Server route `src/app/api/booking/gift/route.ts` drops `termsConsent` body field. Phase 0 e2e Test 8 un-fixme'd. New durable rule memory `feedback_legal_ui_never_cms_conditional.md` (filed during PRD audit). PRD Phase 1.5 (49/49 ISC) | ✅ shipped 2026-05-16 to `release/v1.0.0` (#118, squash `6d28dc3`) |
| **PR #129** | Phase 5 audit-fixes — Pentester + /simplify HIGH+MED + comment-rot sweep. Closes the intake-architecture-repair Phase 5 audit cycle. **Pentester (CONDITIONAL GO):** H-1 `/api/internal/issue-magic-link` hard-gated on `ENVIRONMENT !== "production"` (account-takeover risk closed); M-1 `wrangler.jsonc` declares `NEXT_PUBLIC_SANITY_DATASET` (prod + staging) + `assert-staging-bindings.mts` extended; M-2 silent 404 on `/api/internal/gift-claim-regenerate` auth fail; M-3 dropped `?recipient=` URL override on `/gift/claim/[token]` (content-injection vector closed). **/simplify Reuse:** new `src/lib/auth/adminTokenAuth.ts` (3 admin/internal routes), `src/lib/api/parseBody.ts` (5 sites), `src/lib/copy/templateSlots.tsx` (`renderWithSlots` shared); `LegalAcknowledgments` unified with `idPrefix` / `showArt9` / `consentIntro` props (gift consent parity closed structurally; `LegalAcknowledgmentsGift.tsx` deleted); `tests/e2e/helpers/datetimeLocal.ts` + `accessHeadersOrEmpty()` for e2e. **/simplify Quality:** Q-H1 recipient greeting CMS-driven via 3 new `giftClaimPage` fields (`welcomeHeading` / `welcomeBody` / `welcomeCtaLabel` with `{recipientName}` slot); Q-H2 `gift-claim-regenerate` 8-arm if-chain → exhaustive `Record<GiftClaimRegenerateRefusalReason, …>` reason-map; Q-M2 typed `mergeParsed` helper replaces 2 `as FieldValues` casts. **/simplify Efficiency:** E-H1 `gateListenAssetRequest` parallelizes session + submission D1 reads via `Promise.all` (~50% latency cut on audio Range hot path); E-H2 `usePageErrors` memoizes `pageSchema` by `[allFields, currentKeys]`; E-M2 `findReadingRef` module-local Map cache. **Comment-rot sweep:** 30 citation tags (`Phase N Session M — Bxx` / `PR #` / `Sub-PR` / `ISC-foo`) stripped from 29 `src/**` + `studio/**` files; 3 Studio-UX strings de-jargoned (Becky-facing dropdown title + field descriptions). **Mirror-drift root-cause fix:** `/api/sanity-sync/route.ts` now refuses `submission` / `magicLinkRequest` / `giftPurchase` doc types server-side (closes the 2026-05-15/16 51-doc production-Sanity drift; Sanity webhook GROQ filter on dashboard side updated by Max). **Apex-allowlist legal pages:** `/privacy`, `/terms`, `/refund-policy` added to `APEX_ALLOWLIST_PREFIXES` (statutory compliance reachability — holding page is soft-launch posture, not legal waiver). **Pre-prod data cleanup** (separately landed 2026-05-17): 88 staging + 6 prod D1 rows wiped + 83 staging + 59 prod Sanity submission docs deleted via `scripts/cleanup-test-submissions.mts` + 1 staging + 6 prod R2 objects deleted via CF dashboard. Vitest 1424 → 1446 (+22); Playwright 28p+0s mock-mode baseline maintained. Net diff −163 lines (dedup beat additions). LOW deferrals filed in BACKLOG with promotion triggers; Q-M1 consent-type cleanup deferred to Phase 6 (gated on Bundle F migration) | ✅ shipped 2026-05-17 to `release/v1.0.0` (#129, squash `ebc16f1`) |
| **PR #128** | E2E coverage — localhost-mockable round-trip specs + CI gate + flag consolidation. Three new mock-mode specs (`stripe-local`, `gift-local`, `listen-local`) cover the same class of round-trip the staging specs catch, runnable in CI on PRs to `main` with zero external deps. Mock infra: extended `tests/e2e/fixtures-server.ts` (Hono) with Sanity v3 `POST /:apiVersion/data/mutate/:dataset` + `POST /:apiVersion/assets/:type/:dataset` + `/_e2e/captured-mutations` + `/_e2e/captured-emails` + `/_e2e/reset` endpoints; `src/lib/resend.tsx` adds a 5-line testability hook in the `RESEND_DRY_RUN` branch that POSTs payloads to the sidecar when `E2E_CAPTURE_URL` is set; spec-side signed Stripe webhook helper (`tests/e2e/helpers/stripeWebhook.ts`) uses `Stripe.webhooks.generateTestHeaderString`; new test-only `/api/e2e-reset` D1-truncate route with defense-in-depth gating (`E2E=1` + `ENVIRONMENT!=="production"` + secret-header). **Test-mode consolidation** (mid-PR refactor per Max feedback): collapsed 6 spec-mode flags to 1 (`E2E_SANDBOX=1` for staging; default = mock). Spec selection by filename pattern via `testMatch`/`testIgnore` — no per-spec opt-in skip. `helpers/roundtripFlags.ts` (99 lines) → `helpers/e2eMode.ts` (5 lines). **Three load-bearing fixes uncovered during build:** (1) `src/lib/sanity/client.ts` needs `useProjectHostname:false` under `apiHost` override (without it the SDK builds `${projectId}.${host}/...` which fails DNS for the local sidecar); (2) Playwright launches `webServer` BEFORE `globalSetup`, so the sidecar moved into a dedicated `webServer` array entry on a fixed port (47391); (3) Next App Router treats `_`-prefixed folders as private — `_e2e-reset/` was invisible to the router, renamed to `e2e-reset/`. **/simplify pass:** Turnstile waiter extracted to `helpers/turnstile.ts` (was duplicated in gift-local); `waitForTimeout(500/750)` replaced with `expect.poll` in stripe-local + gift-local (real flake fix); aggressive comment scrub. 28 playwright tests pass in mock mode (24s, well under 5-min CI budget) — 15 baseline + 4 stripe-local + 4 gift-local + 5 listen-local. Vitest 1441 baseline preserved | ✅ shipped 2026-05-17 to `release/v1.0.0` (#128, squash `82ce965`) |
| **PR #121** | Phase 4 — close Becky bugs #1, #4, #5, #6. **#1 booking-copy:** `formatNote` + `deliveryNote` rewritten to distinct WHAT (voice note + PDF) vs WHEN (7 days); migration `scripts/migrate-booking-page-copy-2026-05.ts` resets matching legacy strings on Sanity. **#4 gift thank-you addressing:** new gift-variant fields on `thankYouPage` schema (`giftPurchaserHeading/Subheading/Body` + `giftRecipientHeading/Subheading/Body`); `/thank-you/[readingId]` now resolves 3 modes (purchase / giftPurchaser / giftRecipient) via new `resolveContext` helper; purchaser name derives from Stripe session → D1 submission (`purchaser_first_name`); `purchaserFirstName` search param is test override; `renderWithSlots` extended to interpolate `{purchaserFirstName}` in heading + body. New `src/app/gift/claim/[token]/page.tsx` greets recipient by name (with `?recipient` test override) before the existing query-style claim flow takes over. **Silent prod bug fixed in passing:** gift recipients used to bounce to `/` because gift-redeem redirected to `/thank-you/<id>?gift=1` with no Stripe sessionId — now uses `&redeemed=1` as first-class branch (updated `gift-redeem/route.ts`, `gift/intake/page.tsx`, `useIntakeFormHandlers.ts`). Migration `scripts/migrate-thank-you-gift-variant-2026-05.ts` seeds new fields idempotently. **#5 thank-you body lock-in:** "copy of your answers" already absent; Test 6 locks continued absence. **#6 Birth Chart routing:** Test 4×3 un-fixme'd against `/book/<slug>/gift`; Phase 1 P1.10 GROQ uniqueness on `stripePaymentLink` is the regression net. `GiftForm.tsx` h1 now renders `{readingName}` (editorial copy moves to eyebrow span) so the selector tests can locate the heading. **/simplify pass applied 5 fixes:** extracted shared `<VellumShell />` (`src/components/VellumShell.tsx`) consumed by both `/gift/claim` routes; added `purchaserFirstNameOrNull` to `giftPersonas.ts` (existing `purchaserFirstNameFor` now delegates); extracted `firstParamValue` to `src/lib/next/searchParams.ts`; hot-path fast lane on `/thank-you/[readingId]` skips D1 lookup for paid purchaser without gift flag; inner `Promise.all` parallelizes `fetchPaidAmount` + `findSubmissionById`; page-level `Promise.all` for `fetchThankYouPage` + `fetchSiteSettings` after security guard fires (sequential `resolveContext` preserved to maintain "no Sanity fetch when sessionId guard rejects" invariant). 5 Playwright fixmes removed; suite **9p+6s → 15p+0s**. Vitest 1424/1424. PRD Phase 4 (163/163 ISC) | ✅ shipped 2026-05-16 to `release/v1.0.0` (#121, squash `fd03454`) |
| **`69c1d38`** | Becky bug #6 staging data fix — `scripts/fix-staging-stripe-link-swap-2026-05.ts` swapped Birth Chart ↔ Soul Blueprint `stripePaymentLink` on staging Sanity (root-cause confirmed via direct read: the two had been editorially swapped). Idempotent + guards against running outside `NEXT_PUBLIC_SANITY_DATASET=staging`. Production fix is a separate Studio Max-action | ✅ shipped 2026-05-16 to `release/v1.0.0` (`69c1d38`) |
| **PR #120** | Phase 3 — per-field error surfacing on booking intake (Becky bug #3 "Continue stuck with no cause"). New `usePageErrors` hook returns `{pageErrors, firstErrorKey, firstFieldLabel, errorCount}` via the existing `validateCurrentPage`. New `<PageValidationSummary />` (alert + count + first-field label + jump button) replaces the dead-end "Please complete the required fields above" banner. `renderContext.errors` now merges `pageErrors` over the submit-time `errors` state so per-field `aria-invalid` + inline errors derive on render. Next/Submit no longer disabled while invalid — clicking either triggers the existing `focusFirstError` path so users always see why they're stuck. New `src/__tests__/contracts/render-validator-parity.test.ts` asserts every required Sanity formField maps to a renderable type or paired-unknown companion (catches future drift between `formField` enum and `renderField.tsx` switch). Playwright Test 7 (`intake-errors.spec.ts`) un-fixme'd. `/simplify` 3-vantage applied: extended `focusFirstError` with `{scroll?: boolean}` + single-key form, extracted `derivePairedUnknownKeys` + `deriveTimeUnknownPairs` from `useIntakeSchema` (consumed by hook + parity test), dropped wrapper `<span>` in PageValidationSummary, flattened period/label ternary. Vitest 1408 → 1424 (+16); Playwright 8p+7s → 9p+6s. PRD Phase 3 (35/35 ISC + 5 anti) | ✅ shipped 2026-05-16 to `release/v1.0.0` (#120, squash `c2bb50d`) |
| **PR #119** | Phase 2 — IntakeForm god-component decomposition. **`IntakeForm.tsx`: 1049 → 297 LoC (-72%).** Extracted 6 hooks (`useIntakeSchema`, `useDraftRestore`, `useAutosave`, `useTurnstileChallenge`, `useFieldFocusTelemetry`, `useIntakeFormHandlers`), 3 sub-components (`renderField.tsx`, `RenderedSection.tsx` memoized, `IntakeFormBody.tsx`), 1 utility module (`intakeValidation.ts` with `validateCurrentPage` + `validateFullSubmission` + `collectFieldErrors` + `focusFirstError` + `fieldDomId` + `INTAKE_SUBMIT_ERROR`). Shared `collectConsentErrors(snapshot, requireArt9)` helper added next to `isFullyConsented`. `BOOKING_API_GIFT_REDEEM_ROUTE` const. New `intake_page_review_edit_click` event in `ClientEventMap` — fixes a telemetry bug where `handleReviewEdit` fired `intake_page_back_click` on forward jumps. `useDraftRestore` uses `useSyncExternalStore` + module-level swap-name cache + `queueMicrotask` to defer post-restore setStates (lint `react-hooks/set-state-in-effect`-compliant). Coupling-metric targets met: prop-drilling ≤2, sibling-hook-imports = 0, `RenderedSection` wrapped in `React.memo` (D-24). 49 new colocated tests; 1408/1408 vitest total; Playwright 8 passed + 7 skipped (no fixme delta); selector-rewrite blast radius = 0. `/simplify` 3-vantage applied (ledger at PRD D-25). Magic-strings audit P2.12d shipped (full repo sweep deferred as P2.12e follow-up). BACKLOG `react-hooks/refs sweep` entries for IntakeForm removed; DatePicker `setMonth` carry-in retained. PRD Phase 2 (73/80 ISC; remaining 7 are intentional follow-up filings) | ✅ shipped 2026-05-16 to `release/v1.0.0` (#119, squash `e50e516`) |

PR-E intentional omissions (not bugs): Stripe sessionId server-verify on `/thank-you` (Stripe redirects only on success — URL is the proof), abandonment-recovery (would touch users who never reached consent step — Mixpanel funnel covers visibility need), Resend template IDs (bodies inlined; defer).

---

## Phase 5 — Gifting feature (multi-session arc on `feat/listen-redesign-and-gifting`)

### Phase 5 Bundle A.2 — Listen round-trip Playwright spec — SHIPPED (parallel-worktree branch)

Automates Stage B-2 of `docs/LAUNCH_SMOKE_TEST_PLAN.md` (magic-link + listen flow) so Becky doesn't have to drive it manually. Extends the existing `stripe-roundtrip.spec.ts` pattern with two narrow production seams that keep customer-facing behavior unchanged.

- **New engineering-seam route** `POST /api/internal/issue-magic-link` — admin-token-gated (`x-admin-token` timing-safe-compared to `ADMIN_API_KEY`), returns raw token + verifyUrl + expiresAt without firing Resend. Wraps the existing `issueMagicLink()` helper from `src/lib/auth/listenSession.ts` (no reimplementation). Uniform HTTP 404 with empty body on every failure path (missing/wrong token, missing env, malformed body, unknown email) — same no-enumeration posture as `src/app/api/admin/regenerate-gift-claim/route.ts`. Auth-failure paths still write an `admin_auth_failed` audit row. 12 vitest cases cover all branches.
- **Cron force-mode** `GET /api/cron/email-day-7-deliver?force=<submissionId>` — single-submission entry that bypasses the `paidAt >= 7d` candidate filter so the spec can mirror a fresh paid submission Sanity → D1 without waiting for the daily window. Same `CRON_SECRET` Bearer auth; default sweep behavior unchanged. 4 new vitest cases cover the force-mode happy path + asset-awaiting branch + missing-submission branch + auth-gate parity.
- **Spec** `tests/e2e/specs/listen-roundtrip.spec.ts` — env-gated by `E2E_LISTEN_ROUNDTRIP=1`. Reuses the booking flow from Bundle A.0 + `seedIntakeDraft`. Drives: booking → Stripe webhook poll → Sanity asset upload (voiceNote + readingPdf) + `deliveredAt` set → cron force-mirror → `/listen/<id>` sign-in card → magic-link form → token retrieval via engineering seam → verify card → confirm submit → `?welcome=1` delivered surface (welcome ribbon + audio + PDF) → audio fetch with `Range: bytes=0-` → Sanity poll for `listenedAt` (15s ceiling). Per-run UUID email (`listen-roundtrip+<runId>@withjosephine.com`) so state stays isolated. CF Access service-token headers carry on every request via `extraHTTPHeaders`. Skips with a clear message when `ADMIN_API_KEY` is missing locally.
- **Helpers** `tests/e2e/helpers/stagingApi.ts` (new) exports `issueMagicLink` + `pollSanityListenedAt` + `pollUntilPaid` + shared `accessHeaders`/`requireStagingEnv` primitives reused by sibling spec helpers. `tests/e2e/helpers/sanityE2EAssets.ts` (new) exports `uploadDummyVoiceAndPdf` + `forceD1Mirror`. Fixtures `dummy-audio.wav` (0.5s silent WAV, 8KB) + `dummy-reading.pdf` (minimal valid 1-page PDF, 299 bytes) committed under `tests/e2e/fixtures/`. Sanity dedupes assets by SHA-1 so N spec runs share one underlying asset doc per fixture.
- **Config** `playwright.config.ts` recognizes `E2E_LISTEN_ROUNDTRIP` env flag alongside `E2E_STRIPE_ROUNDTRIP` (5min timeout, 1 worker, staging baseURL). `tests/e2e/global-setup.ts` skip predicate extended to both staging-roundtrip modes.
- **Auth + read-fallback decisions** — `ADMIN_API_KEY` is the secret; `CRON_SECRET` Bearer for force-mode (unchanged); Sanity read polls use `SANITY_E2E_READ_TOKEN` if provisioned, else fall back to `SANITY_READ_TOKEN`. Audio listenedAt mirror is triggered via direct `Range: bytes=0-` fetch in the page context (carries the `__Host-listen_session` cookie) rather than driving `audio.play()` (headless autoplay flakes).
- **Pre-push gates** — lint clean; typecheck clean; build clean with `.env.staging` sourced; new vitest cases (12 route + 4 cron force-mode + 1 unchanged-sweep regression) all green; baseline pre-existing studio test failure (`@sanity/icons` resolve via worktree node_modules symlink) is unrelated and was already documented in Session 7 CI fix.
- **/simplify pass** — shared CF Access header construction lifted out of `sanityE2EAssets.ts` to import from `stagingApi.ts`; `__dirname` ESM bug fixed via `fileURLToPath(import.meta.url)`; `sendMagicLink` not-called assertion strengthened from "implicit" to explicit mock.
- **Bake**: Spec runs end-to-end against staging only AFTER this PR merges to `release/v1.0.0` and CI's `deploy-staging` job lands the new `/api/internal/issue-magic-link` route on the worker. Locally the spec correctly skips when `ADMIN_API_KEY` is absent — verified.

### Phase 5 Session 1a — server-side gifting foundation — SHIPPED 2026-05-12

Server-side only. 14/41 ISC verified.
- D1 migration `0007_gifting.sql` adds 10 columns + 2 indexes: `is_gift`, `purchaser_user_id`, `recipient_user_id`, `recipient_email`, `gift_delivery_method`, `gift_send_at`, `gift_message`, `gift_claim_token_hash`, `gift_claim_email_fired_at`, `gift_claimed_at`, `gift_cancelled_at`.
- Repository/type layer with `markGiftClaimSent` + `countActivePendingGiftsForRecipient` helpers.
- `/api/booking/gift` route — Zod-free inline validator, both delivery modes, anti-abuse cap.
- Stripe webhook gift branch: `self_send` issues SHA-256 token + fires purchaser link-email immediately; `scheduled` defers token to Session 2 alarm + fires purchaser scheduled-email.
- New `GiftPurchaseConfirmation` React Email template (discriminated `self_send` / `scheduled` variant).
- New Sanity doctype `emailGiftPurchaseConfirmation` registered in deskStructure.
- Sanity mirror passthrough for gift fields; Studio submission doctype gains 5 read-only gift fields for Becky's panes.
- Tests: 18 route + 4 webhook gift-branch (1115 total).

Phase 5 Session 1b (UI) was deferred to a fresh quality-gate session per Max's directive — see PR #105.

### Phase 5 Session 1a/1b — combined gifting UI — SHIPPED via PR #105

Quality-gate session shipped purchaser-facing GiftForm + intake redirect + thank-you self_send claim URL handling. Council review (PM + UX + UI designer + Copywriter) signed off before scaffold.

### Phase 5 Session 2 — Durable Object alarms for scheduled gift claims — SHIPPED 2026-05-12 (PR #106, squash `3b95966`)

Architectural pivot mid-session: chose Durable Object alarms over the originally-locked hourly polling cron. Per-gift `GiftClaimScheduler` DO fires the recipient claim email at the exact `gift_send_at`, with +7d reminder fires up to 3 retries / 30 days. No cron route, no entry in `CRON_DISPATCH`.

**Shipped surfaces:**
- `src/lib/durable-objects/GiftClaimScheduler.ts` — DO class (sqlite-backed storage), `fetch("/schedule")` persists submissionId + sets alarm, `alarm()` posts internal dispatch route, reschedules or clears storage.
- `src/lib/booking/giftClaimDispatch.ts` — pure async dispatcher with discriminated outcome (`first_send` / `reminder` / `stop: claimed|cancelled|abandoned|max_retries|missing|not_scheduled`).
- `src/app/api/internal/gift-claim-dispatch/route.ts` — admin route the DO posts to (`x-do-secret` gated via shared `_lib/headerSecretAuth.ts`).
- `src/app/api/internal/gift-claim-regenerate/route.ts` + `scripts/regenerate-gift-claim.ts` — admin recovery primitive (issues fresh token, invalidates prior URL; send-first-then-persist semantics protect the prior token on Resend failures).
- `src/lib/emails/GiftClaimEmail.tsx` + `studio/schemas/emailGiftClaim.ts` — two-variant template (`first_send` with CTA + URL, `reminder` with no URL).
- Stripe webhook scheduled-mode parallel-schedules the DO alarm via `Promise.all` alongside `dispatchGiftPurchaseConfirmation`.
- Helpers extracted: `src/lib/booking/giftPersonas.ts` + `src/app/api/internal/_lib/headerSecretAuth.ts`.
- New worker secret: `DO_DISPATCH_SECRET` — distinct rotation from `CRON_SECRET`.

Tests: 1213 / 1213 (1173 baseline + 40 new). `/simplify` 3-vantage sweep applied (7 in-PR fixes); Pentester CONDITIONAL GO with MED-1 fixed in-PR.

### Phase 5 Session 3 — /my-gifts purchaser self-service — SHIPPED 2026-05-12 (PR #107, squash `4b790e6`)

Adds the purchaser-facing `/my-gifts` self-service surface plus 3 mutation routes. Closes the gap where purchasers had zero visibility or control after Stripe purchase confirmation. Magic-link auth re-uses Phase 1 `listenSession` (same cookie, authorized against `purchaser_user_id`).

**Shipped surfaces:**
- `src/app/my-gifts/page.tsx` + `MyGiftsView.tsx` — server component, session-authed, 5-state status rendering. Privacy-locked: status + recipient label only; never voiceNoteUrl/pdfUrl/responses/listenedAt.
- `src/app/api/gifts/[id]/edit-recipient/route.ts` — pre-fire edit of `recipient_email` / `recipient_name` / `gift_send_at`. Gated on `gift_claim_email_fired_at IS NULL`. Re-schedules DO alarm when `gift_send_at` changes.
- `src/app/api/gifts/[id]/cancel-auto-send/route.ts` — flips `scheduled` → `self_send`, cancels the DO alarm, issues a fresh claim token, sends the purchaser the self_send purchase-confirmation email.
- `src/app/api/gifts/[id]/resend-link/route.ts` — self_send only, rate-limited 1/hour + 3/day via `giftResendRateLimit` walking `emails_fired_json`.
- Shared `authorizeGiftPurchaser` auth preamble + `scheduleGiftAlarm`/`cancelGiftAlarm` DO scheduler client wrappers.
- `giftStatusFor(record)` pure status function + `giftResendRateLimit(emailsFired, nowMs)` predicate as single source of truth.
- New singleton Sanity doctype `myGiftsPage` (21 editable fields including 6 status labels + 3 action CTAs).
- `safeNext` allowlist regex extended with `/my-gifts`; `/api/auth/magic-link` forwards `next` for any non-default allowlisted target.

Tests: 1248 / 1248 (+35). `/simplify` 3-vantage sweep applied (6 in-PR fixes). Pentester CONDITIONAL GO (one MEDIUM rate-limit TOCTOU accepted-and-documented; closed in Session 4b).

### Phase 5 Session 4 — multi-agent council review — SHIPPED 2026-05-12 (`feat/phase-5-session-4-review`)

Council review (PM + UX + UI Designer + Copywriter + QA + Pentester) against the final shipped surface of Sessions 1a/1b/2/3. Surfaced 4 launch-blockers beyond the brief's known-deferral list.

**Shipped fixes (per Council disposition):**
- **LB-1 — `/my-gifts` action buttons were functionally broken.** `ActionForm` submitted as form-urlencoded against JSON-only routes. Replaced with new `GiftCardActions.tsx` "use client" component: fetch+JSON, in-button loading state, 2-stage tap-to-confirm on the irreversible flip-to-self-send, inline edit-recipient drawer.
- **LB-2 — `refundLine` copy contradicted non-refundable policy.** New copy points purchaser at `/my-gifts` instead of "write to me". Sanity migration ships both staging + production.
- **LB-5 — `/api/internal/gift-claim-dispatch` not in apex allowlist.** Would silently break every scheduled-mode gift during soft-launch. 2-line middleware fix.
- **M-1** Footer spacing on `/my-gifts`. **M-3** `recipientEmail` RFC 5321 254-char cap. **M-4** `ownEmailKey()` plus-aliasing + NFKC. **M-5** Persona names removed. **Copy locks D-3/D-4/D-5/D-6.**

HIGH-priority deferrals (LB-3 anti-abuse cap claim-time re-check, LB-4 GDPR cascade purchaser walk, LB-6 GiftForm a11y) — all shipped via Session 4b.

Tests: 1266 / 1266 (+18). Council artifacts: `PRD.md` (97 atomic ISC), `DECISIONS.md`, `FLOW_INVENTORY.md`.

### Phase 5 Session 4b — Stripe live-mode blocker sweep — SHIPPED 2026-05-12 (PR #109, squash `0ad4bb2`)

Closes every HIGH-priority deferral from Session 4 + every new Session-4 Pentester finding + every Session-3 corroborated Pentester deferral. Self-review audit (`SELF_REVIEW.md`, 236 lines) walked Session 4 ship-claims vs reality → 13 GAPs surfaced; audit-surfaced GAPs folded into the same PR.

**Shipped fixes:**
- **LB-3** Anti-abuse cap claim-time re-check (`countActivePendingGiftsForRecipient` extension with `excludeSubmissionId`; gift-redeem route gates between email-match and redeem). Closes the self_send mode bypass where `recipient_email` is NULL at purchase.
- **LB-4** GDPR Art. 17 cascade purchaser walk. `cascadeDeleteUser` enumerates `listGiftsByPurchaserUserId`; recipient-different-and-claimed → pseudonymise purchaser fields (preserves contract-base Art. 6(1)(b) data); recipient-not-yet-claimed → full delete.
- **LB-6 + GAP-1/2/12** GiftForm + GiftCardActions a11y bundle. All 8 GiftForm fields wired with `id` + `aria-describedby` + `aria-invalid`; focus-on-error via `useRef`; ✦ wrapped in `<span aria-hidden="true">`; drawer heading + form `aria-labelledby`. WCAG 3.3.1 + 4.1.2 compliant.
- **B5.14–B5.19** Every new Session-4 Pentester finding. Gift confirmation idempotency (pre-append-then-update pattern), Stripe `prefilled_email` dropped, constant-time `verifyGiftClaimToken`, `gift-claim-regenerate` 5-min cooldown, Stripe metadata canary warn-log, DO logging discipline audit.
- **B7.26** `stripTemplateTags` helper applied to purchaser-controlled inputs in gift route + edit-recipient route.
- **B6.20** Resend-link TOCTOU atomic lock. Migration `0009_gift_resend_lock.sql` adds `gift_resend_lock_until` column; `acquireGiftResendLock`/`releaseGiftResendLock` helpers; route uses try/finally with 60s TTL.
- **B6.21** Cancel-auto-send atomic-flip-first redesign. Conditional D1 UPDATE with placeholder hash WHERE `gift_claim_email_fired_at IS NULL` first (0 rows → 409); cancel DO alarm; issue fresh token; send email; final UPDATE swaps placeholder.
- **B6.22** `GiftCardData` narrow projection. `listGiftsByPurchaserUserId` returns minimal columns; purchaser email + financial fields no longer serialised to the `"use client"` boundary.
- **B6.23** 401-vs-404 timing parity. `authorizeGiftPurchaser` performs a no-op `findSubmissionById('<placeholder>')` on the 401 path.
- Audit-surfaced GAPs 4/5/6/7: `ownEmailKey` ZWSP JSDoc, `MAX_EMAIL_CHARS` hoist, `purchaserEmail` length cap, route JSDoc.

Tests: 1304 / 1304 (+32). D1 migration `0009_gift_resend_lock.sql` applied to staging 2026-05-12; production runs at the `feat/listen-redesign-and-gifting` → main merge.

### Phase 5 Session 5 — pre-launch completion sweep — SHIPPED 2026-05-12 (commits `f7a08ec` through `c99c3c8`)

T4–T8 carry-over from Session 4b + audit GAPs 8/9/10/11 closed; final `/simplify` 3-vantage sweep applied in-PR; Pentester re-audit returned GO with zero ship-blockers.

**Shipped (10 commits, 1304 → 1313 tests):**
- **T4** DO `env.SELF` service binding replaces public-edge HTTPS round-trip with in-isolate fetch. `wrangler.jsonc` services blocks for prod + staging.
- **T5** `src/lib/http/jsonPost.ts` typed POST helper + `src/lib/hooks/useMutationAction.ts` + `src/components/Form/InlineError.tsx`. `GiftCardActions` 3 mutation controls consume all three. Server Actions migration shipped as `useMutationAction` adoption (OpenNext 1.19.4 + workerd server-action manifest handling is fragile).
- **T6** Vellum article framing on `/book/[readingId]/gift` + `/gift/claim` (no-token + already-claimed branches via local `VellumShell`). Two new Sanity doctypes (`giftClaimPage`, `giftIntakePage`). GiftForm validation strings to Sanity (10 new fields). Shared `TimezonePreview` component below send-at picker in both GiftForm and GiftCardActions drawer. Resend-link precomputed `resendVerdict` via `GiftCardData`.
- **T7** `/my-gifts` action surface copy extraction. 14 new fields on `myGiftsPage` doctype covering drawer headings, field labels, Save/Saving/Cancel buttons, 2-stage confirm copy, Sending/Throttled, 3 action-error variants.
- **T8** `src/lib/booking/sendAndRecord.ts` helper + 5 call-site adoptions. `GIFT_DELIVERY = {selfSend, scheduled} as const` exported from `booking/constants.ts`.
- **T9** Audit GAPs 8/9/10/11. JSDoc on GiftCardActions 422 test; partial-intake recovery for gift recipients (IntakeForm autosave keyed by `draftScope = gift-redeem.<submissionId>`); 8 Becky-friendly Studio description rewrites.
- **/simplify dedupe** Shared TimezonePreview, collapsed `actionErrorLabel` helper, rate-limit summary copy to Sanity, removed duplicate `GiftDeliveryMethod` type.

Pentester GO 2026-05-12 — zero HIGH/MED findings.

Sanity migrations queued for production main-merge (ran against staging): `migrate-gift-pages-2026-05.ts`, `migrate-gift-form-validation-2026-05.ts`, `migrate-my-gifts-actions-copy.ts`. All `setIfMissing` idempotent.

Honest deferrals (production-signal-required triggers in `BACKLOG.md`):
- T6.28-30 (thank-you self_send claimUrl) — trigger: first purchaser support request asking where their link is.
- T8.7-10 (EmailShell consolidation), T8.11-14 (GiftForm → Form/* primitives), T8.15-17 (AuthGatedPage chrome extraction), T8.19-20 (GIFT_DELIVERY const sweep), T8.21 (regenerateGiftClaim Studio action) — **all closed in Session 6** below.

### Phase 5 Session 6 — cleanup and audits — SHIPPED 2026-05-12 (`feat/listen-redesign-and-gifting`)

Closes 4 of the 6 Session 5 deferrals (GIFT_DELIVERY const sweep, AuthGatedPage chrome extraction, EmailShell consolidation, GiftForm Form/* primitives) plus the regenerateGiftClaim Studio action that was re-promoted to pre-launch per Becky's customer-support self-serve need. Also a codebase-wide comment audit and a CLAUDE.md trim that promotes shipped-session history out of the live context file.

- **W1.1** `GIFT_DELIVERY` const adopted across 16 production sites. Derived `GiftDeliveryMethod` type from the const so the const is the single source of truth. Tests intentionally untouched (string literals document intent better there).
- **W1.2** `src/components/AuthGatedPage/AuthGatedPage.tsx` extracted. Dedupes SignInCard + CheckYourEmailCard between MyGiftsView and MyReadingsView. New `AuthGatedPageContent` interface in `src/data/defaults.ts`; both page-content interfaces extend it structurally. No new Sanity doctype.
- **W1.3** EmailShell gains `bareContainer?:boolean` prop (Path A). GiftClaimEmail, GiftPurchaseConfirmation, OrderConfirmation render via EmailShell. Default 560px Container path unchanged for the other 6 templates.
- **W1.4** GiftForm migrated to Form/Input + Form/Checkbox + Form/Textarea primitives (the same primitives IntakeForm already uses). Focus-on-error switched from refs to `document.getElementById`. Closed an a11y gap shared by every Form/Input + Form/Textarea consumer where `aria-describedby` previously lived on the FieldShell wrapper div instead of the input itself.
- **W1.5** New `studio/actions/regenerateGiftClaim.tsx` document action. Mirrors `deleteCustomerData` pattern: operator pastes admin token (Studio bundle never carries the secret). New `/api/admin/regenerate-gift-claim` admin route. Shared `regenerateGiftClaim` helper extracted to `src/lib/booking/regenerateGiftClaim.ts` — both internal DO/CLI route and the new admin route consume the same implementation. `isVisibleForDocument` predicate hides the action when the gift is claimed, cancelled, or not-a-gift. 6 unit tests.
- **W2** Comment audit — ~22 task-ref / WHAT-narration deletions across 15 files. New gitignored `www/docs/DECISIONS.local.md` (11 entries) captures future-session knowledge that doesn't belong in source. Studio schema description fields preserved (operator-facing Becky surface).
- **W3** CLAUDE.md trim — per-session SHIPPED blocks moved to this changelog; the project-root CLAUDE.md now carries only active state + durable rules + binding constraints.

Tests: 1319 / 1319 (+6 new Studio-action unit tests). Typecheck + lint + build all green. `/simplify` 3-vantage sweep + Pentester re-audit run on the combined Session 5+6 diff. No new Sanity migrations required this session.

### Phase 5 Session 7 — perf, refactor, staging verify, Becky guide — SHIPPED 2026-05-12 (`feat/listen-redesign-and-gifting`)

Closes the three /simplify-deferred refactors flagged in PR #86 reviewers (defaults-merge convention drift, `EmailSendResult` discriminated union, `sanity-write-client.mts` helper extraction), publishes a Becky-facing customer-journey smoke-test guide, verifies staging deploy + secrets + D1 migrations, and a magic-string sweep across the env/feature-flag helpers.

- **W1** happy-dom swap — auto-pass; shipped on main in PR #104 / commit `d586e93`. Baseline 1321/1321 tests was already running on happy-dom (~37s env setup).
- **W2.1** Hero / HowItWorks / Footer / Navigation migrated from `content ?? DEFAULTS` whole-object fallback to `{ ...DEFAULTS, ...content }` spread merge. `FooterContent.logoUrl` widened to `string | null | undefined` so explicit `null` is the typed sentinel for "hide logo". Test updated to cover both omitted-→-default and explicit-null-→-hide. Commit `0bec189`.
- **W2.2** `EmailSendResult` discriminated union — 4 variants (`sent` / `dry_run` / `skipped` / `failed`). `sendOrSkip` wraps Resend send in try/catch and returns `kind: "failed"`. Contact route switch-dispatches on `kind`; the `isFlagEnabled("RESEND_DRY_RUN")` leak is removed. `sendAndRecord` adapter narrows on `kind === "sent"` before writing the email_fired audit row. Stripe webhook keeps its sentinel-append pattern via new `getResendId(result)` helper. 10 sender exports + 9 test files updated. Commit `0c5036f`.
- **W2.3** `scripts/_lib/sanity-write-client.mts` helper extracted; all 13 `scripts/migrate-*.{ts,mts}` files migrated. Optional `{ apiVersion, dataset, readOnly }` overrides. `migrate-theme-semantic.ts` env-var standardized from `SANITY_API_TOKEN` → `SANITY_WRITE_TOKEN`. `tsconfig.scripts.json` gained `allowImportingTsExtensions`. Commit `edf4b8f`.
- **W3** Staging verification — D1 reports "No migrations to apply" (all 9 of 0001–0009 applied to `withjosephine_bookings --env staging`). Wrangler secrets list — 19/21 set; `SANITY_EXPORT_TOKEN` + `SANITY_BACKUP_WEBHOOK_SECRET` not set (flag-gated by `SANITY_BACKUP_ENABLED=0`). CI `deploy-staging` job ✓ on the Session 7 HEAD.
- **W4** `www/docs/BECKY_SMOKE_TEST_GUIDE.md` (362 lines) — 8 customer journeys: self-purchase booking + listen, self-send gift, scheduled gift, /my-readings repeat customer, /my-gifts (3 sub-flows), contact form, Studio regenerate gift claim, GDPR cascade delete. Each closes with ✅ Passed / ❌ If-it-didn't. Commit `a66122d`.
- **CI fixes** — Setup action now installs studio deps (`pnpm --dir studio install --no-frozen-lockfile`) so vitest's `studio/**/*.test.{ts,tsx}` include pattern resolves `@sanity/icons`. Closes 5 consecutive failures since Session 6 shipped regenerateGiftClaim.test.ts. Commits `73fe215` + `ef4acc3`.
- **/simplify follow-ups + magic-string sweep:**
  - Notification-email guard duplication lifted to `requireNotificationEmail(subType)`.
  - `EMAIL_LABELS: Record<EmailSubType, string>` added next to `EmailSubType` in `src/lib/analytics/server-events.ts` — single source of truth for human-readable log labels.
  - `sendOrSkip` collapsed: dropped duplicate `emailKind: string` parameter; takes only typed `subType: EmailSubType` and derives the label.
  - `isFlagEnabled(name: string)` → `isFlagEnabled(name: FeatureFlag)` — 3-member union of the flags actually used.
  - `requireEnv(name: string)` / `optionalEnv(name: string)` → typed `EnvVar` union of all 19 env vars used via these helpers.
  Commits `896fe20` + `dc9991e` + `99f6b46`.

Tests: 1323/1323 (+2 vs baseline 1321 — W2.1 added 1 explicit-null Footer test, contact route gained 1 for the new failed-kind path). Pentester re-audit on W2.2 → GO. Net diff +700 / -300 LOC including the 362-line Becky doc. CI all 5 jobs green on `99f6b46`; staging deploy verified.

### Phase 5 Bundle A.1 — gift round-trip Playwright spec — SHIPPED

Replaces manual Becky QA of the gift purchase + recipient claim flow with an automated Playwright spec env-gated by `E2E_GIFT_ROUNDTRIP=1`. Targets `staging.withjosephine.com` via CF Access service-token headers, drives the full flow end-to-end including Stripe sandbox checkout with the 4242 card.

- **Production seam:** `POST /api/internal/gift-claim-regenerate` now returns `claimUrl` on the 200 success branch. The endpoint is auth-gated by the `DO_DISPATCH_SECRET` worker secret (header `x-do-secret`); the spec calls it to retrieve a fresh claim URL because the staging worker has `RESEND_DRY_RUN=1` and never delivers the original purchase email. `RegenerateGiftClaimOutcome` ok-variant gained the new field; failure variants remain type-tightened.
- **Spec architecture:** purchaser leg fills `/book/birth-chart/gift` (scheduled delivery only — self-send is out of scope), drives Stripe to `/thank-you/<id>?gift=1&purchaserFirstName=...`, polls until the purchaser heading renders, calls regenerate, then clears cookies and drives the recipient leg through `/gift/claim?token=...` → `/gift/intake` → submit-with-3-consents → `?redeemed=1` thank-you.
- **Per-run isolation:** purchaser and recipient emails use `crypto.randomUUID().slice(0, 8)` local-parts (`gift-roundtrip-purchaser+<runId>@…`) so multiple spec runs against the persistent staging dataset don't collide on anti-abuse caps.
- **New helpers:** `tests/e2e/helpers/stagingApi.ts` (`regenerateGiftClaim`, `pollUntilPaid`) and `tests/e2e/helpers/giftDraft.ts` (`seedGiftIntakeDraft` mirrors `intakeDraft.ts` for redeem-mode storage key shape `gift-redeem.<submissionId>`).
- **Config wiring:** `playwright.config.ts` introduces `isE2ERemote = isStripeRoundtrip || isGiftRoundtrip` so future round-trip specs join the union without re-architecting the file. `global-setup.ts` skips the fixture sidecar + MSW under either flag.
- **Backlog update:** `Pre-prod data cleanup` SQL filter broadened to also match `gift-roundtrip-*` / `stripe-roundtrip-*` so post-run residue gets swept at launch time.

Unit tests +1 (claimUrl shape assertion on the regenerate route success branch); existing 11 route tests still green. Anti-criteria respected — no `Co-Authored-By: Claude`, no `eslint-disable`, no test-only code in production runtime paths.

---

## Pre-launch punch list — DONE

| # | Task | Status |
|---|------|--------|
| 1 | Contact form fix (web3forms → Resend) | ✅ |
| 2 | Manual e2e smoke test (320/375/768/1024 px) | ✅ |
| 3 | Code review pass + must-fix sweep | ✅ |
| 4 | Security review pass + fixes (listenToken expiry, R2 ContentLength signing, Turnstile gate on upload-url) | ✅ |
| 5 | Push branch to origin + merge + deploy | ✅ (PR #34 → #41) |
| 6 | R2 bucket CORS in CF dashboard | ✅ |
| 7 | Sanity content fill (Soul Blueprint price = $129 per Josephine, not $179 spec) | ✅ |
| 8 | Persistence resilience review (D1 source of truth + Sanity one-way mirror) | ✅ |

---

## Session-shipped narratives

### PRs #66–#70 (2026-05-06)

- **PR #66** — Sanity `priceDisplay`↔`price` validation rule + prebuild bake of Sanity prices into `src/data/readings.generated.ts`. Closes the static-fallback drift class. Data reconcile (canonical price per reading + Stripe Payment Link sync) is Max+Josephine.
- **PR #67** — PR-F2 server-side Mixpanel events: `payment_success`, `payment_expired`, `email_sent` (6 sub-types), `delivery_listened`. `serverTrack` helper at `src/lib/analytics/server.ts`. Threads identity via `submission_id` (not Stripe metadata — Payment Links don't support it).
- **PR #68** — Weekly `/api/cron/check-price-drift` cron firing `pricing_drift_detected` to Mixpanel for any drifty reading. Production-only schedule (Mon 12:00 UTC). Plus CF analytics token CI plumbing (later ripped in PR #70).
- **PR #69** — Backlog cleanup: dropped Mixpanel scheduled-report follow-up (free tier paywalls subscriptions; cron is operational telemetry, Studio yellow warning is the user signal).
- **PR #70** — Ripped unused CF Web Analytics integration. Re-added server-side error tracking via `@sentry/cloudflare` 10.51 in `custom-worker.ts` wrapping OpenNext's worker fetch. PII scrubbing in `beforeSend` (security review caught `/listen/[token]` HMAC + `cf-cron` bearer secret would otherwise leak to issue tracker). DSN as kill switch.

### PR #71–#72 — Sentry browser + sourcemap upload (2026-05-06)

- **PR #71** — Closed the two open Sentry follow-ups from PR #70. `@sentry/browser` for client-side React error capture from `error.tsx` / `global-error.tsx`, gated on the same consent banner as Mixpanel via `<AnalyticsBootstrap>`. Same DSN-kill-switch + PII scrubbing posture as the server SDK; init wrapped in try/catch so a Sentry init throw can never break the React error path. Plus client-bundle sourcemap upload to Sentry: `productionBrowserSourceMaps: true` + `scripts/upload-sourcemaps.mjs` (lazy-import, finally-block deletePublicMaps so `.map` files NEVER reach the public asset output regardless of upload status). `cf:build` chain extended; CI deploy jobs gain `SENTRY_ORG` + `SENTRY_PROJECT` env. Hoisted `deriveEnvironmentFromHost` to shared `src/lib/constants.ts`. Worker-bundle sourcemap upload to Sentry is scoped out — blocked upstream on sentry-javascript#19213.
- **PR #72** — Hotfix for #71's `deploy-staging` failure: `@sentry/cli`'s CJS shape under ESM consumption needed `import { SentryCli }` (named) not `import SentryCli` (default). Skip path didn't reveal it because the constructor was never reached. The `deletePublicMaps()` finally block ran before the throw — no sourcemap leak occurred. End-to-end verified after merge: staging deploy uploaded sourcemaps to Sentry (`[sentry-sourcemaps] upload complete.`), stripped 43 `.map` files, wrangler bundled 88 assets (vs 133 with maps), bundle gzip 4095 KiB unchanged.

### Integration branch PR #75 + Bundles 1–4 (2026-05-07)

`feat/operational-completeness` shipped to main as PR #75 (commit `e3331dd`). Carried 4 sub-PRs as one squash:
- **Item #1** (commit `6e042c4`) — OpenNext `scheduled` handler dispatch in `custom-worker.ts`. Wrangler crons map `event.cron` → `/api/cron/*` via `src/lib/cron-routes.ts` with `cf-cron: 1`.
- **Item #2** (commit `ff0b47c`) — `/api/cron/reconcile-mirror` D1 → Sanity diff cron, walks last 7 days, uses pure `diffSubmission` helper. "Create missing doc" intentionally not implemented (no consent snapshot in D1).
- **Item #4** (PR #73) — `@react-email/components` migration of all 6 transactional emails. TDD-first. `<Tailwind config={emailTailwindConfig}>` brand-token provider.
- **Sub-PR #4a** (PR #74, commit `2d183f4`) — Sanity file-upload UI for Day-7 delivery. New `isDeliverable()` predicate + GROQ helpers. Now in 1-week bake against Becky's first real reading on main directly.

Same-day backlog cleanup PRs landed: PR #76 (Bundle 1), PR #77 (Bundle 2), PR #78 (Bundle 3), PR #79 (Bundle 4). See PR table above for per-bundle detail.

### Quality sweep PRs #82–#87 (2026-05-07–08)

Clarity bundle (#82–#84) → projection + copy + return-type sweep (#85) → staging-smoke fix-ups (#86: contact route dry-run handling, paymentButtonText force-set, orphan-VALUE cleanup script, runbook hardening) → multi-workspace Studio config (#87). Quality-sweep item 4 (CWV→Mixpanel) dropped — Clarity already captures LCP/INP/CLS natively. Manual Max-ops landed alongside the merges: `seed-quality-sweep-fields.mts` + `cleanup-orphan-field-values.mts` ran against staging + production; `pnpm studio:deploy` re-deployed Studio with multi-workspace config. Audit findings docs at `www/docs/audits/orphan-schema-fields.md` + `www/docs/audits/hardcoded-copy.md`.

### Polish shipped beyond the 6-PR plan

- Booking flow split into entry → letter → intake routes (Max overrode v6 single-page after seeing it)
- BookingFlowHeader extracted, BOOKING_ROUTES constants
- NavigationButton + LinkContent (next/link `useLinkStatus`) for click-feedback loaders that swap label with spinner without width shift
- Outlined Button variant on Hero CTA + Nav CTA (matches intake Next button)
- About Josephine link → `/#about` (no separate `/about` page); html `scroll-behavior: smooth` for cross-page hash nav
- Entry page restack: tag/name/price/desc on left, illustration on right, What's Included on left-bottom, Book CTA on right-bottom (centered, top-aligned in cell)
- Thank-you page redo: `sessionId` validation guard, copy reflects email + 7-day timeline, dynamic route
- Field rename: `legal_full_name` → first_name + middle_name (optional) + last_name with letters-only Unicode pattern. Marginalia "Page 1" → "Your name". Migration `scripts/fix-applies-to-refs.mts` + `migrate-name-fields.mts` already applied to live Sanity.
- Cormorant Garamond actually loading now — was silently broken because of a `--font-display` CSS variable name collision between next/font and `@theme inline`. Fixed via abstract `--font-display-source` intermediate so Sanity theme font swaps still work.
- Calendar popover (react-day-picker) restyled to match brand
- Time popover with hour/minute selects (replaced native `<input type="time">`)
- "Anything" repetition cut in §5 (4 → 2 uses)

### Closed in the 2026-04-30 post-apex-fix day

- PR #45 — Turnstile site key forwarded in CI, `?? "bypass"` fallback removed, intake form helper text lifted out of the button column.
- PR #46 — image upload preview thumbnail; `<SanityLive />` gated behind `draftMode()`.
- PR #47 — CSP `img-src` accepts `blob:` (so the new preview actually renders); `cursor-pointer` on file picker + Remove button.
- PR #48 — High-res logos (1536² source for Hero) + responsive Nav (mobile horizontal w/ illustration vs desktop text-only) + optimizer width caps doubled to 2× retina + booking-letter mark + Sanity siteSettings fallback all pointed at `logo-main.webp`. 4 orphan logo files deleted.
- PR #49 — D1 client refactored REST → workerd binding. `createD1HttpClient` (61 LoC, 3 worker secrets, per-request HTTPS) replaced with `createD1BindingClient` using `getCloudflareContext({ async: true })` + `prepare(sql).bind(...).all()/.run()`. `initOpenNextCloudflareForDev()` wired so the binding works under `next dev`. Dead `preview_bucket_name` dropped from `wrangler.jsonc`. Local end-to-end smoke verified before merge.
- Worker secret `D1_DATABASE_ID` corrected to the actual UUID earlier in the day — that was the root cause of the post-deploy submission 500s, fixed without a code change. PR #49 makes the whole secret-misconfig class unreachable.

---

## Deploy infrastructure shipped (initial Workers/D1/R2 setup)

- D1 database `withjosephine-bookings` provisioned, schema migration applied to remote.
- Worker secrets set: `D1_ACCOUNT_ID`, `D1_DATABASE_ID`, `D1_API_TOKEN`, `LISTEN_TOKEN_SECRET`.
- Worker plain var: `BOOKING_DB_DRIVER=d1`.
- Apex `withjosephine.com` added as Custom Domain on the Worker (replaced parked Hostinger A record).
- `preview.withjosephine.com` gated by CF Zero Trust Access.
- R2 CORS configured to allow `https://withjosephine.com` and `http://localhost:3000`.
- Bundle size battle won via: `better-sqlite3` moved out of src tree to test-only, AWS SDK swapped for `aws4fetch`, `@sentry/nextjs` removed (re-enable as `@sentry/cloudflare` in backlog), `@vercel/og` references stripped post-build, NFT excludes for capsize-font-metrics + edge-runtime + react-dom legacy variants. Final compressed worker fits the 3 MiB free-tier limit.

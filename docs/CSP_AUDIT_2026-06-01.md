# CSP audit — 2026-06-01

Periodic audit of CSP allowlist entries against current vendor documentation. Triggered by dex `1pv3m55c` (code-review LOW finding on `src/middleware.ts:93`).

## Clarity origins

Vendor doc: https://learn.microsoft.com/en-us/clarity/setup-and-installation/clarity-csp (verified 2026-06-01)

| Origin | Directive | Vendor doc status | Verdict |
|---|---|---|---|
| `https://*.clarity.ms` | `script-src`, `connect-src`, `img-src` | Required (entry tag + collection subdomains, e.g. `c.clarity.ms`, `e.clarity.ms`, `f.clarity.ms`, regional shards). Wildcard is the vendor-recommended form because the collection subdomain is resolved at runtime based on user region. | **Keep.** No tighter form available without breaking regional shards. |
| `https://c.bing.com` | `script-src`, `connect-src` | Required (beacon endpoint). Single hostname; no wildcard. | **Keep.** |

## Other vendor allowlist entries (incidental scan)

| Origin | Reason | Verdict |
|---|---|---|
| `https://challenges.cloudflare.com` | Turnstile widget assets + verification | Keep |
| `https://*.sanity.io`, `wss://*.sanity.io`, `https://*.sanity.studio` | Studio Presentation Tool drafts + live updates | Keep |
| `https://*.ingest.de.sentry.io` | Sentry error reporting (EU region) | Keep |
| `https://*.r2.cloudflarestorage.com`, `${R2_PUBLIC_ORIGIN}` | R2 signed-URL PUTs + public R2 origin | Keep |
| `https://api-js.mixpanel.com`, `https://api.mixpanel.com` | Mixpanel ingestion (browser SDK + server-side proxy) | Keep |
| `https://cdn.sanity.io` (`img-src`) | Sanity-hosted images | Keep |

## Next audit trigger

- Re-run on any Clarity vendor doc update (subscribed via Microsoft Learn RSS where available).
- Tied to dex Max-action `niv9cfvu` (Clarity provisioning checklist) — when `NEXT_PUBLIC_CLARITY_PROJECT_ID` lands in production, walk this audit again with real traffic flowing.

## Notes

- The wildcard `*.clarity.ms` is broad but vendor-recommended; tightening to specific subdomains breaks regional shards and is documented as a Clarity anti-pattern.
- No CSP violations observed in current production logs (last 30 days; Sentry CSP report endpoint).
